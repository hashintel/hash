/**
 * @param {AgentState} state
 * @param {AgentContext} context
 */
const behavior = (state, context) => {
  // Custom agent logic can be added here. Apply it to agents by including the
  // "custom" behavior in their behaviors[] set.
  state.height = 10;
};
