{
  // the outputs of the experiment (do not remove this comment - it is here to test that the
  // engine supports comments in configuration files)
  "outputs": {
    "loaded_stock": [
      {
        "op": "filter",
        "field": "color",
        "comparison": "eq",
        "value": "purple"
      },
      {
        "op": "get",
        "field": "stock_amount"
      },
      {
        "op": "sum"
      }
    ]
  },
  /* the plots to produce (do not remove this comment - it is here to test that the
     engine supports comments in configuration files) */
  "plots": [
    {
      "timeseries": ["loaded_stock"],
      "title": "Stock Loaded",
      "layout": { "width": "100%", "height": "50%" }
    }
  ]
}
