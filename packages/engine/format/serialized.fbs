// A collection of serialized (bytes).
//
// Can be used to send arbitrary information.
//
// fields:
//    `inner` : payload (bytes)
table Serialized {
  inner:[ubyte] (required);
}

root_type Serialized;
