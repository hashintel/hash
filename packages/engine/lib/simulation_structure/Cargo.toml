[package]
name = "simulation_structure"
version = "0.0.0"
edition = "2021"

[dependencies]
stateful = { path = "../stateful" }
execution = { path = "../execution" }

serde = { version = "1.0.137", features = ["derive"] }
