[package]
name = "error"
version = "0.0.0"
edition = "2021"

[dependencies]
provider = { path = "../provider" }

tracing-error = { version = "0.2.0", optional = true }

[build-dependencies]
rustc_version = "0.2.3"

[features]
default = ["std"]
std = []
spantrace = ["tracing-error"]
