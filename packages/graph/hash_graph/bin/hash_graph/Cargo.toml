[package]
name = "hash-graph"
version = "0.0.0"
edition = "2021"
publish = false


[dependencies]
graph = { path = "../../lib/graph" }
tokio = { version = "1.18.2", features = ["rt", "macros"] }
error-stack = "0.1.1"
