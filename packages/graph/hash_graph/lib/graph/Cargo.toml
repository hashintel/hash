[package]
name = "graph"
version = "0.0.0"
edition = "2021"
publish = false


[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# We probably don't need full.
tokio = { version = "1", features = ["full"] }
sqlx = { version = "0.5", features = [ "macros", "runtime-tokio-native-tls" , "postgres", "json", "uuid" ], default-features = false}

[dev-dependencies]

[build-dependencies]

[features]
