version: "3.9"

volumes:
  hash-graph-data:

services:
  postgres:
    build:
      context: ./postgres
    deploy:
      restart_policy:
        condition: on-failure
    environment:
      PGDATA: /var/lib/postgresql/data/pgdata
      POSTGRES_USER: "${POSTGRES_USER}"
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD}"
      HASH_GRAPH_PG_USER: "${HASH_GRAPH_PG_USER}"
      HASH_GRAPH_PG_PASSWORD: "${HASH_GRAPH_PG_PASSWORD}"
      HASH_GRAPH_PG_DATABASE: "${HASH_GRAPH_PG_DATABASE}"
    ports:
      - "${POSTGRES_PORT}:5433"
    volumes:
      - hash-graph-data:/var/lib/postgresql/data
      - ./postgres/postgresql.conf:/etc/postgresql/postgresql.conf:ro
      - ./postgres/init-user-db.sh:/docker-entrypoint-initdb.d/100-init-user-db.sh
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -p ${POSTGRES_PORT}"]
      interval: 4s
      retries: 5
      timeout: 30s
      start_period: 6s
    tmpfs:
      - /tmp
      - /run/postgresql
    command: -c 'config_file=/etc/postgresql/postgresql.conf'
    security_opt:
      - no-new-privileges:true
    read_only: true
