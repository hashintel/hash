overwrite: true
schema: ./src/graphql/typeDefs/*.ts
require:
  - ts-node/register
generates:
  ./src/graphql/autoGeneratedTypes.ts:
    plugins:
      - "typescript"
    config:
      skipTypename: true