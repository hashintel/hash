import { UnknownEntity, Visibility } from "../graphql/autoGeneratedTypes";
import { namespaces } from "./namespaces";
import { randomTimes } from "./util";

export const entities: UnknownEntity[] = (() => {
  const entityData: UnknownEntity[] = [
    {
      id: "text1",
      type: "Text",
      ...namespaces[0],
      ...randomTimes(),
      properties: {
        text: "Ciaran's Header Text",
        bold: true,
      },
      visibility: Visibility.Public,
    },
    {
      id: "text2",
      type: "Text",
      ...namespaces[0],
      ...randomTimes(),
      properties: {
        text: "A paragraph of regular text",
      },
      visibility: Visibility.Public,
    },
    {
      id: "text3",
      type: "Text",
      ...namespaces[0],
      ...randomTimes(),
      properties: {
        text: "A paragraph of italic text",
        italic: true,
      },
      visibility: Visibility.Public,
    },
    {
      id: "text4",
      type: "Text",
      ...namespaces[0],
      ...randomTimes(),
      properties: {
        text: "A paragraph of underline text",
        underline: true,
      },
      visibility: Visibility.Public,
    },
  ];

  return entityData;
})();
