overwrite: true
schema: ../backend/src/graphql/typeDefs/*.ts
generates:
  ./src/graphql/fragmentTypes.json:
    plugins:
      - fragment-matcher
  ./src/graphql/autoGeneratedTypes.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
    config:
      skipTypename: true
    documents: ./src/graphql/queries/*.ts