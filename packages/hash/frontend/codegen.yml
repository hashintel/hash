overwrite: true
schema: ../backend/src/graphql/typeDefs/*.ts
generates:
  ./src/graphql/fragmentTypes.g.json:
    plugins:
      - fragment-matcher
  ./src/graphql/autoGeneratedTypes.g.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
    config:
      skipTypename: true
    documents: ./src/graphql/queries/*.ts