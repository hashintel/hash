{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["./dist", "./.next"],
      "dependsOn": ["codegen"]
    },
    "codegen": {
      "outputs": ["./src/**/*.gen.*"]
    },
    "fix:eslint": {
      "cache": false
    },
    "lint:eslint": {
      "env": ["CHECK_DISABLE_UNTIL_FIXED"],
      "outputs": []
    },
    "lint:tsc": {
      "outputs": []
    }
  }
}
