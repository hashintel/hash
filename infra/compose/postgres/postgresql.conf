# Allow connections from all addresses
listen_addresses = '*'
log_min_messages = warning

max_connections = 300

# Loaded into every session but inactive until a session raises
# auto_explain.log_min_duration, which stays at -1 by default.
session_preload_libraries = 'auto_explain'
