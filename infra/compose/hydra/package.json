{
  "name": "@infra/compose-hydra",
  "version": "0.0.0-private",
  "private": true,
  "description": "Ory Hydra Docker image. Exists as a workspace package so turbo attributes Dockerfile/context changes to a `build:docker` task.",
  "license": "AGPL-3.0",
  "scripts": {
    "build:docker": "docker buildx build --tag hash-hydra --file Dockerfile . --load"
  }
}
