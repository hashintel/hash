coverage:
  status:
    project:
      default:
        informational: true
        target: auto
        threshold: 1%
    patch:
      default:
        informational: true
comment:  # Only post a comment if the coverage changes
  require_changes: true
github_checks:
  annotations: false
