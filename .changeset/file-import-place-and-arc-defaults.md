---
"@hashintel/petrinaut-core": patch
---

File import defaults a place's `colorId`, `dynamicsEnabled` and `differentialEquationId`, and an arc's `weight`, instead of rejecting a file that omits them.
