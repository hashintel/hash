---
"@hashintel/petrinaut": patch
---

Extract the contour plot into a presentational `ContourSurface` component shared by the sweep and optimization surfaces, decouple `SweepNavigator` from the experiments context, and add dedicated Storybook stories with fake providers covering each component's states and streaming flows.
