---
"@hashintel/petrinaut": patch
---

Removes the `viewportActions` prop and the top-bar `slots`; hosts install plugins through `PetrinautPluginsProvider` instead, and the Definitions view is the first built-in plugin.
