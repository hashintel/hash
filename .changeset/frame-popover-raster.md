---
"@hashintel/petrinaut": patch
---

The frame inspector popover draws a frame's distribution as a small canvas histogram with value and count axes, so the whole shape is visible at once instead of a dozen bins at a time behind a scrollbar.
