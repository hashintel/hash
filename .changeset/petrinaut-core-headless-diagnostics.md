---
"@hashintel/petrinaut-core": minor
---

Add a Node/tooling-only `@hashintel/petrinaut-core/diagnostics` entry exposing the editor's TypeScript checker headlessly: `checkDefinition(sdcpn)` type-checks every code-bearing item of a definition without a browser, alongside the underlying `checkSDCPN` and `SDCPNLanguageServer`.
