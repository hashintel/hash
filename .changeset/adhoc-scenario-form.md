---
"@hashintel/petrinaut": patch
---

Add the ad-hoc scenario form component: define Initial State + Parameters inline (fixed and template rows mixing per place, shared column values, Variables at two scopes, expression cells), with an `optimizable` mode growing an Optimize control on every value slot. Not wired into any view yet.
