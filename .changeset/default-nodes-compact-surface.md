---
"@hashintel/petrinaut": patch
---

The default (classic) canvas nodes draw the same surface as the compact nodes: a 1px border, the compact transition's border and fill colours, and a soft shadow. Their sizes and shapes are unchanged.
