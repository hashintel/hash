---
"@hashintel/petrinaut": patch
---

The default (classic) canvas nodes draw the same surface as the compact nodes: a 1px border, the compact transition's border and fill colours, and a soft shadow. Default transitions have square corners.
