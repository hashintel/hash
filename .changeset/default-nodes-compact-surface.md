---
"@hashintel/petrinaut": patch
---

The default (classic) canvas nodes draw the same surface as the compact nodes: a 1px border, the compact transition's border and fill colours, and a soft shadow. A default transition is a square with square corners, and its name wraps over up to three lines. A node name that is not written in PascalCase keeps all of its words.
