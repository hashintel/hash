---
"@hashintel/petrinaut": patch
---

Hosts can append their own items to the top bar's burger menu with the new `menuItems` prop, for things that belong to the surrounding application rather than the open net — an account or sign-in item, say. The `MenuItem` type is re-exported from `@hashintel/petrinaut/ui` so typing one needs no direct dependency on the design system.
