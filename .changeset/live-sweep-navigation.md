---
"@hashintel/petrinaut": patch
---

Sweep navigation feels continuous: a point selection uses a single-thumb slider (coincident range thumbs trapped the drag on the upper one, which could not move left), slider moves commit per quantized step during the drag instead of on release, and a re-stream after a parameter change keeps the chart's axes and grid in place — the y ceiling only ratchets up, and "Waiting for metric data" only shows before a metric's first data.
