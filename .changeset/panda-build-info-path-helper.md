---
"@hashintel/ds-components": patch
---

Add `resolvePandaBuildInfoPath` to `@hashintel/ds-components/preset`, which resolves a shipped `panda.buildinfo.json` to a path Panda's `include` accepts on every platform
