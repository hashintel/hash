---
"@hashintel/petrinaut": patch
---

Fix the Simulation Settings scenario picker overlapping its action buttons, keep the BottomPanel tab bar a constant height, fade truncated tab labels instead of wrapping, and adapt the Timeline header (shrinking metric picker, hidden label) to narrow panels.
