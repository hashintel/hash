---
"@hashintel/petrinaut-core": minor
---

Admit edits to existing parts of the net in `selectedMutationBatchSchema`: `updatePlace`, `updateTransition`, `updateArcWeight`, `updateArcType`, `updateType`, `addTypeElement`, `updateTypeElement` and `updateParameter` join the batch alongside the add, remove and `updateDifferentialEquation` operations. Canvas position updates stay outside the batch.
