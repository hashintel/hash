---
"@hashintel/ds-components": patch
---

Add a `dropdownFitContent` prop to `Select`: sizes the dropdown to its content (no wrapping) instead of matching the trigger width, staying at least as wide as the trigger and capped at 480px.
