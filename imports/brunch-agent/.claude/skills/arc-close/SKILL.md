---
name: arc-close
description: Run the arc-close sweep — inbox settle, INDEX pass, CONVERGENCE re-evaluation, registry audit, tense repair — per docs/agents/arc-close.md
---

Read `docs/agents/arc-close.md` (the canonical procedure — this wrapper carries no content of
its own) and execute its five steps in order. Report each step's outcome explicitly: a step
that finds nothing to do reports *clean*, never silence.
