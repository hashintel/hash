{
  "name": "brunch-lite",
  "private": true,
  "type": "module",
  "description": "Elicitation harness — agentic interviewing against pluggable elicitation targets",
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "lint": "oxlint --fix --deny-warnings .",
    "lint:check": "oxlint --deny-warnings .",
    "fmt": "oxfmt .",
    "fmt:check": "oxfmt --check .",
    "typecheck": "tsc -p tsconfig.json",
    "test": "bun test",
    "build": "bun run --filter '@brunch/dev' build",
    "smoke": "bun run lint:check && bun run fmt:check && bun run typecheck && bun test && bun run build"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "oxfmt": "^0.5.0",
    "oxlint": "^1.42.0",
    "typescript": "^5.9.0",
    "vite": "^8"
  }
}
