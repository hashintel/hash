/**
 * Multiplicative zoom step applied per zoom-in/zoom-out button click.
 *
 * @defaultValue 0.7. Larger values jump zoom levels faster per click, at the cost of
 * coarser control near a target zoom level.
 */
export const GRAPH_CAMERA_ZOOM_STEP = 0.7;
