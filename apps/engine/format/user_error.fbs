// `UserError` Message Body Type.
//
// Used by packages to signal of failures in user code.
// These errors are not necessarily specific to a simulation,
// but can be.
//
// fields:
//    `msg` : the error message
table UserError {
  msg:string (required);
}

root_type UserError;
