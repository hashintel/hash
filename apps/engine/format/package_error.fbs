// `PackageError` Message Body Type.
//
// Used by the runners to signal of failures in package code.
// These errors are not necessarily specific to a simulation,
// but can be.
//
// fields:
//    `msg` : the error message
table PackageError {
  msg:string (required);
}

root_type PackageError;
