{
  "name": "@apps/hash-agents",
  "version": "0.0.0-private",
  "private": true,
  "description": "HASH LLM Agents",
  "scripts": {
    "codegen": "yarn exe ./scripts/codegen.ts './agents/**/*.ts' && yarn fix:prettier",
    "exe": "ts-node --transpile-only",
    "fix:prettier": "prettier --write  --ignore-unknown ."
  },
  "dependencies": {
    "dedent": "0.7.0",
    "execa": "5.1.1",
    "quicktype": "^23.0.19",
    "typescript": "4.9.4",
    "yargs": "^17.7.1"
  },
  "devDependencies": {
    "@local/tsconfig": "0.0.0-private"
  }
}
