{
  "extends": ["//"],
  "pipeline": {
    "codegen": {
      "outputs": ["./agents/**/*.py", "./src/agents.ts"],
      "dependsOn": ["^build"]
    },
    "install:tools": {
      "cache": false
    },
    "build:docker": {
      "cache": false
    },
    "fix:black": {},
    "fix:ruff": {},
    "fix:lock-files": {
      "inputs": ["poetry.lock", "pyproject.toml"],
      "outputs": ["poetry.lock"]
    },
    "lint:black": {},
    "lint:lock-files": {
      "inputs": ["poetry.lock", "pyproject.toml"]
    },
    "lint:ruff": {}
  }
}
