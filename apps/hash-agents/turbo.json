{
  "extends": ["//"],
  "pipeline": {
    "install:tools": {
      "cache": false
    },
    "build": {
      "outputs": ["./agents/**/*.py", "./src/agents.ts"],
      "dependsOn": ["^build"]
    },
    "build:docker": {
      "cache": false
    },
    "fix:black": {},
    "fix:lock-files": {
      "inputs": ["poetry.lock", "pyproject.toml"],
      "outputs": ["poetry.lock"]
    },
    "lint:black": {},
    "lint:lock-files": {
      "inputs": ["poetry.lock", "pyproject.toml"]
    }
  }
}
