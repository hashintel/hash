use quote::{ToTokens, __private::TokenStream};

use crate::input::QueryBuilderInput;

impl ToTokens for QueryBuilderInput {
    fn to_tokens(&self, tokens: &mut TokenStream) {
        todo!()
    }
}
