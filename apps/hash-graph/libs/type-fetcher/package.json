{
  "name": "@rust/type-fetcher",
  "version": "0.0.0-private",
  "private": true,
  "scripts": {
    "start": "../../../../target/release/hash-graph type-fetcher",
    "start:test": "../../../../target/debug/hash-graph type-fetcher",
    "start:test:wait": "../../../../target/debug/hash-graph type-fetcher --healthcheck --wait --timeout 10",
    "start:wait": "../../../../target/release/hash-graph type-fetcher --healthcheck --wait --timeout 10"
  },
  "dependencies": {
    "@blockprotocol/type-system-rs": "0.0.0-private"
  },
  "devDependencies": {}
}
