import { Box } from "@mui/material";
import { FunctionComponent, PropsWithChildren } from "react";

export const Hint: FunctionComponent<PropsWithChildren> = ({ children }) => (
  /**
   * @todo: implement the `Hint` component, or remove it from the MDX files.
   */
  <Box>{children}</Box>
);
