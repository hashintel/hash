/**
 * To increase determinism between Open AI LLM requests, a fixed `seed` can
 * be used in requests to help make outputs more consistent.
 *
 * @see https://cookbook.openai.com/examples/reproducible_outputs_with_the_seed_parameter
 */
export const openAiSeed = 1234;
