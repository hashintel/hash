{
  "name": "@rust/chonky",
  "version": "0.0.0-private",
  "private": true,
  "scripts": {
    "fix:clippy": "just clippy --fix",
    "lint:clippy": "just clippy",
    "test:unit": "cargo hack nextest run --feature-powerset --exclude-features static --all-targets && cargo test --all-features --doc"
  },
  "dependencies": {
    "@rust/error-stack": "0.6.0"
  }
}
