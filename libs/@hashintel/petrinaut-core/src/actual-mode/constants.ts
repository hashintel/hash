export const ACTUAL_MODE_TIMELINE_TICK_MS = 500;

/**
 * The recording version this package writes and reads. Version 3 firings
 * carry token values (`inputTokens` / `outputTokens`).
 */
export const ACTUAL_MODE_RECORDING_VERSION = 3;
