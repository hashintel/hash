/**
 * Shared model/grid unit used by bundled example SDCPNs.
 *
 * This lives in core because example documents are headless domain data, but
 * their persisted node coordinates still need to line up with the editor grid
 * rendered by `@hashintel/petrinaut`.
 */
export const GRID_SIZE = 15;
