/**
 * `@hashintel/brunch-agent-plugin-gherkin` — the software-behavior domain
 * typology paired with the Gherkin target formalism.
 *
 * The plugin is a contribution bundle: `prompts/`, `skills/gherkin-specification/`,
 * and `flue.ts`. No parser, step-binding, or execution tool is earned yet. The
 * retired YAML definition and verbatim-floor proposal type were removed on
 * 2026-09-02. This root export carries the pairing's identity only; the
 * `./flue` subpath owns the contribution bundle.
 */

export const GHERKIN_DOMAIN_TYPOLOGY = "software behavior";
export const GHERKIN_TARGET_FORMALISM = "gherkin";
