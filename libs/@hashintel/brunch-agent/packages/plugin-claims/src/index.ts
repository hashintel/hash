/**
 * `@hashintel/brunch-agent-plugin-claims` — claims with justificatory
 * dependencies paired with statement cards in an external claims ledger.
 *
 * This is a stubbed contribution bundle. It holds the pairing so the
 * core/plugin architecture is pressure-tested against a normative-source
 * mode that SDCPN and Gherkin do not exercise. No ledger API, verifier, or
 * application mount is earned; the `./flue` subpath packages the authored
 * prompt and skill only.
 */

export const CLAIMS_DOMAIN_TYPOLOGY = "claims with justificatory dependencies";
export const CLAIMS_TARGET_FORMALISM = "statement cards";
