/**
 * `@hashintel/brunch-agent-plugin-dafny` — the software-correctness domain
 * typology paired with the Dafny target formalism.
 *
 * This is a stubbed contribution bundle. It holds the proposed homes for the
 * third pairing so the two-level core/plugin architecture is pressure-tested
 * against a formalism whose transformation boundary (formalization and proof)
 * differs from both SDCPN and Gherkin. No definition, proposal type, tool, or
 * verifier integration is earned yet; the `./flue` subpath mounts only the
 * stub prompt and skill.
 */

export const DAFNY_DOMAIN_TYPOLOGY = "software correctness obligations";
export const DAFNY_TARGET_FORMALISM = "dafny";
