{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "inputs": [
        "$TURBO_DEFAULT$",
        "$TURBO_ROOT$/libs/@hashintel/brunch-agent/library-vite-config.ts"
      ]
    },
    "test:unit": {
      "dependsOn": ["^manifest"],
      "env": ["TEST_COVERAGE"],
      "inputs": [
        "$TURBO_DEFAULT$",
        "$TURBO_ROOT$/libs/@hashintel/brunch-agent/library-vite-config.ts"
      ]
    }
  }
}
