/**
 * Legacy persisted tool identity. New conversations never mount or emit this
 * tool; history projectors use the name only to hide old rows safely.
 */
export const BRUNCH_QUESTION_TOOL_NAME = "brunch_mark_question";
