# First construction progression — review and visible-result correction

## Review disposition

Independent read-only review of `c41aeb54ca` through `476370d652ef6abdf5bc808042a8d916ea04a920` found no confirmed first narrow-path data/control safety blocker. One fresh actual-Chrome probe passed with **23 synthetic requests, two applied records and nine completed checkpoints**; 31 focused admission/reconciliation tests also passed. Native creation and weight correction cite verified prior raw bases, record actual effects and distinguish origin/current change/attempt. The faux callback oracle has outside completion counts and unique successful text gates; it does not exhibit the passage draft's demonstrated presence-only false green.

The installed artifacts and available source maps were inspected. The reviewer could verify all 24 available application source-map entries, but did not rebuild and could not independently prove a reproducible website build merely from bundle markers/hashes. Full producer build evidence and a parent integration replay remain required. Broader root classes, secondary-effect-producing operations, epochs, real-provider compatibility and semantic usefulness remain unearned. The full review and exact raw observations are retained here; its secondary source concerns are explicitly unconfirmed/retracted, not new safety policy.

**Milestone credit remains withheld for a visible false-success result.** The reviewer and producer independently noticed it, and the parent inspected `primary/browser.png`: the generic card shows a green check and “Updated arc weight …: 4”, while the actual properties field remains 3, the result is `applied: false`/stale, and the assistant correctly says refused. This is not a false browser record, but the product must not tell its user the refused change applied.

## Owning-boundary trace and bounded decision

The existing canonical `AiToolOutput` already includes `applied: false` with a reason. Stock mutation execution can return that contract for no change, and the Brunch host retains it. In `libs/@hashintel/petrinaut/src/ui/views/Editor/panels/ai-assistant-panel/ai-assistant-contents/tool-list.tsx`, generic summary fallback uses requested arguments, tone defaults to success, and the completion icon follows output availability; `applied: false` is ignored. The request-only summary helper is not itself an observed effect oracle.

Under the existing AFK delegation, authorize the narrow shared-renderer correction proposed by construction:

- For completed `output-available` results with explicit `applied: false`, show **Not applied**, the actual reason where available, neutral tone and a non-success icon. Never derive a successful requested-value summary for that result.
- Preserve canonical protocol state, raw output, execution, admission, continuation and history. Preserve actual applied summaries, read-tool behavior, interactive widgets and dangerous/error presentation; no Brunch-specific provenance vocabulary enters Petrinaut.
- Change the group's count label from **changes** to **operations**, because completed reads, declines and no-ops are not applied changes. This is result honesty at the same renderer, not a panel/layout redesign.
- Add actual-renderer applied/error/blocked/declined/no-op/stale controls, plus a DOM assertion and new screenshot at the original actual-Chrome stale boundary. Keep original red screenshot/result/field observations unchanged.
- Limit changes to `tool-list.tsx`, its existing `ai-assistant-contents.test.tsx` tests, `docs/ai-assistant.md`, one Petrinaut-only patch changeset, and the candidate browser assertion. Follow the affected published package's normal verification/docs gates. Other first-path product/test source remains frozen until this correction is reviewed.

The partially obscured pane is an explicit unpolished/utility limitation, not permission for a broader redesign. No class, paid-run, genuine-construction, semantic-utility or Step A/B acceptance is granted. After correction and focused re-review, continue the construction lane toward the required root classes rather than declaring this two-operation path complete.

## Retention

`artifact-manifest.json` preserves hashes for 22 review artifacts, including the report, original mistaken-CWD test invocation, corrected test invocation, source/build pins, actual browser records/why/refusal/history, compressed request captures and screenshot. Newly packed files preserve exact source bytes. Original SQLite stores remain at their original temporary paths; no saved history or projection becomes state-reconstruction authority. Alpha has not integrated the new progression/registration yet; the optional transport prerequisite is already integrated separately. No provider egress or paid allocation occurred.
