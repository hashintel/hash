# Local recovery implementation handoff

> Historical result; raw contract-establishment payloads were retired in the [bounded reduction](../landing-20260909/evidence-reduction.md). Original commands, manifests and payload links below describe the recorded run, not a maintained replay or a claim that those bytes remain local.

## Result

**Both authorized Flue recovery corrections are implemented and locally verified.** Final built-route safety packet `a4-safety-N6GqzEeW` passes **11/11 checks**. The forced affected portfolio passes **63/63 uncached tasks and 1,573 tests**. Installed native/schema, accounting and actual Chrome/browser-result oracles pass. No paid application/provider call, upstream work, new store/ledger, Step B or acceptance claim.

Worktree: `/Users/lunelson/.herdr/worktrees/hash/m7-local-recovery`; branch: `ln/fe-1573-local-recovery`; exact reconciliation base: `b5f320b90c753e2b1eeded5455e0f8d02e06584b`. The initial blocked evidence milestone **`b78a30c4be`** remains intact. **`a521cfd109`** promotes diagnostics into red safety assertions; **`a5daa71373`** installs the local repair and extended overflow/Stop controls. This following evidence commit retains the complete packet. No push.

See [recovery-result.md](recovery-result.md) for source causality, exact fault matrix, red/green evidence, current-state render discipline and limits. No expected-failure diagnostic exit is counted as repaired safety.

## Maintained changes

- `.yarn/patches/@flue-runtime-npm-2.0.3-192c31f50c.patch`: three production JavaScript changes on the exact published Flue **2.0.3** distribution. Pending hook state shares the live outcome append; durable repair shares one append for buffered state/new outcomes/result commitment; successful-stop silent overflow compacts and keeps its already-completed response instead of continuing from an assistant tail. Three package documentation updates state these semantics and limits. No store schema/format, record type, Pi guard, provider identity or generation-policy change.
- `yarn.lock`: only the existing Flue patch locator hash/checksum change, **`49a743` → `194e8a`**. Root resolutions and `.yarnrc.yml` remain unchanged. All workspace manifests retain their exact original versions. Both Pi patches, native Standard Schema dependency wiring and synchronous built-in preparation correction remain installed.
- `apps/brunch-agent/test/history-retention-{crash.integration.ts,runtime-hook.ts,diagnostics.sh,audit.py}`: prospective exact public-state and same-batch assertions; independent synchronous direct-boundary kill; plain, observe, before/after-outcome and interrupted-repair controls; all final recovery processes uninstrumented; exact source/build snapshots and read-only original-store audit. `history-retention-diagnostics.sh` is now a **safety** runner, despite its retained filename.
- `apps/brunch-agent/test/history-retention.integration.ts` and `.test.ts`: retain the threshold/reopen/marker/correlated-result oracle and add successful-stop silent overflow, explicit-error continuation and active summarizer cancellation. Four cases are discovered by the existing unit script. No shared package script or hermetic inventory insertion is required; no new substrate importer was added.

Core's one render-captured setter and raw history/call/result/revision identities remain unchanged. There is no separately memoized setter, reconstructed state from historical JSON, generic-runtime Brunch state, altered tool termination, new storage system or application implementation. Ordinary mixed-batch safety, question marker, admission/accounting registration, causal client results, legacy prepared path, Voice/Stop and production core/plugin/app/website code remain unchanged. The identity audit checks **1,108 protected source files** and exact immutable prior packet trees; use its recorded hashes rather than inferring immutability from prose.

## Verification earned

| Boundary | Result and retained evidence |
| --- | --- |
| Guard prerequisite | `network-guard.log.gz`: Java and Node descendant external EPERM under all three retained profiles, deny/commit loopback EPERM and browser loopback positive control. Passed before the re-entry build. |
| Local generator prerequisite | `jar-reuse.json.gz`: regular non-hardlinked/non-symlink copy of alpha's already-local, incident-disclosed OpenAPI Generator 6.6.0; source and destination both **27,103,489 bytes**, SHA-256 **`9718ff7844e89462c75dcd9b20a35136f6db257bfe1b874db1e3002e99de4609`**. Source unchanged, destination ignored/uncommitted. No new download; original acquisition is not relabelled offline. |
| Actual patch installation | `install-recovery-exact.log.gz`, `install-immutable.log.gz`; public type fixture and exact Standard Schema resolution pass. `identities.json` proves the installed runtime differs from the previously installed native-patched runtime by exactly the three named source substitutions, with other pinned native files unchanged. |
| Definitive TDD red | `a4-safety-uL79Q3by`: overall exit 1. Successful-result/missing-current-state and invalid assistant-tail continuation fail prospectively. Controls separately distinguish eventual state from same-batch atomicity. Initial failed instrument/time-limit attempt is retained separately. |
| Forward revision crash safety | Final seven cases all pass exact Markdown/hash/pointer/current state and next ordinal 2 with two distinct IDs. Both normal and recovered outcome boundaries have matching state before replacement boot; interrupted-repair state/outcome/result are atomic. See `recovery-result.md`. |
| Overflow and cancellation | Silent 20 → 3 fold completes the retained successful stop; next actual input/reopen succeeds, without invented input or completed tool reissue. Synthetic explicit error retries from a valid retained user tail. Stop cancels an active summarizer and prevents its summary/retry while preserving the previously successful response. No universal provider-error or unfinished-response cancellation claim follows from that last case alone. |
| Installed native runtime | `native-runtime/{candidate,native-input-schemas}.json.gz`: **19 named observations, 26 synthetic SDK responses, zero intercepted network attempts**. Both SDK entrypoints, native default/recursion/refinement/normalization/parsed-once behavior, hooks/cancellation, generic/Valibot behavior and admission controls pass. |
| Built native mount | `native-mounted-final.log.gz` and `native-mounted-final/`: **20 synthetic SDK responses**, both provider entrypoints, native schema carriage and successful correlated waiting-client controls pass. |
| Built accounting | `accounting-final.log.gz`, copied `accounting-final-report.json.gz`: **13 outcomes**, six main-process synthetic starts/dispatch observations. No real provider invocation; unknown-spend, no-retry, late usage/no late publication, restart and disabled-route controls retained. Original shared ledgers unchanged. |
| Actual native browser | `a4-safety-N6GqzEeW/browser/`: real local Chrome and **ten synthetic native SDK responses**; read → normalized root arc → actual verified transition record → one continuation. Boolean/unknown-citation controls have zero browser results/effects; duplicate/reload do not reapply; contradictory delivery does not continue. Browser/listener/origin-block counters are empty. |
| Threshold/reopen on actual browser records | Same original browser store: **46 → 3** context fold, **23 original public records preserved**, exact revision retained, authorized process reopen and no tool reissue. Saved history is only an equality oracle. No second live browser after process restart or product why claim. |
| Rendered inspection | Inspected `browser/browser.png` and `browser/boolean-refusal.png`: labelled test-authored prepared route, expected native Boolean and unknown-citation refusals, final “Added input arc” and correlated verified-result prose. This is not genuine construction, a why pane or a microphone/audio-provider witness. |
| Forced affected portfolio | `portfolio.log.gz`: **63/63**, zero cache hits; core 107, plugin 69, binding 20, transport 44, app 262, Petrinaut 692, website 379 = **1,573 tests**. All selected build/type/lint tasks pass, unchanged original mixed-batch assertion included. App files remain serial. |
| Architecture/public types | `architecture.log.gz`: 70 layers / 356 edges; `native-public-types.log.gz`, `standard-schema-resolution.log.gz`: pass. Historical type fixture compiled only, not executed or repinned. |
| Format/whitespace | `format-final.log.gz`: root formatting passes. Source-only default `git diff --check` and patch-aware check pass. The default whole-diff check exits 2 only for generated unified-patch context prefixes; its complete warnings are retained in `git-whitespace-generated-context.log.gz`, not erased or called a clean default check. |

## Commands and reproduction

Commands are from repository root. `E=libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/native-local-delivery`; this fresh output root is `O=libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/local-recovery-wnhhyezf`. All safety runners allocate their own fresh `mktemp` subdirectories. Use a **new** output root for reproduction, not these retained artifacts.

```sh
node "$E/verify-network-guard.mjs"
sandbox-exec -f "$E/deny-network.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 YARN_ENABLE_SCRIPTS=0 yarn install --immutable
sandbox-exec -f "$E/deny-network.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 CARGO_NET_OFFLINE=true yarn exec turbo run build --filter=@apps/brunch-agent --force --concurrency=1
sandbox-exec -f "$E/deny-network.sb" bash apps/brunch-agent/test/history-retention-diagnostics.sh "$O" recovery
sandbox-exec -f "$E/deny-network.sb" node "$E/installed-runtime.mjs" "$O/native-runtime"
sandbox-exec -f "$E/deny-network.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 yarn workspace @apps/brunch-agent exec vitest run test/history-retention.test.ts
sandbox-exec -f "$E/deny-network.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 CARGO_NET_OFFLINE=true yarn exec turbo run build test:unit lint:tsc lint:eslint --filter=@hashintel/brunch-agent --filter=@hashintel/brunch-agent-plugin-sdcpn --filter=@hashintel/brunch-agent-binding-flue --filter=@hashintel/brunch-agent-transport-aisdk --filter=@apps/brunch-agent --filter=@apps/petrinaut-website --filter=@hashintel/petrinaut --continue=always --force --concurrency=1
sandbox-exec -f "$E/deny-network.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 VITE_BRUNCH_CHAT_ENDPOINT=/agents/chat yarn workspace @apps/petrinaut-website build
sandbox-exec -f "$E/loopback-only.sb" bash apps/brunch-agent/test/history-retention-diagnostics.sh "$O" all
sandbox-exec -f "$E/deny-network.sb" env M7_NATIVE_OUTPUT="$O/native-mounted-final" YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 yarn workspace @apps/brunch-agent test:native-schema
sandbox-exec -f "$E/deny-network.sb" env HASH_OTLP_ENDPOINT='' node --experimental-strip-types apps/brunch-agent/test/provider-accounting.integration.ts
sandbox-exec -f "$E/deny-network.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 yarn workspace @local/petrinaut-arch-docs lint:arch-docs
sandbox-exec -f "$E/deny-network.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 yarn exec tsgo --project libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/native-schema-20260908T150034Z/tsconfig.json
sandbox-exec -f "$E/deny-network.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 yarn why @standard-schema/spec
sandbox-exec -f "$E/deny-network.sb" python3 "$O/identities.py"
sandbox-exec -f "$E/deny-network.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 yarn lint:format
```

Successful commands exit 0. The definitive pre-patch recovery command intentionally exits **1**, not an expected-failure pass; deliberate creation/intermediate SIGKILL subprocesses exit **137**. Each safety packet's `commands.log.gz` records exact expanded commands/expected/actual exits. Root logs retain each other command's output and the table above identifies the instrument. Protected artifact hashes, source/patch/build identities and compressed/uncompressed evidence hashes are retained in `identities.json`, per-run `source-build-identities.json.gz` and `manifest.json`.

### Yarn authoring and installation details

Install the committed patch with the guarded immutable command above; do not manually copy JavaScript into installed modules. All required packages and the pinned JAR must already be local. A missing artifact fails closed, as the preserved `b78a30c4be` attempt did. The user explicitly authorized the one local JAR copy from alpha; no general permission to modify shared/hardlinked node_modules follows.

Authoring used guarded Yarn 4.16.0 `patch @flue/runtime@npm:2.0.3 --update`, edits only in Yarn's extracted `user` directory, then `patch-commit -s`. **Observed tooling pitfall:** the explicit npm locator extraction did not include the root-resolution native patch despite Yarn's generic “along with its current modifications” message. Review caught the incomplete generated patch **before installation**; it is retained as `not-installed-incomplete-patch.patch.gz`. The exact inherited native patch from the safety milestone was reapplied to that extraction, then Yarn regenerated the complete combined patch. A patch-tool `.orig` backup was removed from the owned extraction before final generation; one typo-path patch-commit failure is also retained. No incomplete/backup-bearing runtime was installed.

Yarn's authoring command rewrote workspace dependency descriptors; they were restored to the exact base versions, retaining centralized root resolutions. During this restoration, four mistakenly ranged descriptors caused one configured-offline install refusal (`install-recovery.log.gz`, YN0080); they were corrected back to exact `2.0.3` before the successful install. No resolution lookup succeeded, dependency upgrade occurred or changed workspace manifest remains. `install-recovery-exact.log.gz` is the first successful complete-patch install; `install-immutable.log.gz` verifies the final lockfile. Source maps remain the published upstream maps, not regenerated mappings of modified bundle lines. No upstream source build or upstream regression-suite claim.

Do not format the generated unified patch. Default `git diff --check` reports its newly added context-prefix spaces/tabs as whitespace; these are generated patch syntax, not changed source whitespace. Exact checks were `git diff --check b5f320b90c753e2b1eeded5455e0f8d02e06584b HEAD -- . ':(exclude).yarn/patches/@flue-runtime-npm-2.0.3-192c31f50c.patch'` and `git -c core.whitespace=-blank-at-eol,-space-before-tab diff --check b5f320b90c753e2b1eeded5455e0f8d02e06584b HEAD`, both exit 0. No project-wide whitespace policy was changed.

## Scope and limits

This earns forward consistency for the reached single-revision outcome/repair windows and valid handling of retained successful-stop silent overflow, plus the named explicit-error and cancellation controls. It does **not** repair already-inconsistent legacy stores, provide per-tool isolation for arbitrary parallel state writers, guarantee external-effect rollback or power-loss durability, or fix every partial/provider-error continuation. No model-response retry is invented for a successful stop. Existing first-write-wins results and unknown-outcome policies remain.

The local guard demonstrates descendant direct-socket enforcement, not an exhaustive privileged-service or packet/DNS audit. The inherited browser profile permits localhost generally; exact-port isolation is not claimed. Chrome is real, uses its existing local binary, and blocks non-origin requests; provider responses are synthetic. The initial guarded missing-JAR attempt and the original native-delivery acquisition incident remain honestly disclosed; copying the incident-disclosed artifact does not relabel its acquisition offline.

A5 product evidence/why/pane, genuine Vestera, real-provider schema acceptance, broader scenario classes, shared-browser/crash breadth, reviewer utility, paid readiness, Step A acceptance and Step B remain owner-held later gates. Repeat A5 joins on this repaired runtime after integration. No parent script/inventory insertion is requested. Original protected ledgers remain five paid calls / US$0.09113535, zero outstanding reservation; this chunk makes no paid reservation or synthetic write to them.
