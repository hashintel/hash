# Bounded evidence reduction

Authority: Lu's instruction to proceed, recorded separately in `b4030f1`. This pass removes redundant contract-establishment payloads, not executable regressions or product state semantics. The persona/browser checkpoint remains synthetic; a sophisticated elicitation session is still the next product result.

## Retention decision

Completed synthetic request/context dumps, repeated screenshots, build/source pin forests and per-lane replay payloads are **intentionally not preserved** unless listed below. Intermediate Git commits are provenance, not a promised recovery service. There is no replacement archive, no claim that deleted payloads remain available, and no claim that current code reproduces the historical instrument byte-for-byte.

Keep the small Markdown decision/result notes as historical interpretation, not current replay instructions. Their original counts, commands, temporary paths and payload references describe the original run; they do not assert present local availability after this disposition. In particular, the former native `installed-runtime.mjs` replay chain and landing verification manifest/payload are retired together. Use maintained tests and current tooling for new checks. Further consolidation of historical prose is not necessary to complete this pass.

### Coherent exceptions kept unchanged

- `a1-paid-2026-09-08T08-44-14-222Z/`: unique real `addType` contact; not genuine elicitation or current carrier breadth. Its unexpected ignored database is untouched and is not added to Git.
- `usage-ledger.json`, `attempt-ledger.md`: sole live accounting. Six attempts, five complete, US$0.09113535 recorded spend, one unknown request and US$7 held; no release or allocation.
- `a5-real-provider-20260909-r2-outcome/`, r2 and r1 activation packets, and `real-provider-a5-final-sibling-preflight/`: intact contact/authorization/frozen-instrument context. Old grants remain consumed. Historical removed-worktree paths are not live launch instructions; externally preserved raw state is untouched.
- Whole `a5-process-retention/` and `a5-process-retention-integration-alpha/`: conservative coherent retention of the reviewed process proof, its falsifier context and manifest relationships. `reviewed/`, not `final/`, is the accepted result. This exception avoids partial-manifest surgery; it does not establish portability or permanent retention value.
- `landing-20260909/typed-original-review/` and `review-artifact-manifest.json`: independent aggregate counterexample.
- Historical rationale, the unchanged comparative assessment, current class table and current persona/browser integration note. The class table remains diagnostic, not an execution queue.

The separate Voice/evaluation evidence trees, private session archives, refs, configuration and unexpected files are outside the deletion set.

## Living consumers moved

Nine regression files (110,458 stored bytes) now live beside app/plugin tests under `test/fixtures/`. Their local README files retain original paths, stored-byte SHA-256, consumer and synthetic-capture limits. All bytes and file modes are unchanged; no assertion was weakened. These histories are regression premises, not seed/import authority.

The five-file guard unit (4,374 bytes) now lives at [`evaluations/protocols/network-guard/`](../../../../../evaluations/protocols/network-guard/). Java/profile-relative lookup and profile bytes are unchanged. Future instrument enumeration includes the complete unit; historical manifests were not repinned. The diagnostic runner defaults to fresh temporary output rather than repopulating this campaign; an explicit output base still works.

## Surviving reasons

- Native schema input preservation and validation-before-coercion: [boundary findings](../native-schema-20260908T150034Z/native-schema-boundary.md).
- Single-browser proposal refusal: [observed continuation gap and decision](../single-browser-admission/decision.md).
- Carry conservation and structural-versus-semantic limits: [passage result](../passage-policy/passage-identity-result.md).
- Object-key-order reconciliation only, never relaxed mutation bases: [decision](../a5-serialization-equivalence/decision.md).
- Forward-scoped atomic recovery and completion-oracle correction: [recovery result](../local-recovery-wnhhyezf/recovery-result.md) and [review correction](../local-recovery-review-KWim2OUc/review-correction.md).
- Canonical capacity semantics: [owning decision](../capacity-preservation/decision.md).
- Conservative aggregate attribution: [decision](shared-aggregate-decision.md) and the retained original counterexample.
- Network download incident and distinction from paid calls: [incident](../native-local-delivery/network-incident.md). Its retired logs are historical evidence, not a present zero-network certification.

The exact deleted paths are reviewable in this change's Git diff. Selection was mechanically enumerated and byte-checked before deletion; no extension-wide or unclassified recursive deletion was used.

## Verification

- Evidence tree: **7,763 files / 218,663,247 logical bytes before → 707 files / approximately 15.8 MB after** (about 92.8% less payload; filesystem allocation now 17M). Exactly **7,043 files / 202,784,057 bytes** were retired; fourteen files moved out, and this index was added. Counts include the untouched ignored paid-corpus database; it remains untracked.
- Parent post-retirement replay: four app fixture-consumer files **46/46 tests**, plugin reconciliation **3/3**. All fourteen relocated inputs match original bytes, SHA-256 and modes.
- Parent independently ran the moved verifier: all three profiles pass with actual shell → Java/Node descendant denial and the expected loopback distinction. Shell syntax, fresh default/explicit temporary destinations and complete guard enumeration pass. No full installed/build refreeze or browser campaign.
- Parent compared every protected packet/root authority/unexpected file against its pre-deletion hash; all unchanged. The r2 freeze manifest remains `1c71ed054d89a7a0183df5b8877faba042982638a58c6696151fc119df439586`.
- Maintained repository source/command references were scanned, including dynamic fixture paths and future instrument enumeration. Only a historical source-provenance comment still names a moved campaign fixture. New index links resolve. Retained historical notes explicitly identify retired-payload status; protected original notes remain unchanged and are interpreted under this index.
- A separate read-only review of the parent's retirement delta at `532fddb` found no blocker: exact deletion membership, 612 original protected files' bytes/modes, maintained consumers, dynamic enumeration and status/index links were checked. The reviewer authored the earlier relocation, not the retirement; this is independent deletion review, not an independent reimplementation of the whole pass.
- `git diff --check` passed. Parent temporary logs: `/tmp/m7-reduction-parent-{app,plugin}.log`, `/tmp/m7-parent-relocated-guards.log`, `/tmp/m7-reduction-protected-check.log`. These are local check logs, not a new promised archive.

No paid request, authentication request, ledger mutation or external archive removal occurred. The pass closes evidence maintenance, not persona quality, portability, provider readiness or mission acceptance.
