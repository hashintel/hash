# Template delivery checkpoint — return to elicitation

## Disposition

Lu selected stable templates with independent working sessions, then explicitly directed that delivery implementation no longer precede Mission 7 elicitation. Local Postgres support is committed at `988ad5c`; stable-template authority was `09ce874`. Seeder, catalogue, picker and production clone implementation are not shipped or claimed complete. No remote write, rollout or paid inference occurred during this investigation.

The accepted future contract lives in `MISSION.next.md` under Stable template delivery after valuable elicitation. Resume with the real local persona/browser session; do not make same-browser copies or startup concurrency another prerequisite. The former guidance-remediation side quest remains suspended, not accepted or resumed.

## Evidence and limits

Parent verified the production Postgres startup and explicit development Postgres actual browser path; see [Postgres departure](postgres-fixture-departure.md). The subsequent builder probes reported selective restore and limited copy feasibility. Parent read their code/report but did not independently rerun the copy experiment; these are bounded implementation-side feasibility results, not importer acceptance.

- `/tmp/brunch-selective-1788956780/result.md`: one settled, attachment-free real-API session selectively restored into a populated Postgres target. Actual UI continuation succeeded; unrelated rows remained equal; missing submission and repeated import refused. The source-bundle digest checked an exact known specimen, not a general dependency validator.
- `/tmp/brunch-copies-1788957771/result.md`: one source template and two separately owned copies coexisted with unrelated sessions. Each fresh Chrome profile opened inherited history/workpiece with zero model or old-tool execution, then independently settled a new revision (two operators versus three). Template and other-copy rows remained unchanged. Native submission parsing passed. No public clone API was found; only specifically typed identity/binding fields were transformed, not arbitrary content.

The copy experiment allocated new owner/application instance/native conversation/UID/submission/document-incarnation identities while preserving stream-scoped record/message/revision/evidence IDs, content, timestamps and hashes. It retained an explicit template origin outside product state. Production would need durable origin attribution and scoped references; inherited testimony must not be presented as newly elicited from the working owner.

Only a single settled root/default lane, empty bound document, no effects, attachments/chunks, child conversations, joins, compaction caches or pending actions was exercised. The tracer hardcodes a document key: two separate profiles passed, not two copies in one browser or under one principal. Runtimes were stopped and native tables locked; online instantiation and startup coexistence with an old ECS task are unproved. Native schema/version compatibility and broader identity consumers require verification when delivery resumes.

Both probe-created databases were removed and owned containers stopped, preserving the original local source database. Private bundles/browser identities stay in the restrictive temporary directories; no principals or database secrets were copied into this checkpoint. Child status/result/session were captured at `/tmp/brunch-template-checkpoint-agents/` before closing its pane.
