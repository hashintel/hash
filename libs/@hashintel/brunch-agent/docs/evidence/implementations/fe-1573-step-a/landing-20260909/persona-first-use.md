# First local persona use — stopped, not a fixture

## Result

After the template checkpoint, ALPHA integrated local two-participant accounting at `43a9763`. Parent verification: all 400 app tests passed; app types/lint passed, and the actual request-free development configuration selected Sonnet and the intended process API key. The raw attestation regression was test cross-control reuse of a deliberately poisoned ledger; isolated control processes preserve each control's first/second request safety, with all 25 original controls passing. No production poisoning or usage check was relaxed.

Parent allocated `vestera-persona-20260909-r1`: at most 160 combined calls / USD85, with output cap4096 and USD7 request hold covering catalogue bound USD6.06144; the pre-existing sequence6 unknown/USD7 hold was explicitly accepted against that allocation without changing its row. The resulting real use made 19 complete requests (4 Pi persona, 15 ChatAgent), costing USD0.35302395 in catalogue estimates. Shared totals are 25 attempts / USD0.4441593 confirmed catalogue spend, with only the original unknown/USD7 hold outstanding. Allocation is now blocked. No new usage/journal uncertainty occurred and no retry is authorized by that allocation.

The actual local development server and website used Postgres database `brunch_persona_20260909_r1`, exact `anthropic/claude-sonnet-4-6`, and the existing browser-attached persona bridge with host none. The initial UI opening was the scenario's shared operational opening; five native ChatAgent calls settled before Pi launch. The restricted real Pi CLI used a fresh private configuration, the same original accounting ledger, only `brunch_turn`, and the private approved situation pack. No successful record or model reply was substituted.

## Reasons for stopping

1. **Persona fidelity:** first persona testimony invented a dated order (“last Thursday”), SKU VW-01 and a 4,000-litre quantity absent from the pack. The pack and persona policy prohibit invented operational facts. Parent stopped Pi with SIGINT after observing this, preserved all material, and refuses to promote the session as trusted fixture material. The next bounded prompt correction must explicitly handle a request for a historical case the pack does not supply, without fabricating one; no new domain facts or elicitor answer key are authorized.
2. **Reached attachment gap:** opening and persona snapshots have different native root conversation IDs, with the browser opening absent from the latter projection. Parent initially aggregated the dedicated database without grouping stream paths and incorrectly inferred two roots in one instance. Independent inspection corrected this: there are two distinct instance streams, each with its own root; only the original browser stream carries initialData. Both use default/default lanes. The operator configuration had been verified against the original browser instance before Pi launch. Cause is established: the extension factory reads the custom browser-session flag before Pi applies CLI flag values, so it registers the default client. The persona instance exactly matches SHA-256 of the stock local principal and its actual PI_SUBAGENT_NAME. A credential-free actual Pi RPC/no-prompt discriminator observed factory flag absent, session_start flag present, and successful brunch_turn registration in session_start under the real tool allowlist. Evidence: `/tmp/brunch-flag-lifecycle.ITWilI/diagnosis.md`. No storage-loss or native-root-reset claim is established. Repair by resolving/validating attachment and registering the tool during session_start; a requested but invalid attachment must fail closed even if Pi continues after lifecycle-handler errors. The subsequent bounded repair resolves and validates attachment in `session_start`, invalidates stale tool closures before fallible cleanup, and rejects blank requested attachments. Independent review (`/tmp/alpha-persona-review.md`) found no blocker: seven actual restricted CLI invocations exercised native browser-derived admission with captured UID, missing/invalid attachments, and stale-closure refusal; 23 focused tests passed. The synthetic endpoint refused admission deliberately, so this is routing evidence, not completed elicitation. The persona's existing policy now explicitly handles unsupplied historical cases without inventing dates, identifiers or quantities; actual model compliance remains to be observed.
3. **Visible workpiece remains unearned:** the stopped browser UI still says current state has not been queried, while the persona used another stream without construction initialData. This is not evidence of poor update cadence in the intended joined path. The failed use establishes neither a sophisticated account, construction nor utility; do not redesign elicitor teaching from this wrong-route observation.

## Retention and operator path

Private run material is in `apps/brunch-agent/.data-wipe-me/vestera-persona-20260909-r1/`: original opening history, persona native session/events, bridge evidence, actual input, configuration, cost disposition and CDP screenshots. The situation pack and operator identities remain private and uncommitted. The Postgres database and browser profile are retained; neither is a portable/importable fixture claim. Before compaction, the owned dev server/panel process groups were stopped, the actual-run database had zero connected clients, and both owned containers were stopped without removal. The isolated browser was left open at that checkpoint; after resumption its CDP endpoint was unavailable. Its retained profile was not cleared. The diagnosis child status/result/session were saved under `/tmp/m7-persona-dev-continuity-settled/` and its pane closed.

CDP was used at Lu's request. The first page navigation encountered the panel startup failure (missing PWD in the minimal launcher environment); the panel was relaunched with PWD, without restarting the server or invoking a provider. A late page fetch observer missed the original POST because the SDK had already captured fetch. Parent instead obtained the exact persisted root initialData/UID, the actual browser principal and the canonical public route, then ran the existing public-history attachment validator without sending another turn. An initial storage-path/public-route mismatch refused before send and was corrected using the owning route/identity helpers. This was read-only operator configuration, not state reconstruction or rebinding.

The shared ledgers retain authoritative usage. No public inference credential, browser principal or database password appears in this note. The prior consumed r2 failure remains unresolved, not rewritten as zero-cost.
