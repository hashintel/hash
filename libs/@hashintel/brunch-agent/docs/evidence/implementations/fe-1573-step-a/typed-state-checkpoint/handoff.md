# Typed-state construction checkpoint — review handoff

## Disposition and freeze

Continue Mission 7 Step A's remaining CONSTRUCTION stratum from reviewed base `dd748a42399b6d0866156ef7e72002b41a1873ac`; this is not a new mission. The initially clean worktree was `/Users/lunelson/.herdr/worktrees/hash/m7-typed-state`, branch `ln/fe-1573-typed-state`. The [class/field matrix](class-table.md) preserves every remaining root obligation. **Production is frozen at `3b98cb287275c60c45087e4dc2500f626a39f163` pending independent review.** Do not treat the first typed path or these record totals as lane completion, provider-class admission, genuine construction, semantic utility, or Step A/B acceptance. After review, continue the required remaining root operations in this same mission; do not laterally rewrite this frozen seam during review.

| Commit | Ownership and write set |
| --- | --- |
| `788a9dcc1e` | Parent-owned serial coordination: `apps/brunch-agent/package.json` adds `test:typed-state`; `apps/brunch-agent/test/architecture/boundaries.integration.ts` admits the concrete native source and witness importer. Parent reported architecture 27/27 and retained its logs at `/tmp/m7-typed-inventory-owner.ZCQWmI`. |
| `3b98cb287275c60c45087e4dc2500f626a39f163` | This worker's 13 scoped production/test files below; signed under the exact local SSH socket allowlist. Signature verification succeeds. |

Worker source paths:

- Plugin: `libs/@hashintel/brunch-agent/packages/plugin-sdcpn/src/{root-state,root-node,root-arc,transition-record,index,flue}.ts` and `src/tools/petrinaut-construction.ts`.
- App composition: `apps/brunch-agent/src/conversation/{root-arc,why}.ts`.
- Browser: `apps/petrinaut-website/src/main/app/local-storage-demo/transition-record.ts`.
- Focused tests: `libs/@hashintel/brunch-agent/packages/plugin-sdcpn/test/root-state.test.ts`, `apps/petrinaut-website/src/main/app/local-storage-demo/typed-state.test.ts`, `apps/brunch-agent/test/typed-state.integration.ts`.
- Evidence-only additions live in this directory. No existing witness helper, sibling-browser implementation, paid driver/accounting algorithm, runtime patch, dependency/lock wiring, `app.ts`, MISSION, core evidence semantics or published Petrinaut source was changed by this worker. No published changeset/user-guide change is needed because no published package behavior changed.

`source-and-build-manifest.json` pins all 15 changed source/config files including the parent's two files, 314 built files, 11,354 installed runtime/schema package files, Chrome version/executable, and the authorized JAR. It verifies 11 protected Git file/tree objects remain identical to the reviewed base, including core, published Petrinaut/core, patches, lock/config, admission registration and the paid ledger. Build pins describe the exact final compiled path exercised; no historical instrument was repinned as if it were the same instrument.

## Actual throughline and results

The new witness uses the existing empty root-creation candidate browser route, a fresh document incarnation/conversation, production built ChatAgent, native input/provider serialization capture, actual Chrome, the production canonical browser callbacks/recorder, client-result continuation, and the actual model-facing `brunch_why`. It imports no prepared net, workpiece or conversation history. All user lines are labelled GENERIC TEST; the assistant responses are scripted faux-provider responses, not a real model or genuine testimony. Newly referenced types/places/elements/scenarios and construction locators come from actual tool results. The workpiece is settled by the one existing core state owner.

The first workpiece creates a type with nested text attribute, two typed places, and a scenario with two explicitly synthetic rows. The correction appends a boolean attribute (canonical false defaults migrate rows), changes text to integer (canonical coercion/defaulting yields 2 and 0), explicitly corrects the intended TEST initial values, renames the type, creates a transition with actual returned endpoints (generating a kernel), then deliberately makes the result place uncoloured (sanitizing that kernel). The canonical net check runs after those consequential corrections.

| Oracle | Final result |
| --- | --- |
| Forced serial affected portfolio | **66/66 uncached tasks, 3,251 unit tests**, exit0. Counts: Petrinaut-core1554, Brunch-core128, plugin101, binding20, transport45, app315, Petrinaut699, website389. Full app build includes its client build. |
| Actual typed Chrome | **71 synthetic requests; 52 externally counted callbacks**, with no page/server/blocked-origin/callback errors. |
| Actual records | **12 total: ten applied, one no-op, one stale**. Rejected pre-execution controls gain no browser outcome. |
| Actual direct/derived accounting | **37 created, five updated, zero deleted, seven derived entries.** Independent raw audit reconstructs every complete post-definition from its pre-definition and disjoint effects; independently recomputes raw hashes and checks actual earlier read/base/binding/call inputs. |
| Native input export | All six new canonical root input exports compare exactly after removing only the Brunch envelope, including metadata and input optionality. Actual serialized native requests match these canonical-composed exports. No Valibot round trip or strict-generation workaround. |
| Omitted scenario default | Raw provider/admitted history/request omit `parameterOverrides`; canonical callback input/post contain `{}`. The inserted field is derived; ordinary field why refuses inherited basis. Explicit `{}` remains distinguishable in the focused test. |
| Actual migration why | Derived row value query refuses with no governing request basis. After explicit correction, ordinary scenario/cell query identifies its creation origin, correction record and governing second revision. Unchanged cells do not acquire a new cause. |
| Generated/sanitized kernel why | Both actual generation and later sanitization are recorded as derived, and queried kernel fields refuse automatic support. |
| Reopened full raw state | Final recorded raw hash `ddc8a2006d6a8514b7c3b485f7a2b3c86f67c089dac9447afdc4d59959fff400`; reopened raw hash `eaf8de513fed70964d71b0a1c7d9b6b85d816d0557e8453b01468d5204fca028`. Independently hash-verified complete JSON content matches ignoring object-key insertion order only. Both hashes remain recorded; mutation bases are never aliased. |
| Post-correction canonical checking | Actual output is exactly `No errors detected in your model – everything compiles!`, asserted outside the faux callback exception trap and retained. No scenario execution, simulation, reachability or operational-rate claim follows. |
| Actual controls | Six new names reject mixed server/browser proposals at built registration; read+scenario rejects multiple-browser proposals. Duplicate type, duplicate/missing nested element, no-op, stale after actual UI element deletion, verified known-retired element, duplicate result, foreign/conflicting result and external-change why refusal pass. |
| Required-assertion falsifier | `M7_FALSIFY_TYPED_ASSERTION=1` exits1 through the outside callback oracle: actual first type count0 contradicts intentionally required1; callback error is retained, only two earlier callbacks completed. This is not a false green from a swallowed faux-provider exception. |
| Existing actual-browser regressions | Root-node witness remains 50 requests/36 callbacks/seven applied+no-op+stale. Arc/weight progression and prepared A3/A5 three-cohort reopened route pass unchanged. Their records/screenshots are archived separately; no new true-process compaction, Voice or remote durability claim is inferred. |

The independent audit executable is `audit.mjs`; it imports no production effect/hash/reconciliation helper. `browser-final/independent-audit.json.gz` records its complete operation/field matrix. This is an independent implementation of an evidence oracle, not a new mutation/provenance engine or production store. Browser JSON, raw native requests/contexts/deliveries/history, why outputs, complete observations, controls and screenshots are losslessly retained under `browser-final/`. Original live SQLite stores remain at the `/tmp` locations below; no saved history/projection was loaded into product state.

## Red/green and observed corrections

1. `red-output-contract/`: the first test incorrectly asserted that a successful canonical result equalled only `{applied:true}`. Actual `addType` also returns its title/selection target. The assertion failed outside the callback; the test now checks the actual `applied` field while preserving the complete output. This was a test API-shape error, not a product effect failure.
2. `red-default-projection/`: the next actual attempt reached the omitted-overrides scenario call but did not deliver its browser continuation; the test timed out after nine callbacks. Source inspection exposed the recorder comparing raw optional input against the canonical defaulted execution input. The fix compares the state operation's **canonical parsed execution projection**, never changes provider/call/history raw input, and retains field omission for effect classification. The focused browser test directly distinguishes omitted raw/defaulted callback/defaulted post, rejects a changed callback field, and the unchanged real omission path now passes with a derived-field why refusal. The original timeout's missing client-result boundary, contexts and raw calls are retained; no unavailable error screenshot is invented.
3. The first broad portfolio caught focused-test TypeScript/lint mistakes and an over-broad arc preflight affecting the old legacy failing-callback oracle. The preflight is now scoped to the distinct observation-bound construction request; legacy callback/error/count semantics remain unchanged and their test/browser replays pass. It prevents the newly reachable, unearned standalone derived-arc footprint before execution. This is not standalone arc-kernel admission.
4. The next portfolio passed65/66; a new host test omitted the required canonical output `title` in its mock type. Supplying the actual output contract fixed the test; production was not changed to fit the mock. The final portfolio passes66/66.

Metadata retention is explicitly pinned against the canonical owner, including `.meta()` after schema composition. Root-state Zod use is native composition and type inference only; core/query contracts stay Valibot-owned. Canonical actions, not copied migration logic, produce expected detached footprints, and only independent actual browser observations establish applied results. Unsupported code/ad-hoc scenarios, nested nets/components and standalone derived arc effects are visibly unavailable, not silently treated as supported.

## Guarded commands and artifact locations

The guard passed before installation/build/probes: `native-local-delivery/verify-network-guard.mjs` verified Java/Node descendants under deny, commit and loopback profiles. Immutable install was cache-only with lifecycle scripts disabled. The authorized alpha OpenAPI6.6.0 JAR was a regular-file copy only after size27,103,489/SHA256`9718ff7844e89462c75dcd9b20a35136f6db257bfe1b874db1e3002e99de4609` checks; source inode108379967 and target116486853 differed. It was not downloaded, linked, overwritten or edited. No guard failure was retried with networking enabled. All application/provider traffic was synthetic and process-tree denied except the explicitly scoped local browser/listener. No paid call, provider/auth/DNS probe, push, history rewrite or other-worktree cleanup occurred.

From repository root, with `E=libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/native-local-delivery`:

```sh
node "$E/verify-network-guard.mjs"
sandbox-exec -f "$E/deny-network.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 YARN_ENABLE_SCRIPTS=0 yarn install --immutable
sandbox-exec -f "$E/deny-network.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 CARGO_NET_OFFLINE=true VITE_BRUNCH_CHAT_ENDPOINT=/agents/chat yarn exec turbo run build test:unit lint:tsc lint:eslint --filter=@hashintel/petrinaut-core --filter=@hashintel/petrinaut --filter=@hashintel/brunch-agent --filter=@hashintel/brunch-agent-plugin-sdcpn --filter=@hashintel/brunch-agent-binding-flue --filter=@hashintel/brunch-agent-transport-aisdk --filter=@apps/brunch-agent --filter=@apps/petrinaut-website --continue=always --force --concurrency=1
sandbox-exec -f "$E/loopback-only.sb" env M7_TYPED_STATE_OUTPUT=/tmp/m7-typed-browser-final HASH_OTLP_ENDPOINT='' yarn workspace @apps/brunch-agent test:typed-state
sandbox-exec -f "$E/deny-network.sb" node libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/typed-state-checkpoint/audit.mjs /tmp/m7-typed-browser-final
```

Use fresh output paths on replay; the commands above name the retained original run, not reusable destinations. The falsifier used `/tmp/m7-typed-browser-falsifier` and `M7_FALSIFY_TYPED_ASSERTION=1`. Regression outputs remain `/tmp/m7-typed-regression-root`, `/tmp/m7-typed-regression-arc`, `/tmp/m7-typed-regression-prepared`. Archive directories retain reports/observations, not an imported/restored state authority. Exact guard/install/build/portfolio/probe/format/signature logs accompany this report. A fresh architecture-only check passes27/27 with `vitest --config /tmp/m7-typed-architecture.config.mjs`, whose exact configuration is retained. Two earlier invocations guessed the wrong inventory path and found no tests (exit1); neither is counted as a pass. Inspecting the owner's actual commit identified `test/architecture/boundaries.integration.ts`, and the corrected invocation passes without production/inventory edits. Signing used `commit-network.sb` with only `/var/run/com.apple.launchd.fwnROfHu8a/Listeners` and its resolved `/private/var/run/...` socket permitted; IP sockets remained denied.

## Limits and next gate

The screenshot inspection shows the actual TEST workpiece/structured why pane and neutral Not applied state; the overlapping minimal panel remains unpolished. Scripted assistant text is not independent model interpretation. All positive why outputs are only operation-level/revision-local **partially supported linkage**, with no elicited evidence relations in this fixture; passage standing remains temporal context, not source relevance or genuine testimony. The missing-field/default/migration/external controls refuse. These partials and refusals do not establish the final ordinary/per-class useful-coverage numerator or denominator.

The canonical diagnostics returned no warning text for the final corrected net; build/lint warnings remain disclosed in raw logs (including existing React/compiler/large-chunk warnings). This check is not scenario initial-state execution or simulation, and no rate, timing distribution or inventory claim was invented. Record counts cannot substitute for meaningful construction, behavioral properties or Lu's semantic/utility gates.

Remaining recorded removals, arc type/connectivity operations, conditional parameters and canonical layout/title handling remain in `class-table.md`. Complete those required root obligations after independent checkpoint review, preserving the one current workpiece, original/change/attempt distinctions, exact bases, source/evidence/carry semantics, Voice/Stop and legacy modes. A concrete unexpressible required rule or owning canonical API loss still stops for owner reorientation. Step B and genuine/provider/utility acceptance remain unauthorized.
