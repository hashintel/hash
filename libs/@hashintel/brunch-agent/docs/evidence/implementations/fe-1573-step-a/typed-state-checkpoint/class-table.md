# Typed-state checkpoint class and field matrix

This is a bounded GENERIC TEST mechanical checkpoint within Mission 7 Step A, not root-stratum completion or provider/genuine/utility admission. The [post-capacity matrix](../root-creation-post-capacity/class-table.md) remains the prior footing; its zero-derived browser result is not overwritten. This instrument earns seven actual derived entries. Production is frozen at `3b98cb287275c60c45087e4dc2500f626a39f163` for independent review before lateral changes to the same seam.

| Operation or class | Current candidate path and actual proof | Limits and required continuation |
| --- | --- | --- |
| `addType` | Native canonical input/schema carriage, including nested `elements`; actual Chrome creation from the first empty read; duplicate root identity refuses before browser execution. All five created type fields accounted. | No genuine/provider class admission. Duplicate nested IDs and canonical `addType` duplicate courtesy are explicit focused tests. Root/subnet component collisions remain unavailable rather than a migration claim. |
| `updateType` | Actual type name correction, ordinary-name reopened name why with distinct creation origin/correction; actual no-op and stale refusal. | Description/display fields beyond this name correction are not separately browser-proved. Cosmetic type fields remain in history, not an earned semantic-utility numerator. |
| `addTypeElement` | Actual append of boolean `active`; three direct nested fields, two derived scenario cells defaulting to false. Duplicate element and known-retired element refuse from actual verified reads/history after UI deletion. | No inherited basis for defaulted cells. Element identity is parent-scoped. UI deletion is an external negative control, not recorded removal admission. |
| `updateTypeElement` | Actual `value` string→integer correction; direct element `type`, two derived row cells (`"2"`→2, `"bad"`→0); ordinary element-name/parent-type/field why after reopen. | Coercion/defaulting is canonical migration, not intended inventory or elicited testimony. Other primitive conversion policies remain canonical, but this instrument proves only the named string→integer path. |
| `addScenario` | Actual `per_place` initial state, two explicitly labelled synthetic rows. Raw `parameterOverrides` omitted in provider/call/history/request, native execution inserts `{}`; inserted field is derived and its why refuses. Five direct scenario fields plus one derived field. Full native input export comparison remains against Petrinaut, not just the composed schema. | Empty scenario parameters are used; no parameter overrides or invented operational inventory/rates. Code/ad-hoc initial-state footprints are explicitly unavailable in this candidate, although the full canonical schema is preserved. Nonempty override references have focused guard tests only. |
| `updateScenario` | Actual explicit initial-state correction to `[[2,true],[3,false]]`; only two changed cells become direct effects. Reopened ordinary scenario/cell query selects the actual place ID from a verified tool result and identifies scenario origin plus correction. | Unchanged cells do not acquire a new cause just because the whole initial state was submitted. Row/cell pointers are positional values, not token identities or general continuity. Scenario execution is untested. |
| `addPlace` | Earlier uncoloured creation remains; this path creates two typed places referencing the actual returned type ID. | Typed place reference must resolve uniquely. Other fields beyond prior capacity work and current typed setup remain unproved. |
| `updatePlace` | Actual output-place colour→null correction and actual derived kernel sanitization; complete pre/post accounting. | Earlier capacity path remains regression proof. Further sanitation combinations, executable dynamics and nested references remain unavailable/unproved. |
| `addTransition` | Actual embedded input/output arcs using actual returned place IDs; typed output generates a kernel, classified derived. Ordinary kernel-field why refuses inherited request basis. | This earns generated kernel creation through `addTransition`, not every `addArc` footprint or simulated firing. |
| `updateTransition` | Earlier actual predicate correction and reopened why replayed unchanged. | No new kernel-edit, code semantics or behavioral claim from this slice. |
| `addArc`, `updateArcWeight` | Earlier actual root progression replayed, including exact bases and stale Not applied. | Standalone arc-generated/secondary footprints are not earned here: the distinct construction recorder preflights canonical actions and refuses derived arc footprints before execution. The new guard is focused host-test proof, not a new actual-browser derived-arc proof. Legacy prepared execution remains unchanged. |
| `removePlace`, `removeTransition`, `removeType`, `removeTypeElement`, `removeScenario` | Unavailable as recorded construction operations. | Still required: canonical recorded removal, connected/reference/row cleanup, retirement and no index-shift misattribution. External UI element deletion here is not a recorded delete/recreate epoch proof. |
| `removeArc`, `updateArcType`, `updateArcPlace` | Unavailable. | Still required: retirement, standard/read/inhibitor semantics, corrected endpoint identities, actual canonical sanitation/kernel effects and exact permutation bases. |
| `addParameter`, `updateParameter`, `removeParameter` | Unavailable and not used by this TEST representation. | Remain conditional on a workpiece-supported representation actually using parameters. Unknown quantities are not resolved by inventing a rate or default. |
| `getLatestNetDefinition` | Full raw observations before every actual mutation; exact earlier read/base references; full raw post→reopen content equality with distinct retained hashes. | Explanation-only serialization equivalence does not alias mutation bases or prove absence of intervening history. |
| `getNetCompilationErrors` | Actual canonical check after the consequential corrections returns `No errors detected in your model – everything compiles!`. Exact result is asserted and retained. | Clean net compilation, not scenario initialization execution, simulation, reachability, universal properties or utility. No timing/rates were invented to obtain this result. |
| `applyAutoLayout`, `setNetTitle` | Unavailable. | Still required where used: canonical execution/observation, layout consent, complete history, explicit cosmetic exclusions. |

## Actual field explanations and refusals

| Query | Observed disposition and governing operation |
| --- | --- |
| `TestCorrectedAttributes` / element `value` / `type` | Partially supported linkage: origin `typed-type`, change `typed-integer`, revision `typed-revision-two`. |
| `TestInitial` / affected row-1 value after migration, before explicit correction | Refused derived/unmapped; no governing request basis supplied. |
| `TestInitial` / affected row-1 value after explicit correction/reopen | Partially supported linkage: origin `typed-scenario`, change `typed-explicit-initial`, revision `typed-revision-two`. |
| `TestInitial` / `parameterOverrides` | Refused: canonical inserted default, not authored raw field. Scenario origin remains distinct. |
| `Test transfer` / `transitionKernelCode` after generation | Refused derived/unmapped; generated code is not automatically supported by the operation's workpiece locator. |
| `Test transfer` / `transitionKernelCode` after sanitization/reopen | Refused derived/unmapped; origin `typed-transfer`, current recorded field change `typed-discard-attributes`. |
| `TestCorrectedAttributes` / `name` | Partially supported linkage: original type creation plus later `typed-type-description` name correction. |
| `TestInitial` / nonexistent initial-state path | Refused absent field; no invented field origin. |
| Type `elements` after external UI deletion and stale attempt | Refused unrecorded intervening content; external state and stale/no-op attempts are not conversation causes. |

The actual new browser record total is 12: ten applied, one no-op, one stale. Independent raw reconstruction accounts for 37 created, five updated, zero deleted and seven derived entries. These are effect entries, not ordinary inventory or useful-coverage counts. Deliberate controls were authored prospectively; ordinary partial linkage was not relabelled as useful support. Purely cosmetic x/y, icon and display-color fields remain recorded, but no final semantic exclusion denominator is asserted here.

All supplied user lines and initial conditions are explicitly test-authored. Workpieces are scripted assistant outputs settled by the real core tool, not prepared/imported workpieces. Locators come from the actual core read tool. This fixture supplies no elicited evidence relations: its passage standing is temporal-context-only. Relevance, template completeness, semantic correspondence and useful ordinary/per-class coverage remain unassessed and Lu-held. Existing Voice/Stop, true-process retention and passage-policy claims retain their prior scopes; this browser reload is not a new crash/compaction proof.
