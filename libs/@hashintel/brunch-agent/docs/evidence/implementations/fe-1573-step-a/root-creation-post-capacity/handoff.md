# Root creation after canonical capacity preservation

> Historical result; raw contract-establishment payloads were retired in the [bounded reduction](../landing-20260909/evidence-reduction.md). Original commands, manifests and payload links below describe the recorded run, not a maintained replay or a claim that those bytes remain local.

## Review disposition

**Synthetic creation checkpoint and named controls pass; production is frozen for independent review.** This is a new post-fix packet, not a rewrite of the [original blocked checkpoint](../root-creation-checkpoint/handoff.md), root-lane completion, provider/genuine class admission or useful-field-coverage acceptance. No production implementation changed during this resumed segment. The only new executable change is focused browser witness coverage in `apps/brunch-agent/test/root-creation.integration.ts`; remaining classes stay unmounted.

The delivered owning fix now passes the unchanged raw Chrome capacity/reopen assertion and the unchanged direct canonical diagnostic. The actual reopened definition retains both capacity **3** and explicit **null**. Full independently verified definition comparison additionally establishes content equality, while preserving the distinct raw hashes:

- Recorded post hash: `4266f030dd106c86e35c079a6a19ca4e8c212ebfc6cabd3763b6cf35fe3be864`.
- Reopened observation hash: `ffd39f9e00a8b41af98aa22143b07d68c7048acf6cada9869fc2f5d90a6ba606`.

Only object-key insertion order differs. No mutation uses an equivalence waiver: each still cites the exact earlier raw read ID/hash, and the actual hand edit makes the old base stale. The model's XML-escaped projection is not the raw observation authority. Old overwritten stores are not healed or backfilled.

## Authority, commits and ownership

The resumed worktree was clean at `28ea6db82842702c540ca0ad597764a6bce7b00b` on `ln/fe-1573-root-creation`. The ALPHA Mission and `capacity-preservation/decision.md` were read under authority `be29875ac3`. The parent-owned commits were already present and were not cherry-picked again:

| Commit | Responsibility |
| --- | --- |
| `e13a8e0e8ff9bd2c9973a88f821486fba1f3135c` | Parent importer/script admission for actual `test:root-creation` source. |
| `28ea6db82842702c540ca0ad597764a6bce7b00b` | Parent-delivered equivalent of ALPHA `28a529a7bf`: canonical optional capacity projection/pass-through, tests, guide note and one core patch changeset. |
| `f19c5b6976` | This segment: positive reopened capacity/code why, full-content comparison, exact duplicate-result and multiple-browser refusal checks, typed foreign/conflict failures, retained histories and an outside total-completion assertion. |
| Following evidence commit | This separate packet and its exact source/build/artifact pins. |

`write-set.json` enumerates the actual new paths and separately identifies the delivered parent commits. `source-build-manifest.json` pins full commits, current source/build/runtime bytes and unchanged protected Git objects. No edits to model/guidance, core evidence/locator/carry, Flue/Pi patches, package-manager wiring, `app.ts`, admission/accounting, paid instrumentation, shared witness helpers or sibling-browser work. The parent-owned capacity fix and existing changesets/docs are preserved exactly. No new API, storage mechanism, restore route or generic observer/history engine was added. Nothing was pushed.

## Reached production path

The final built ChatAgent + actual Chrome witness runs **50 synthetic SDK requests**, completes **36 externally counted callback checkpoints**, and independently verifies **nine mutation records: seven applied, one no-op and one stale**. Rejected duplicate/unknown-read/retired mutations never gain browser outcomes. The applied records retain complete pre/post definitions and disjoint effect lists; the independent offline `audit.mjs` confirms **27 created / two updated / zero deleted / zero derived** diff entries, verifies every earlier raw read/base and binding, and checks complete direct-diagnostic/reopen content equality. These counts are not a semantic coverage denominator; this uncoloured path has primary effects only. Generated-kernel and transition-sanitization partitions remain covered by the existing canonical unit tests, not newly claimed as browser-generated-effect coverage.

The first real user submission initializes the distinct empty-document host through the existing route. Synthetic user accounts remain plain operational prose, distinct from the scripted assistant's settled Markdown. No initial prepared net/workpiece, restored conversation or hidden case facts supply the successful path. Tool-returned revision IDs, spans, read IDs/hashes and entity references drive subsequent calls. The path creates two places and a transition, connects two arcs, checks canonical compilation, settles a correction, changes capacity/code and obtains ordinary-name entity/field why. The clean diagnostic result applies to the connected net at its check; this is not a behavioral simulation or a final semantic completeness claim.

The post-reopen controls now cross the previously failing boundary:

| Control | Actual discriminator |
| --- | --- |
| Capacity and complete content survive reopen | Original raw assertion remains byte-for-byte unchanged. Receiving-boundary hash verification succeeds, capacity is 3, and the complete last applied definition equals the raw reopened definition under only object-key-order-insensitive content comparison. |
| Reopened capacity why | Unique ordinary name resolves the preserved field; governing call/revision remains `creation-capacity` / `creation-revision-two`, while origin remains `creation-queue`. Applied changes exclude the no-op, which remains an attempt. |
| Reopened code why | Origin remains `creation-step`; the governing field correction remains `creation-pause` / revision two. No new cause or post-reopen mutation is invented. |
| Honest reconciliation scope | Both answers retain their recorded/observed raw hashes and report serialization equivalence. The first query uses the active read-result delivery and is live-observed; the second server turn reusing that read is explicitly as-of. |
| Duplicate node result | Replay the actual delivery and its original idempotency key. Provider request count stays unchanged and exactly one canonical result remains for the correction. |
| Mixed and multiple-browser proposals | Node+revision and read+node proposals each fail before sibling tool publication; one underlying synthetic request per rejected proposal, no continuation. Shared plugin/app catalogue ownership is unchanged. |
| Duplicate and unknown read | Actual pre-execution errors; no client result for either mutation. |
| No-op | Same-value capacity update returns Not applied and independently verified no-op, never a field cause. |
| Stale hand edit | Actual properties UI changes capacity 3→4. Requesting 5 from the earlier exact raw base returns Not applied/stale, and the live field stays 4. |
| Known-retired identity | Actual UI deletion is followed by a fresh full read. Reusing the earlier recorded place identity refuses before browser mutation; no Petrinaut duplicate-ID courtesy is relied on. This is known-retired refusal, not a new recorded deletion/epoch class. |
| External why | The unrecorded change/deletion yields explicit refusal for an intervening content gap, not conversation attribution. |
| Foreign and conflicting node outcomes | Each injected delivery fails with the SDK's typed failed-submission result. Request count stays **50→50**; raw delivery/history is retained and no effect is reissued. |

The generic renderer is unchanged. Inspected screenshots show preserved capacity 3 after reopening and actual capacity 4 beside the stale Not applied card. Existing panel overlap/limited visibility remains an explicit presentation limitation; no UI redesign or semantic utility claim follows.

## Verification and visible failures

| Oracle | Result |
| --- | --- |
| Fresh Java/Node process-tree guards | Pass; deny profile refuses external and loopback sockets, browser profile permits only loopback. |
| Initial unchanged `test:root-creation` replay | **45 requests / 33 checkpoints / seven applied records**, exit 0. This first post-fix run uses the unchanged inherited witness, including the old failing capacity assertion and previously unreachable controls. |
| Unchanged `capacity-reopen.mjs` | Exit 0 on initial and final post-fix actual records. Its historical printed `claim` string still describes the original diagnostic; the script was not edited to relabel the old red. The preservation assertion now passes, and the new audit compares its complete before/normalized/reopened objects. |
| Final expanded actual Chrome | **50 requests / 36 checkpoints / seven applied + no-op + stale**, exit 0, no page/server/blocked-origin/callback errors. |
| Final required-assertion falsifier | Same frozen witness with `M7_FALSIFY_EMPTY_ASSERTION=1` exits 1. Actual first-read `0 !== 1` survives the faux helper and reaches the outside callback-error assertion. |
| Forced serial affected portfolio | **66/66 uncached tasks / 3,232 tests**, exit 0; all builds, typechecks and lints pass. Includes the actual app server/client, website and rebuilt canonical core. Counts: core1554, Brunch128, plugin85, binding20, transport45, app315, Petrinaut699, website386. Existing warnings are retained in logs. |
| Legacy first-progression actual Chrome | **23 requests / nine checkpoints / two applied records**, exit 0, including original stale/duplicate/admission controls. |
| Legacy prepared A3/A5 actual Chrome | Exit 0; all three cohorts and **14 why answers** pass, including the existing scoped runtime/browser reopen. No new process-compaction or genuine-record retention claim. |

One new test red is retained under `scope-oracle-red/`: the first positive reopened-why addition incorrectly asserted that both queries would remain in the active browser-result delivery. Actual output and current production source distinguish the first live-observed query from the subsequent as-of server turn. The corrected new oracle now requires each exact scope; it does not accept either arbitrarily or change product policy/teaching. It preserves the original raw capacity assertion. A separate intermediate lint error on optional access in the new witness was corrected with an explicit outside assertion; no production fix was needed.

The original pre-fix raw red, original direct diagnostic, original source/build pins and entire original checkpoint remain unchanged. This packet records new successful runs and the new scope-oracle red rather than converting historical failures into green artifacts.

## Reproduction and pins

From repository root, `E=libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/native-local-delivery`. The existing cache installation and verified regular-copy generator were already available; this segment performed no install/download or runtime editing. Builds/probes were never retried with networking enabled.

```sh
node "$E/verify-network-guard.mjs"
sandbox-exec -f "$E/deny-network.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 CARGO_NET_OFFLINE=true VITE_BRUNCH_CHAT_ENDPOINT=/agents/chat yarn exec turbo run build test:unit lint:tsc lint:eslint --filter=@hashintel/brunch-agent --filter=@hashintel/brunch-agent-plugin-sdcpn --filter=@hashintel/brunch-agent-binding-flue --filter=@hashintel/brunch-agent-transport-aisdk --filter=@apps/brunch-agent --filter=@apps/petrinaut-website --filter=@hashintel/petrinaut --filter=@hashintel/petrinaut-core --continue=always --force --concurrency=1
sandbox-exec -f "$E/loopback-only.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 M7_ROOT_CREATION_OUTPUT=/tmp/<fresh-output> yarn workspace @apps/brunch-agent test:root-creation
sandbox-exec -f "$E/deny-network.sb" node libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/root-creation-checkpoint/capacity-reopen.mjs /tmp/<fresh-output>/records.json > /tmp/<fresh-direct>.json
sandbox-exec -f "$E/deny-network.sb" node libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/root-creation-post-capacity/audit.mjs /tmp/<fresh-output> /tmp/<fresh-direct>.json
```

Use another fresh output and `M7_FALSIFY_EMPTY_ASSERTION=1` for the required negative. Legacy scripts are `test:construction-progression` (`M7_CONSTRUCTION_OUTPUT`) and `test:reopened-why` (`M7_BROWSER_OUTPUT`), each under loopback-only. Logs/exit codes, full raw final requests/deliveries/history/observations, positive and refused why, screenshots and the unchanged-witness replay are retained. JSON/log gzip is lossless; PNG bytes are unchanged. `artifact-manifest.json` pins the packet, and `source-build-manifest.json` pins source, complete build outputs, installed runtimes and the protected original packet. The final original SQLite store remains `/tmp/m7-root-postfix-final/conversation.db`; packet JSON is observation evidence, never restore authority.

## Freeze and remaining obligations

Return this source/evidence checkpoint to the integration owner for independent review and downstream reconciliation. Production remains frozen; no further operation classes were added after the successful creation path. The [class matrix](class-table.md) retains all lateral obligations: remaining arcs, deletion/retirement operations, types/elements with scenario migration, scenarios, parameters when used, generated/sanitized browser effects and useful field coverage. Full accepted-region meaning, source relevance, template completeness, semantic utility and Lu's owner gate remain unearned. Step B and provider/paid-profile/DNS/auth work remain unauthorized for this lane. This segment made zero paid calls and no shared-ledger writes.
