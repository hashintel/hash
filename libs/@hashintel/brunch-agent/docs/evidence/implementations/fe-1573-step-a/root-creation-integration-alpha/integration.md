# Root-node construction checkpoint — integrated and replayed

> Historical result; raw contract-establishment payloads were retired in the [bounded reduction](../landing-20260909/evidence-reduction.md). Original commands, manifests and payload links below describe the recorded run, not a maintained replay or a claim that those bytes remain local.

## Disposition

Accept the bounded synthetic root-place/transition creation/correction checkpoint after the canonical capacity repair. This is **not root-stratum completion**, recorded-deletion/epoch breadth, real-provider/genuine admission or useful-field coverage. Remaining required operations and actual generated/sanitized browser effects remain explicit in the [post-capacity class matrix](../root-creation-post-capacity/class-table.md).

The parent read the actual source and independent report, verified **22 source files, 314 builds, 916 runtime files, 178 packaged artifacts and 18 protected file/tree objects**, integrated the commits below without duplicating its core capacity repair, then rebuilt and replayed the combined branch. One initial parent inspection used `git hash-object` on a protected directory; that is not a valid tree check. The corrected check verified the recorded Git tree/file objects against clean HEAD. No test or source was changed to pass it.

| Worker commit | Alpha commit | Scope |
| --- | --- | --- |
| `f0ce69ceab` | `696eacc18a` | Native root node/schema/effect/why/host path and initial red witness |
| `ce9978bac1` | `da87e3d1c5` | Raw, hash-verified reopen assertion rather than model/XML projection |
| `68ca62a4a9` | `b832fc2b43` | Distinct TEST user account versus scripted assistant workpiece |
| `62fd9d8eef` | `6ba81296e5` | Original blocked checkpoint and capacity loss |
| `e1387e7b04` | `78d2aa881a` | Exact guarded logs |
| `e13a8e0e8f` | `8d365c8d90` | Parent-owned importer/script |
| `28ea6db828` | Already `28a529a7bf` | Canonical capacity fix; skipped duplicate cherry-pick |
| `f19c5b6976` | `9a46ba8102` | Positive reopened why and post-gate safety oracles |
| `5ebe50c8ad` | `844126907b` | New post-capacity evidence and class limits |

## Independent review and oracle strength

The [independent full review](review/review.md.gz) found no confirmed reachable safety blocker. It used the correct root worktree, inspected the full new implementation and ran actual Chrome, the unchanged capacity diagnostic, record audit, focused plugin/app/host tests and the required-assertion falsifier. An internal delegated reader had guessed incorrect paths; no implementation credit or finding was taken from it.

Beyond the producer's helpers, the reviewer independently recomputed every raw pre/post hash, rejected overlapping/duplicate effect pointers and reconstructed all nine complete post-definitions by applying the reported effects. It separately checked exact reopened query scopes: first live-observed, second as-of. The falsifier genuinely failed outside the faux-response callback boundary. These are discriminating mechanical checks, not semantic/model usefulness judgments.

## Fresh combined alpha result

All commands used a freshly verified native-local-delivery process-tree guard, with loopback only for actual browser work. Full forced serial build/unit/type/lint passed **66/66 uncached tasks / 3,232 tests**, exit0. Counts: Petrinaut-core1554, Brunch-core128, plugin85, binding20, transport45, app315, Petrinaut699, website386.

| Product oracle | Alpha result |
| --- | --- |
| Actual root creation Chrome | **50 synthetic requests / 36 externally counted callbacks**, no page/server/blocked-origin/callback errors |
| Verified mutation records | **Nine: seven applied, one no-op, one stale** |
| Actual effect accounting | **27 created, two updated, zero deleted/derived entries**; complete pre/post reconstruction and exact earlier read/base/binding checks |
| Unchanged canonical capacity diagnostic | Pass; normalized and reopened complete definitions equal the actual recorded post |
| Reopened node why | Capacity3/null and code preserved; distinct origin/correction; same full content with different retained raw hashes; first query live-observed and subsequent server query as-of |
| Controls | Duplicate result, node+revision/multiple-browser proposals, duplicate/unknown-read/stale/known-retired/foreign/conflicting outcomes pass at their named boundaries |
| Legacy arc/weight Chrome | Existing 23-request/two-applied path passes |
| Legacy prepared A3/A5 Chrome | Existing three cohorts and 14 why responses pass |
| Corrected passage matrix | Preliminary Partial, 58 rows / 215 synthetic requests |
| Existing raw terminal/cache accounting | 25 controls / 34 synthetic HTTPS dispatches, no paid calls |
| A5 process/compaction regression | Distinct PIDs **63231 / 63753 / 63768**, same original store, **two compactions / 31 completion pins / ten rejected falsifiers**; instrument unchanged during phases |

The process-retention store is `/tmp/m7-root-integration.loLvuQ/retention/original/conversation.db`. That regression uses the existing A5 records/instrument; the new root-node browser reload is not itself a new true-process/compaction proof for every node class. Original entries/cached answers leave folded context while current workpiece authority remains.

The parent inspected the actual reopened-capacity screenshot. Capacity3 is present in the real properties panel; the later hand edit leaves4 and the stale request5 is Not applied. Panel overlap remains an unpolished/utility limit. Canonical compilation was checked before the later false-predicate correction; this checkpoint does not claim final corrected-net diagnostics or behavioral simulation from that earlier success string.

## Scope preserved

Only the explicit candidate catalogue mounts native add/update place/transition and canonical compilation reads alongside the existing arc/weight path. Petrinaut owns schemas, executable validation and normalization; Brunch composes its basis/read envelope. Detached canonical action execution validates expected footprints but never substitutes for the browser's independent actual post. Direct and derived fields remain disjoint; unrequested/generated changes do not inherit request basis. This actual uncoloured path has zero derived changes, so unit-generated/sanitization cases are not promoted to browser coverage.

Identity checks use actual verified current/history data, never model courtesy. Known-retired refusal follows actual UI deletion and fresh read; it does not implement recorded removal or general epochs. Why relocates nodes by identity in each snapshot instead of carrying current array indices backward, preserves origins/changes/attempts and refuses unrecorded or derived/unmapped attribution. All supported links remain operation-level/revision-local and partially supported, with relevance and utility unassessed.

## Commands and retention

From repository root, `E=libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/native-local-delivery`, after `node "$E/verify-network-guard.mjs"`:

```sh
sandbox-exec -f "$E/deny-network.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 CARGO_NET_OFFLINE=true VITE_BRUNCH_CHAT_ENDPOINT=/agents/chat yarn exec turbo run build test:unit lint:tsc lint:eslint --filter=@hashintel/petrinaut-core --filter=@hashintel/petrinaut --filter=@hashintel/brunch-agent --filter=@hashintel/brunch-agent-plugin-sdcpn --filter=@hashintel/brunch-agent-binding-flue --filter=@hashintel/brunch-agent-transport-aisdk --filter=@apps/brunch-agent --filter=@apps/petrinaut-website --continue=always --force --concurrency=1
sandbox-exec -f "$E/loopback-only.sb" env M7_ROOT_CREATION_OUTPUT=/tmp/m7-root-integration.loLvuQ/root HASH_OTLP_ENDPOINT='' yarn workspace @apps/brunch-agent test:root-creation
sandbox-exec -f "$E/deny-network.sb" node libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/root-creation-checkpoint/capacity-reopen.mjs /tmp/m7-root-integration.loLvuQ/root/records.json
sandbox-exec -f "$E/deny-network.sb" node libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/root-creation-post-capacity/audit.mjs /tmp/m7-root-integration.loLvuQ/root /tmp/m7-root-integration.loLvuQ/direct.json
```

The other workspace scripts were `test:construction-progression`, `test:reopened-why` (fresh named loopback output), `test:passage-policy` (fresh deny output), and `test:reopened-why-retention` with its own per-phase guards. The raw terminal test ran directly under deny. Exact outputs and the latter's phase commands are retained. Do not rerun into these existing directories.

`artifact-manifest.json` hashes 178 parent integration artifacts; `review/artifact-manifest.json` separately hashes 60 review artifacts. Full raw observations/history/native requests, audit, selected screenshots and exact logs are losslessly retained. Original live stores stay in their temporary locations; no saved history or projection is imported as state authority. No guarded failure was retried with network enabled. Nothing was pushed.

The sibling-browser close fallback remains a separate pending review. Actual paid usage remains **five calls / US$0.09113535**, no active allocation. Continue remaining required operation classes and browser-derived/field-why coverage from this reviewed checkpoint; do not call the construction lane or Step A complete.
