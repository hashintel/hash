# Separate guarded browser roots — bounded adapter approval

## Evidence and owner decision

The actual paid→loopback sandbox application failed before any provider contact, so the original named allocation was released unused. The subsequent local probe demonstrates the minimum browser-control alternative with installed Playwright **1.58.2**: public `chromium.launchServer({ host: "127.0.0.1", port: 0, … })` in a credential-free loopback-only owner, and public `chromium.connect()` in a separately launched loopback-only controller. Chrome belongs to the owner, not the controller. No custom browser-control service, provider proxy, app or model server was introduced.

The parent reviewed the executable probe, inspected the actual screenshot and independently checked **33 original artifact hashes, seven library/profile/binary pins**, sibling process identities, and both source-correlated Chrome `TCP_CONNECT_ATTEMPT` failures to `192.0.2.1:80` with **OS error1 (EPERM)**. The actual controller clicked and read **Clicked in sibling Chrome**. No route interception or `exposeNetwork` was used. This is real socket denial, not interception being presented as OS enforcement.

Both roots and Chrome received only PATH/HOME/TMPDIR, fresh directories and a library-created isolated profile. Normal shutdown closed the context, disconnected the controller, awaited `BrowserServer.close()`, removed the profile and closed the endpoint; both Node processes and Chrome exited0. Known PIDs were checked gone and the old endpoint returned ECONNREFUSED. Chrome attempted background networking/DNS, with denials retained in NetLog; no provider/auth hostname probe was authored. Sandboxed process-table enumeration failed and is retained, so exhaustive descendant enumeration and abrupt-crash cleanup are not claimed.

**Under the existing AFK delegation, approve a narrow evaluation-only adapter change.** Preserve the required browser OS denial; fix process placement instead of weakening the sandbox. The public library-owned ephemeral browser-control endpoint is not a new production/model service or provider proxy. No paid execution is authorized by this approval.

## Permitted implementation and protected contracts

- Add only the bounded browser-owner launch/lifecycle and ordinary process orchestration needed for this one A5 evaluator. Launch owner/Chrome under a separately applied **loopback-only** root; launch the driver under loopback for dry, or the existing paid profile only after named parent activation. Orchestration opens no application/provider sockets itself and starts no SDK/model code outside those guards. Do not build a reusable daemon or process framework.
- Replace the driver's nested `chromium.launch()` with the supported local connection. **Dry and real must use the same sibling-browser path**; an old nested dry-only branch must not become another misleading preflight. Keep the existing contexts, UI actions, canonical result transport, scenario, guidance, raw response attestation, accounting and all-canonical repair limits unchanged.
- Bind the private ephemeral endpoint and owner identity to the named run through explicit activation/readiness data. Use an explicitly loopback-bound host and the library's random endpoint path; verify expected run/owner/library/profile identity and reject foreign/stale/malformed readiness before provider invocation. Keep capability-bearing endpoint data in restricted fresh files and out of page/model input. Do not forward arbitrary activation options to Playwright.
- No `exposeNetwork`, proxying, custom auth headers, credential-bearing browser environment or unguarded Chrome. No change to the paid/loopback network rules to make the old nesting appear valid. The owner/control process itself, not only Chrome, must be credential-free and guarded.
- Orchestration owns bounded lifetime, server shutdown and cleanup verification; the driver owns context closure/disconnection. Reuse public library close/kill operations and known owned process identities. Cover normal completion, controller/attach failure and bounded fallback/interrupt cleanup without claiming universal power-loss or exhaustive orphan recovery. Preserve failures and keep unknown accounting blocking.
- Limit production-adjacent writes to the evaluation driver, its browser/launch/activation/manifest helpers, focused tests and evidence. Remove or retire only the now-obsolete session-owned nested-launch code if no live consumer remains; preserve historical commits/manifests. Shared scripts/importer inventory remain parent-coordinated. No application/core/plugin/Petrinaut semantics, runtime patch, schema, dependency, model, budget or teaching change is included.
- Pin the new launcher, relevant installed Playwright server/client/library and profile identities. The original `34a90935e4` / `8a47…7381` instrument remains historical and unlaunchable under the old topology. Return source/test delta and reliable evidence for focused review before generating another final freeze.

## Required proof and later gates

First demonstrate integrated **dry5** on the new topology through the actual built ChatAgent and canonical browser read/result, with complete independent process/profile/endpoint observations. Add wrong-run/owner/endpoint and early-failure cleanup controls, checking no unreserved provider dispatch and no surviving owned browser/control endpoint/profile. Do not turn malformed readiness into a fallback launch or silently ignore a cleanup failure. Guarded build/test/format requirements remain.

The unpaid work may use only deny-network/loopback guards. It does not prove the later **paid-controller → separate loopback owner** combination; that exact connection and pinned native TLS remain named activation checks after new review/freeze/allocation. The parent still supplies the sole shared ledger allocation and invocation instruction. No DNS/provider/auth probe or paid profile is granted during this correction.

Actual shared usage remains **5 calls / US$0.09113535**, with no active reservation. Root-creation work continues independently. No genuine/provider-class/utility or Step A/B acceptance follows. The complete original local probe is retained losslessly under `probe/`, including its failed process-table audit and normal-cleanup limits.
