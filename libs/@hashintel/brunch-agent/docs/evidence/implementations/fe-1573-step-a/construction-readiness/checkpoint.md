# Construction readiness checkpoint

This is a transport prerequisite checkpoint, not construction completion or new operation/provider admission. Exact departure: `c41aeb54ca83d3aeb6f6678c9e2d318d120b5e33`, worktree `m7-construction-readiness`, branch `ln/fe-1573-construction-readiness`. Mission remains parent-owned. No hidden case inputs were read, no provider call was made, and no paid authority changed.

## Owner decisions and delivered seam

The integration owner approved an opaque SDK-typed `initialData` option at the existing AI SDK transport boundary. Forward it unchanged on every user send, including retries, but never on client-result signals; omit it entirely when unconfigured. No local initialized flag, wrapped client, prepared-signal bootstrap or new route/store. The host must use a distinct mode/incarnation-scoped identity or verify retained authoritative binding; later initialData being ignored cannot prove that binding. The website bridge forwards this optional value. The future callsite and actual first-user initialization witness remain unimplemented at this checkpoint.

TDD: `test/chat-transport.test.ts` first failed because configured data was omitted (one failure / 20 passes). The focused green run covers exact value identity, same-message retry and key preservation, client-result omission, and unchanged ordinary sends. Existing causal transport assertions are unchanged. Logs are retained alongside this checkpoint. This checkpoint does not claim the full affected portfolio or actual browser progression.

Guard controls passed for shell → Java/Node under deny, commit and loopback profiles before immutable offline linking. The missing generator JAR was regular-copied from the authorized already-local alpha artifact: source/destination 27,103,489 bytes, SHA-256 `9718ff7844e89462c75dcd9b20a35136f6db257bfe1b874db1e3002e99de4609`, different inode, no symlink. Its original network-incident provenance remains in native-local-delivery; this is not original offline acquisition.

## Exact operation envelope at departure

Source authority: `petrinaut-core/src/{ai,action-schemas,actions}.ts`; current plugin `src/{root-arc,transition-record,flue}.ts` and `src/tools/petrinaut-construction.ts`; website `local-storage-demo/transition-record.ts`; app `conversation/why.ts`. Canonical action execution is separate from schema carriage and proof. Every action runs through `mutateWithExtensionGuards`: effective disabled extensions may trigger full-document sanitization. The published helper's success/void return is not evidence of a change.

In this table **native** means the existing native input carrier, **loose legacy** means the inherited headless Valibot wrapper, and **unmounted** means unavailable on current joined production construction. No row is newly admitted by source inspection. The sole recorded browser effect/why coverage remains the fixed prepared root arc; A5 linkage is operation-only/partially-supported, never semantic utility.

| Operation | Local carrier at departure | Canonical action and consequential footprint | Browser effect / why coverage; provider limit |
| --- | --- | --- | --- |
| `addPlace` | Loose legacy headless | Validate/sanitize place, check dynamics references, append; no identity collision guard | None; no native/provider class proof |
| `updatePlace` | Unmounted | Assign validated update, sanitize place, sanitize **all transitions**, potentially generate kernels | None; secondary effects need separate derived accounting |
| `removePlace` | Unmounted | Splice place, remove connected arcs/port references, sanitize all transitions | None; retirement, index shifts and ID reuse unearned |
| `addTransition` | Loose legacy headless | Parse/sanitize transition; may generate default kernel; append | None; embedded arcs/defaults are consequential |
| `updateTransition` | Unmounted | Assign update, sanitize and validate target transition | None |
| `removeTransition` | Unmounted | Splice matched transition | None; retirement/index shifts unearned |
| `addArc` | Native joined; loose legacy | Validate endpoint references; skip existing matching endpoint; append canonical arc; sanitize target transition with possible default kernel | Prepared root insertion only; unmapped secondary effects yield unknown, not attributed basis. Current native real-provider admission unearned |
| `removeArc` | Unmounted | Splice matching directional endpoint, sanitize transition | None; arc retirement/recreation unearned |
| `updateArcWeight` | Unmounted | Assign matched directional arc weight, otherwise no-op; extension guard still applies | Next narrow candidate, not yet effect/why/provider proof |
| `updateArcType` | Unmounted | Assign matched input type, sanitize transition with possible default kernel | None |
| `updateArcPlace` | Unmounted | Validate replacement endpoint, replace arc endpoint, sanitize transition with possible default kernel | None; changed endpoint identity must not inherit an origin silently |
| `addType` | Native headless | Parse type, append when colors enabled; **duplicates are not rejected** | Historical paid nested evidence is original instrument only; no browser effects/why |
| `updateType` | Unmounted | Assign/validate matched type when colors enabled | None |
| `removeType` | Unmounted | Remove type; clear place/dynamics references; sanitize all transitions | None; cascades and retirement unearned |
| `addTypeElement` | Unmounted | Append/validate element and migrate scenario rows | None; migrated initial state is derived, not automatically supported |
| `updateTypeElement` | Unmounted | Assign/validate element; type changes migrate scenario rows | None |
| `removeTypeElement` | Unmounted | Splice/validate element and migrate scenario rows | None; retirement and scenario loss require proof |
| `addScenario` | Unmounted; native export diagnostic only | Default-aware canonical parse; initialize scenario array if absent; append | None; `parameterOverrides` input optionality must survive export |
| `updateScenario` | Unmounted | Assign/validate matched scenario | None |
| `removeScenario` | Unmounted | Remove matched scenario | None; retirement unearned |
| `addParameter` | Loose legacy headless | Parse and append only when parameters enabled | None; representation must earn use, never invent quantities |
| `updateParameter` | Unmounted | Assign/validate matched parameter when enabled | None |
| `removeParameter` | Unmounted | Splice matched parameter when enabled | None; retirement unearned |
| `getLatestNetDefinition` | Existing joined read | Return canonical document; website independently observes bound handle and app verifies full definition/hash | Existing read/why correlation; not yet per-mutation base progression |
| `getNetCompilationErrors` | Unmounted on joined path | Existing canonical compilation/read path | No new claim |
| `applyAutoLayout` | Unmounted | Host command/asynchronous layout, not the synchronous observed mutation path | Unavailable pending canonical observation/consent discriminator; cosmetic exclusion cannot omit operation history |
| `setNetTitle` | Unmounted | Host metadata callback, outside observed SDCPN handle | Unavailable pending metadata observation contract; not generic success |

Native schema derivation must use the exact canonical owner schema with input semantics and explicit normalization. No field-copy/carrier converter is proposed. Runtime-only endpoint checks remain executable obligations. Provider acceptance, generated input validity and behavior stay separate gates.

## Next exact discriminator and ownership

The integration owner directed a checkpoint rather than expansion of the broad unverified candidate. Session-owned exploratory code is retained outside the worktree at `/tmp/m7-construction-unverified/` and `/tmp/m7-construction-unverified.patch`, not mounted, committed or credited. A duplicate `addType` probe demonstrated that the canonical helper permits a second identical ID; creation must gain its bound pre-execution duplicate/retired-ID precondition, not a Petrinaut patch or model courtesy.

Continue this same lane with the actual ChatAgent/browser path: first real user initializes a distinct mode/incarnation, settles the workpiece, obtains an independently verified read/base, executes native `addArc`, obtains a fresh verified read/base, settles a workpiece correction and executes canonical `updateArcWeight`, then queries coherent origin/current-change/attempt why. Inspect effective host capabilities and extension guards before relying on the weight footprint. Preserve strict raw mutation-base equality, original immutable legacy data, raw basis history, one-browser admission, duplicate/conflicting delivery controls and stale hand-edit refusal. This pair cannot substitute for the eventual root envelope above. Broader native/effect/why classes follow only after this witness. No Step B, provider-class or genuine-tracer gate is earned here.
