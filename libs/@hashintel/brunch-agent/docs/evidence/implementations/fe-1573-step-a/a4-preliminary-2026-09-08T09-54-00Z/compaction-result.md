# A4 preliminary compaction result

> Historical result; raw contract-establishment payloads were retired in the [bounded reduction](../landing-20260909/evidence-reduction.md). Original commands, manifests and payload links below describe the recorded run, not a maintained replay or a claim that those bytes remain local.

## Verdict and selected branch

**Pass — existing-tool preliminary public-history pin. Final A4 remains pending actual A2 revisions and A3 transitions.** In the built mounted production ChatAgent, actual threshold compaction reduced model context from 20 messages to 3. All 10 previously observed public messages remained accessible, with exactly equal IDs, roles, purposes, parts, tool inputs/results, data and correlations. No archive repair is earned by this observation. A separate oversized-input probe exposed a failing overflow continuation; it limits runtime-continuation claims, not the observed threshold-history survival.

This is unpaid, controlled-provider evidence, not elicitation or genuine Vestera testimony. The actual server `ping` and question-marker implementations ran; `readPetrinautDoc` ran the real server defer path and received explicitly test-authored executor results through the public SDK. No real browser, revision tool, canonical mutation, transition record, basis relation or product why operation is claimed.

## Instrument and boundary

Primary repetition: `threshold-repeat/`, run from test commit `8493526924f43bcb3f29aea90aa306a2b74c2c28`. Earlier successful observation: `threshold/`; that run preceded the final fresh-store guard and type-only cleanup. Both produced the same survival result. All stores are independent disposable A4 stores. No A1 or sibling store was opened.

- Built artifact: `apps/brunch-agent/dist/app.mjs`, via existing `loadBuiltBrunchApplication()`, not a substitute agent. Full built JavaScript hashes are in `threshold-repeat/identity.json`.
- Mount: `/agents/chat/:instanceId`, production ownership middleware, in-process `application.fetch`. No listening port or sibling server was needed.
- Provider: `setProvider(faux.provider)` replaces `anthropic` before loading the built app; only `a4-faux-only` is registered. Scripted replies and zero-cost usage remain controlled. The finite response queue has no real-provider fallback.
- Owner-supplied seam: `BRUNCH_TEST_KEEP_RECENT_TOKENS=256`, set before loading. `NODE_ENV=test`. Provider metadata: `contextWindow=16000`, `maxTokens=1024`; runtime observed reserve `1024`, threshold `14976`. Neither the tools nor their termination were replaced.
- Three short, explicitly test-authored user turns and two separate single-result continuations establish non-adjacent records before compaction. A fourth user turn carries 1,350 repetitions of `synthetic-padding `, explicitly labelled threshold filler. A final short user turn exercises the actual post-compaction agent context.
- Runtime `observe()` retains compaction events and token usage; provider callbacks retain JSON-materialized requests (callable tool implementations are not serialized). All historical snapshots come from `createFlueClient(...).history()`.

The summarizer's response is deliberately lossy test text. It is returned only when the runtime actually requests `compaction`/`compaction_prefix`; the test never inserts a summary signal, truncates a snapshot, writes a private canonical record or restores prepared history. A successful runtime compaction event **and** a later agent request consuming that summary while lacking `violet gear` and `a4-ping-early` distinguish actual folding from merely displaying a fake summary.

## Observations

`threshold-repeat/create-events.json` records a threshold trigger at 15,502 reported faux tokens, summarization of 18 internal messages, and `compaction { messagesBefore: 20, messagesAfter: 3, isError: false }` for submission `sub_01M20795XY5S2KDBDWAGWNAHT9`. These are runtime context counts, not public message counts. Faux token estimates are a trigger instrument, not a provider-cost or performance measurement.

`before.json` has 10 public messages. `after-threshold.json` and `after.json` retain those records; `after.json` has 14 messages after the filler and final turn. `comparison.json` records exact before/after ID arrays, all role classifications, both correlated client results, and empty `lost` and `changed` arrays. A later threshold check reports no valid cut point; this is one successful compaction, not proof of repeated compaction across an arbitrary number of folds.

| Observed pre-compaction public ID | Role/purpose | Parts or correlations | After compaction |
| --- | --- | --- | --- |
| `entry_direct_c3ViXzAxTTIwNzk1VFBBTktWNk0yNVJHOTU5WTc4` | user/user | Early `violet gear` source | Unchanged |
| `entry_01M20795VPA5T8B1DT85TNVX2C` | assistant/assistant | `a4-ping-early`, `a4-question`, `data-brunch-question`, `a4-doc-early` | Unchanged |
| `entry_direct_c3ViXzAxTTIwNzk1V0VTU0E2MVRGN1JRTTM0SjhB` | system/dispatch | Only `a4-doc-early` result | Unchanged |
| `entry_01M20795WJKGT99XC4RZ4AK56V` | assistant/assistant | First continuation prose | Unchanged |
| `entry_direct_c3ViXzAxTTIwNzk1V1IyN05QNEpXRkgyNFRUWEhC` | user/user | Unrelated middle `silver latch` source | Unchanged |
| `entry_01M20795WWXZ1MRTJJ9FSVTRTQ` | assistant/assistant | `a4-ping-middle`, prose | Unchanged |
| `entry_direct_c3ViXzAxTTIwNzk1WDhYWTZZMFZCOTlFNjNHN0hS` | user/user | Late `amber wheel` source | Unchanged |
| `entry_01M20795XC759GX1WHNH4KWP2M` | assistant/assistant | `a4-doc-late` | Unchanged |
| `entry_direct_c3ViXzAxTTIwNzk1WE1HQzVTWUFTNFg3S0ExUEdB` | system/dispatch | Only `a4-doc-late` result | Unchanged |
| `entry_01M20795XQB570BBHVVV5X8Z2S` | assistant/assistant | Second continuation prose | Unchanged |

All five tool-call IDs equal the supplied faux call IDs. Both ping outputs equal `{ ok: true, note }`; the marker output is `{ marked: true }` and its durable named data remains. The doc-tool public output remains the server's `{ awaiting: "client" }`; actual test executor outputs remain in the separate `system/dispatch` signals. The AI SDK projection correlates these into completed tools without converting the signals into user testimony. `before-ui.json`, `after-ui.json` and `reopened-ui.json` retain the actual projections. These two client-result signals contain one result each, in causal order, not a cumulative replay.

**Lost public IDs: none. Changed pre-existing public records: none.** Loss from the model's current context is real and deliberate; it does not imply public history loss. Conversely, public history availability alone does not provide authorized retrieval to the model. A5 must supply that operation.

## Persistent-state scope

This base has no A2 `update_workpiece` state. Neither the public history snapshot nor its AI SDK projection exposes a general persistent-state map. No current Markdown/state survival claim is made. Installed Flue documentation says persistent writes live in the canonical stream; that is a source contract, not this run's state-survival verdict. Do not treat the controlled summary or a currently visible Markdown artifact as current-workpiece persistence.

## Separate failing overflow pin

**Fail — controlled silent-overflow continuation; source-history loss branch untriggered.** With `A4_OVERFLOW_PROBE=1`, the same instrument uses 4,000 filler repetitions. Faux usage becomes 39,352 tokens against the 16,000 window, so the runtime selects `reason: overflow`, not threshold. It successfully folds 20 → 3 messages, then fails the submission with `Cannot continue from message role: assistant`. Public history still contains all 10 pre-fold records. The explicit command exits 1; it is intentionally not swallowed by the green wrapper.

- First observation: `overflow/`; independent disposable repeat of the same instrument: `overflow-repeat/`.
- Repeat settlement: `sub_01M207989JEG2EV7DVBG0MTQKB`; exact error and stack in `overflow-repeat/create.log`, compaction in `create-events.json`, final public state in `create-final-history.json`, safe shutdown in `create-shutdown.json`.
- Competing explanation considered: a faux queue/summary-routing mistake. Both runs record the normal agent response followed by a genuine compaction-purpose callback, successful folding, then the runtime's continuation stack before another faux callback. This narrows the failure to the controlled overflow path, but does not establish how a real provider's explicit context-overflow error behaves.
- Read-only source lead: installed `conversation-stream-store-CXwRWonS.mjs`, `runModelTurnWithRecovery()` / `continueRebuilt()`; the latter calls Pi's `Agent.continue()` without a restart callback on this route. This is a lead for owner/upstream review, not an independently adjudicated root cause or permission to patch the runtime.
- No real-provider reproduction, external issue, model-assisted adjudication, runtime patch or paid request occurred. The owner should retain this failing pin and decide its upstream disposition. It does not authorize an archive design or weaken the required A2/A3 recheck.

## Archive branch

**Untriggered.** No lost IDs justify hardening the archive lane yet. The two additional binding unit tests establish that classification requires both `role: user` and `purpose: user`, and that an already-observed archive version remains available after a synthetic public-window omission while `peek()` still returns only live history. A reader started after omission cannot recover unseen records. That unit fixture is explicitly not compaction evidence.

If actual A2/A3 records disappear, return their exact lost IDs and failing equality assertions before implementing anything. Re-enter the existing binding-owned session-log archive writer, its pre-loss acquisition timing, principal/conversation/incarnation identity and immutable version lookup. The current archive is on-demand, not a subscription and not automatically a complete lineage projection. Do not revive capture envelopes, introduce a second log, or infer evidence from archive ordinals.
