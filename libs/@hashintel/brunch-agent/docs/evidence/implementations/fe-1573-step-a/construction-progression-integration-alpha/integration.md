# First construction progression — integrated and replayed

> Historical result; raw contract-establishment payloads were retired in the [bounded reduction](../landing-20260909/evidence-reduction.md). Original commands, manifests and payload links below describe the recorded run, not a maintained replay or a claim that those bytes remain local.

## Disposition

Accept the first narrow synthetic construction checkpoint, including its admission-registration and visible-result corrections. It is **not construction-lane completion**, broader operation/provider admission, genuine Vestera construction or semantic utility acceptance. The next required work is creation/correction and complete effect/why coverage for the remaining root classes, not another proof of the same two-operation fixture.

The parent reviewed the source, both independent reports and the final class/footprint table; verified **30 source files, 29 protected paths against departure, 199 built files, 293 artifacts and 11 raw/formatted summary pairs**; integrated the commits below; then rebuilt and reran the actual combined path on alpha. Historical reds and source/build pins remain unchanged in the [worker handoff](../construction-readiness/progression/handoff.md).

| Worker commit | Alpha commit | Scope |
| --- | --- | --- |
| `cb5482aae6` | Previously `d606c98050` | Optional opaque initialization transport prerequisite; not duplicated |
| `8210e96d06` | `e9662e18c9` | Parent-owned importer/script |
| `30d026e2ac` | `9e18399e01` | Native root arc/weight progression, verified prior bases, effect/why and host join |
| `476370d652` | `bf970c3344` | Parent-owned browser admission catalogue registration |
| `6e722b13d0` | `910d0b7d1a` | Generic honest non-application renderer, tests/docs/changeset |
| `a144b35380` | `9b89759ff8` | First-path and renderer evidence/class limits |
| `8810f909a9` | `d5331f1da1` | Exact raw summaries and guarded logs |

After verification, `772713508a` consolidated the renderer release note into the existing A3 Petrinaut patch changeset, preserving both consumer changes in one paragraph/one package changeset as required by package conventions. The session-owned duplicate changeset was removed; runtime/test source was unchanged. Earlier manifests retain the original file at their own commit.

## Closed boundaries

The new candidate initializes via its first real wire user send, with TEST text, distinct mode/incarnation and no prepared workpiece or signal bootstrap. It settles a current revision, obtains a verified full browser read and exact raw base, creates an arc, settles a workpiece correction, obtains another verified read/base, updates the weight and asks why. Actual tool responses provide the successful IDs/spans/bases; canonical browser execution and independently recomputed pre/post effects—not a fabricated model result—establish the changes.

Only native `getLatestNetDefinition`, `addArc` and `updateArcWeight` are mounted in this candidate. The app now classifies that plugin-owned catalogue as browser work, so weight+revision and multiple-browser proposals refuse before sibling publication or continuation. Unknown observations, duplicate delivery, foreign incarnation, conflicting outcomes and the stale hand-edit control remain fail-closed at their named boundaries.

The generic Petrinaut renderer now honors its existing explicit `applied: false` contract. Independent re-review passed **43 renderer + 4 stock-widget + 2 supplemental rendered controls**, and repeated real Chrome once. The parent repeated it again after integration: **Not applied**, the actual refusal reason, neutral dash/no success check, no requested-value success summary, actual field **3** versus rejected request **4**. The protocol/output/record remains unchanged. Group labels say operations rather than asserting changes occurred. The [focused independent review](renderer-review/review.md.gz) records exact controls and artifact/source-map limits; no broad layout repair was performed.

## Fresh alpha verification

All application/build/probe commands ran under freshly verified native-local-delivery process-tree profiles, with explicit loopback only for actual browser work. No provider egress or paid invocation occurred.

| Oracle | Alpha result |
| --- | --- |
| Forced serial combined build/unit/type/lint | **63/63 tasks, zero cached, 1,642 tests**, exit 0 |
| Candidate actual Chrome | **9 checkpoints / 23 synthetic requests / 2 applied records**, zero page/server or blocked-origin errors; stale refusal and corrected card verified |
| Legacy A3/A5 actual Chrome | Existing three-cohort A5 path passes, **81 synthetic SDK requests / 14 why answers**, original-store runtime/browser reopen |
| Native schema carriage | Pass, **20 synthetic SDK requests**, zero network attempts |
| Source/locator/evidence | **10 observations / 29 synthetic requests**, pass |
| Corrected passage-policy matrix | Preliminary **Partial**, **58 rows / 215 synthetic requests**, pass |
| Existing built accounting | **13 outcomes**, six synthetic starts/dispatches, pass; separate raw-attestation limits are not waived |
| A5 true-process retention | PIDs **31229 / 32058 / 32061**, same original store, **2 threshold compactions / 31 completion pins / 10 rejected falsifiers**, source/build/runtime unchanged through phases |

The retained store is `/tmp/m7-construction-integration.boEiot/process-retention/original/conversation.db`. Original user entries and cached query answers leave folded context while authoritative current workpiece state remains. Reopened queries retain their as-of labels and do not reissue effects. This new replay joins the construction/renderer changes with the already-corrected A5 retention path; it is still synthetic, local and not a power-loss/relocation or final genuine-record verdict.

Full unit counts: core128, plugin76, binding20, transport45, app288, Petrinaut699, website386. Expected rejected-submission traces, SQLite warnings and existing lint/compiler diagnostics remain in logs rather than being hidden. All command exit codes above are zero.

## Reproduction and artifacts

Repository-root portfolio, after `node "$E/verify-network-guard.mjs"`, where `E=libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/native-local-delivery`:

```sh
sandbox-exec -f "$E/deny-network.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 CARGO_NET_OFFLINE=true VITE_BRUNCH_CHAT_ENDPOINT=/agents/chat yarn exec turbo run build test:unit lint:tsc lint:eslint --filter=@hashintel/brunch-agent --filter=@hashintel/brunch-agent-plugin-sdcpn --filter=@hashintel/brunch-agent-binding-flue --filter=@hashintel/brunch-agent-transport-aisdk --filter=@apps/brunch-agent --filter=@apps/petrinaut-website --filter=@hashintel/petrinaut --continue=always --force --concurrency=1
```

The corresponding guarded workspace scripts are `test:construction-progression` (fresh `M7_CONSTRUCTION_OUTPUT`, loopback), `test:reopened-why` (fresh `M7_BROWSER_OUTPUT`, loopback), `test:native-schema`, `test:workpiece-evidence`, and `test:passage-policy` (fresh `PASSAGE_POLICY_OUTPUT`, deny). Built accounting runs `node --experimental-strip-types apps/brunch-agent/test/provider-accounting.integration.ts` under deny. `yarn workspace @apps/brunch-agent test:reopened-why-retention <fresh-directory>` verifies and applies its own per-phase profiles; its browser phase must not be nested under deny-only. Retained exact phase commands are in `alpha/process-retention/commands.log.gz`.

`artifact-manifest.json` hashes **128** parent logs, exit codes and complete JSON/native/context/history/record observations, including selected actual screenshots. `renderer-review/artifact-manifest.json` separately hashes **35** review artifacts. Gzip preserves original bytes; pre-compressed captures are copied unchanged. Original stores/profiles remain at their local paths and are never imported as authoritative state.

Paid usage remains **5 calls / US$0.09113535**, no reservation. The separate cache-cost correction has now passed focused independent re-review, but its driver still requires integration and a new merged-instrument freeze before allocation. No Step A/B acceptance or push occurred.
