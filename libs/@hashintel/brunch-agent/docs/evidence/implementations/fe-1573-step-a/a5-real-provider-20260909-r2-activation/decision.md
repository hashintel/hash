# Named r2 sibling-browser allocation and activation gates

## Allocation and accepted instrument

Under Lu's existing AFK paid-work delegation, allocate **`a5-real-provider-20260909-r2`** at most **20 underlying calls / US$15** inside the existing **200-call/US$100** total envelope. Use only **`anthropic/claude-sonnet-4-6`**, **4,096 output tokens / US$7 hold per request**, checked against the frozen **US$6.06144** catalogue bound. The caller count includes preparation, continuations, compaction, failures and retries; three rejected attempts of any canonical operation stop further dispatch. No other model participant, self-top-up, automatic resubmission or guessed settlement is authorized.

Code/base: `f2b9bfd040ec989f06c7b72a8973f88aab3b42ca`. Worker evidence: `d7e369690c77cfb95f81da5bc4d0cc4759e700a5`, integrated as `fbb8419f68`. Accepted final manifest: `real-provider-a5-final-sibling-preflight/final-freeze/manifest.json` in `/Users/lunelson/.herdr/worktrees/hash/m7-provider-execution`, SHA-256 **`1c71ed054d89a7a0183df5b8877faba042982638a58c6696151fc119df439586`**. Parent independently verified all **3,916 file pins**, fresh enumeration, current bounds/browser identity, **219 original/retained pairs**, evidence-only changes and a clean worker tree. Credential availability is a local boolean prerequisite, not key validity or provider acceptance.

This is the unchanged synthetic-user/prepared-root-arc current-A5 instrument, not typed-state construction, genuine Vestera, semantic utility or Step A/B acceptance. All prior failures/freezes remain historical. R1 was released unused at the unsupported nested-profile boundary; it is not retried or silently reactivated.

## Concrete egress and control ownership

Accept **`os-tcp443+pinned-native-tls+loopback-browser-v1`** together with **`browserTopology: sibling-playwright-v1`** for this named frozen run only. The OS driver profile permits TCP443 generally plus loopback; it is not destination filtering. Frozen native transport supplies exact Anthropic HTTPS endpoint/POST, reviewed numeric IP, verified TLS/SNI, no proxy/DNS/redirect/retry and pre-dispatch accounting. The credential-free library owner and Chrome instead start under their own loopback-only root. The guarded controller connects to the private loopback endpoint; no `exposeNetwork`, arbitrary headers or proxying is introduced.

Parent may now execute a **connection-only** controller under the actual paid profile against the frozen owner under its separate loopback profile, using the same session/binding/public Playwright interfaces without loading the application or invoking any model/provider. Parent may resolve only the provider hostname and perform credential-free pinned TLS afterward. These checks send no provider HTTP/auth request and do not spend model-call quota. They must not be presented as provider schema/tool/interpretation acceptance.

If any connection/profile/TLS/cleanup check fails, stop before invocation; no unguarded fallback or relaxed browser policy. The actual run requires successful checks, an approved IP, a fresh execution UUID, complete activation JSON and explicit parent instruction. The ordinary launcher applies independent child profiles; it must not itself be nested under a paid/deny parent.

## Exclusive ledger and shutdown limits

Only the canonical absolute ALPHA `usage-ledger.json` and adjacent `attempt-ledger.md` are paid authority. At allocation, all five old call rows/totals are unchanged: **5 calls / US$0.09113535**, zero outstanding per-call holds. Parent retains control during connection checks and explicitly delegates the sole writer to `m7-provider-execution` only with launch instructions. No other actor may concurrently mutate the real ledger/journal or spend from a clone. The driver's exclusive-create writer lock supplements coordination.

Unknown/partial usage, pending journal, exhausted bounds or incomplete cleanup stops further invocation. Raw attestation and the native estimator remain load-bearing; no counter/response repair or invoice claim. Public close rejection/timeout reaches bounded kill; if kill also fails, resource termination is not guaranteed and cleanup remains explicitly incomplete. That requires owner recovery before another invocation/allocation, not a successful result or an automatic retry. The test-only rescue path is not production recovery.

No provider contact has occurred at this decision. The parent will retain each later gate result and the actual run accounting separately.
