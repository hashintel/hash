# Aggregate state why correction — bounded handoff

## Disposition and scope

This closes only the confirmed explanation-selection blocker from the independent review at `/tmp/m7-typed-review.WjrqZt/review.md`. It does not expand construction breadth. Original checkpoint source `3b98cb287275c60c45087e4dc2500f626a39f163` and evidence `1031d34c1e56769453533fd85af45767438c1fdd` remain immutable, including the original overclaiming aggregate behavior. The complete independent report, original failing discriminator, outputs and log are retained unchanged under `original-review/` with compressed/uncompressed hashes.

**Correction source:** `87beb4c654926964d9012535331066ed7bfd0334`, signed. Its exact write set is only:

- `apps/brunch-agent/src/conversation/why.ts` — 28-line conservative refusal at the existing selection boundary.
- `apps/brunch-agent/test/aggregate-why.test.ts` — eight focused tests over untouched actual captured history, without loading it into a product store.
- `apps/brunch-agent/test/typed-state.integration.ts` — mounted aggregate/row/type-elements negatives, working leaf controls and exact outside-callback counts in the existing actual Chrome witness.

No plugin/schema/effect mutation, browser executor, core evidence/state, runtime patch, package/shared inventory, MISSION, admission/accounting/provider code, or published package was changed by this worker. Parent-owned ancestor `8cf9690a40` separately admits the concrete aggregate regression fixture in `apps/brunch-agent/test/architecture/boundaries.integration.ts`; the parent confirmed its ownership and instructed that it be kept/pinned. The correction source commit itself still has exactly the three worker paths above. The owner now requests landing current work and reassessing mission health with **no further breadth**. That hold supersedes the original checkpoint's proposed post-review continuation; this packet does not authorize those remaining operations or modify the mission authority.

## Confirmed failure and smallest correction

The old `affects` selection sees a changed ancestor or exact field, but not a descendant unless querying literal `entity`. An `initialState`, row or `elements` aggregate consequently exposed current migrated/corrected children with the obsolete container-creation basis. Generic partial/utility disclaimers did not repair that structured misattribution. The original review discriminator failed on the actual reachable capture; its primitive explicit and derived controls already worked.

The correction does not reverse coverage and choose the latest child. After existing governing selection and origin/history collection, it checks state aggregates (`type`, `type-element`, `scenario`) for later strict-descendant effects in each independently observed identity-relative snapshot. If present, the aggregate refuses: a single governing basis for current parts is unavailable. It returns neither `governing` nor a purported governing `recordedChange`; current target, creation origin, applied-change/attempt history and exact reconciliation remain available. It adds no composition, per-cell provenance engine, persistent state or reconstructed cause.

This is deliberately conservative, including when later direct descendants cite the same operation-level revision. It does not attempt to prove that every current child shares a basis. Unchanged aggregates and primitive fields remain eligible for their existing scoped answers. Existing root-node/arc query selection remains unchanged; this bounded guard belongs to the newly exposed state aggregate surface.

## Red and green oracles

| Oracle | Result and limit |
| --- | --- |
| Untouched independent red | Original `aggregate-why.mjs/json/log` and `review.md` preserved byte-for-byte. Old output credited `typed-scenario`/revision one for current `[[2,true],[3,false]]` and `typed-type` for changed elements; required assertion exited1. Original files were never rerun in place or overwritten. |
| New focused tests against old production | **Six failed, two passed**, exit1. Aggregate and live-reconciled aggregate assertions fail; existing leaf/unchanged-field controls pass. |
| New mounted Chrome assertions against old build | **Exit1**, callback error `partially-supported !== refused` surfaces in the outer assertion. Forty-four callbacks completed before the first aggregate failure; no page/server/blocked-origin errors. This is a real failing required assertion, not a false green through faux callback handling. |
| Replayed independent captured-history discriminator | **Exit0**. Preserves the original required assertion and additionally checks row/type refusal, no obsolete or latest-child governing basis, exact target/origin/applied-history/attempt/reconciliation preservation. Entire explicit and derived leaf result objects remain equal to the independent original outputs. This is a deterministic product-function replay, not a second mounted run. |
| Focused app regression | **39 tests / three files pass**: aggregate why, existing reconciliation, provider admission. |
| Complete app build/unit/type/lint | **36/36 uncached forced-serial tasks**, full app server and client build; **323 app unit tests**, exit0. This is the app-selected dependency build/check portfolio, not a new whole-repository 66-task claim. |
| Architecture inventory regression | **27/27**, exit0, with the parent-owned `8cf9690a40` admission; no worker inventory changes. |
| Final actual rebuilt Chrome | **83 synthetic requests, 64 externally counted callbacks, 12 records** (ten applied, one no-op, one stale), exit0. No page/server/blocked-origin/callback errors. |
| Raw mutation audit | Unchanged independent audit passes all twelve complete pre/post records: **37 created, five updated, zero deleted, seven derived**. The extra queries did not add or change mutations. |

The first corrected build completed but type/lint rejected a test's spread of a union query with a widened `field`. The test now passes the changed query through the existing parser; production was not changed to fit it. The final full app build/type/lint/unit run passes. Exact red, intermediate and final logs are retained; the original checkpoint's broad portfolio, browser regressions and falsifier remain prior evidence, not silently claimed as rerun here.

### Mounted field outcomes

After actual reopen, each negative obtains its ordinary query through the real mounted `brunch_why` and prior raw read. All five aggregate negatives retain origin/history and live-observation scope while omitting governing basis/change:

| Query | Final result |
| --- | --- |
| Scenario `TestInitial`, `initialState` | Aggregate refusal. |
| Same scenario, `/initialState/content` | Aggregate refusal. |
| Same scenario, row1 selected using actual tool-returned place ID | Aggregate refusal; it contains explicit3 and still-derived false. |
| Type `TestCorrectedAttributes`, `elements` | Aggregate refusal rather than original string-only type basis. |
| Same type, `entity` | Current aggregate refusal; creation origin remains `typed-type`. |
| Scenario row1/value leaf | Existing partially-supported linkage to `typed-explicit-initial`/revision two. |
| Scenario row1/active leaf | Refused derived default from `typed-active`; not assigned the explicit sibling's basis. |
| Type element `value`/`type`, type `name` | Existing primitive correction positives pass. |
| Default overrides, generated/sanitized kernel, absent field and external change | Existing honest refusals pass. |

The focused tests also preserve unchanged `scenarioParameters` aggregate and scenario name support, and compare aggregate versus leaf exact serialization-equivalent reconciliation. JSON content, raw hashes, live/as-of labels, origins and attempt history are not relaxed. The correction does not establish source relevance, useful explanations, token identity or behavioral correctness.

## Commands and pins

All execution remained unpaid, with freshly verified existing process-tree denial. `verify-network-guard.mjs` passed before builds/probes. No install, artifact download, network-profile change, provider/credential work, package change, history rewrite or push occurred. Browser/listener execution alone used loopback-only; commands, tests and build descendants used deny-network. Signed commits used the existing commit profile with exact local SSH-agent socket paths only.

From repository root, `E=libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/native-local-delivery`:

```sh
node "$E/verify-network-guard.mjs"
sandbox-exec -f "$E/deny-network.sb" yarn workspace @apps/brunch-agent exec vitest run --config vitest.config.ts test/aggregate-why.test.ts test/reconciliation.test.ts test/provider-admission.test.ts
sandbox-exec -f "$E/deny-network.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 CARGO_NET_OFFLINE=true VITE_BRUNCH_CHAT_ENDPOINT=/agents/chat yarn exec turbo run build lint:tsc lint:eslint test:unit --filter=@apps/brunch-agent --force --concurrency=1 --continue=always
sandbox-exec -f "$E/deny-network.sb" node --experimental-strip-types libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/typed-state-aggregate-correction/aggregate-why.mjs /tmp/m7-typed-review.WjrqZt /tmp/m7-typed-aggregate-captured-green.json
sandbox-exec -f "$E/loopback-only.sb" env M7_TYPED_STATE_OUTPUT=/tmp/m7-typed-aggregate-browser-final HASH_OTLP_ENDPOINT='' yarn workspace @apps/brunch-agent test:typed-state
sandbox-exec -f "$E/deny-network.sb" node libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/typed-state-checkpoint/audit.mjs /tmp/m7-typed-aggregate-browser-final
```

Use fresh output paths on replay; these name retained original outputs, not reusable destinations. The captured-history replay takes the independently retained review directory and writes only its supplied fresh output. The actual browser retains its original live store at `/tmp/m7-typed-aggregate-browser-final/conversation.db`; no recorded history or saved definition is imported as state. Losslessly archived JSON includes full raw native/browser/why/history/effect observations. `source-and-build-manifest.json` pins the correction sources and complete built files; installed runtime pins are verified unchanged against the exact linked original manifest. `artifact-manifest.json` pins evidence bytes and decompressed originals.

## Hold and mission-health limits

Return this bounded corrective packet for review/integration, then **hold**. The mechanical typed path and the repaired aggregate refusal do not complete the remaining root construction stratum. Recorded removals, additional arc operations, conditional parameters and other unavailable footprints remain unresolved obligations, not discarded meaning. No new completion, provider/genuine admission, simulation, semantic utility or Step B claim follows. Landing/integration and the requested mission-health reassessment remain parent/owner decisions; no lateral expansion is underway. During packaging, the parent reported integration to ALPHA and a follow-up reproduction of the same aggregate issue on existing transition `inputArcs`/`outputArcs` at `/tmp/m7-landing-20260909T055346Z/root-aggregate.*`. The parent owns that broader shared-guard correction and root-query controls on ALPHA. This worker did not duplicate it: this packet remains explicitly typed-scoped at `87beb4c654`, and the parent's combined verification must supersede current-source claims.
