# Sibling browser and bounded shutdown — integrated

## Disposition

Accept the separately guarded browser adapter and the reviewed close-rejection/timeout correction for this bounded A5 evaluator. The original paid→nested-loopback failure remains retained and r1 remains released unused. **No final instrument freeze, provider-capable connection test, TLS check, allocation or real invocation is inferred from this integration.**

| Worker commit | Alpha commit | Scope |
| --- | --- | --- |
| `070cb6fb42` | `8d4f3cc8bb` | Separate guarded owner/controller, private one-run binding, same dry/real connection route |
| `3d4a5ebab5` | `f9fd5fd88a` | Initial adapter evidence and explicit limits |
| `c9f8ced5ec` | `220f514f56` | Rejected/timed-out close enters separately bounded public kill; honest outcomes |
| `20bc9501c7` | `e9726a86b5` | Actual four-fault proof and original reds |
| Parent comment-only update | `bef5dab714` | Describe separate browser roots instead of obsolete nesting; executable profile forms checked identical |

The parent verified three correction source pins, 24 protected non-ledger pins, 106 build pins, 563 browser-library/profile/binary pins, and all 370 initial/363 correction packet artifacts. The [independent focused correction review](review/review.md.gz) re-executed the original fault plus the four new shutdown controls, dry5 and five lifecycle controls. It found no remaining blocker at the bounded correction seam; the parent then rebuilt/replayed the merged branch with the capacity and root-node changes installed.

## Actual merged results

- **66/66 uncached tasks / 3,254 tests**, all affected core/Petrinaut/Brunch/app/website builds, unit suites, types and lints pass. Counts: core1554, Brunch128, plugin85, binding20, transport45, app337, Petrinaut699, website386.
- Actual sibling **dry5**: five synthetic native requests, real browser read/result continuation, zero browser/listener/blocked-origin errors. Owner/driver/Chrome gone, profile removed, endpoint ECONNREFUSED, binding/readiness removed; public close completed and kill was not needed.
- Existing **five** lifecycle controls pass: foreign execution, attach refusal, owner lease, owner interrupt and launcher interrupt. Their zero-native-dispatch and cleanup checks remain intact.
- Actual close rejection: public kill invoked after **0.269ms**, production cleanup complete, owner/Chrome/profile/endpoint gone, no rescue needed.
- Actual hung close: public kill invoked after **5.002s**, production cleanup complete, no rescue needed.
- Injected public kill rejection/hang: production cleanup remains **incomplete**, resources survive at that observation, and failure details are retained. Separate test rescue subsequently closes those test-owned resources; parent PID/profile checks confirm none was left running. Rescue is not production success or permission for a retry.

All SDK/browser/application work was under independently applied loopback-only roots; ordinary orchestration performed process/filesystem work only. No paid profile, provider/auth/DNS probe or live ledger write occurred. The native/raw-accounting, all-canonical retry counting, scenario, guidance and actual app/UI/result path remain unchanged by this adapter.

## Accepted mechanism and explicit limits

The library owner uses public Playwright launchServer with explicit127.0.0.1/ephemeral port and a random private endpoint; the driver connects without `exposeNetwork`, proxy, custom headers or arbitrary option forwarding. Run/execution/owner/launcher/library/profile identity is checked before application/native work. Owner/control Node and Chrome receive minimal non-credential environment and fresh private directories. Dry and real use the same sibling topology; only an explicitly allocated real driver's profile differs.

The close/kill **attempts and reporting** are bounded. **Process termination is not guaranteed if both public shutdown avenues fail.** Such a run is an explicit incomplete-cleanup stop requiring owner recovery; it cannot be called successful, automatically retried or followed by another allocation while resource/accounting state is unresolved. No generic process framework or broader process killing was added to imply a universal guarantee. The parent accepts this precise limit for the controlled local synthetic instrument, not power-loss, blocked-event-loop, exhaustive descendant or hostile-same-UID security claims.

Endpoint capabilities remain in private lifecycle files, outside page/model input. Failure stacks are private evidence. Old manifests and old activation without the new topology/execution identity remain unlaunchable. Final freezing must include the actual merged code, new launcher/library/profile identities and full current source/build artifacts.

## Commands and evidence

Parent output root: `/tmp/m7-sibling-integration.pLzx7u`. After the process-tree verifier, the same eight-package forced serial `build test:unit lint:tsc lint:eslint` portfolio as [root integration](../root-creation-integration-alpha/integration.md) ran under deny-network.

Actual browser commands run from an ordinary shell, not nested inside a broader/restrictive parent profile. Their implementation applies guards independently before any SDK/browser work:

```sh
TMPDIR=/tmp/m7-sibling-integration.pLzx7u node --experimental-strip-types apps/brunch-agent/src/evaluations/real-provider-a5/launch.ts dry /tmp/m7-sibling-integration.pLzx7u/dry
TMPDIR=/tmp/m7-sibling-integration.pLzx7u node --experimental-strip-types apps/brunch-agent/test/real-provider-a5-browser.integration.ts /tmp/m7-sibling-integration.pLzx7u/lifecycle
TMPDIR=/tmp/m7-sibling-integration.pLzx7u node --experimental-strip-types apps/brunch-agent/test/real-provider-a5-shutdown.integration.ts /tmp/m7-sibling-integration.pLzx7u/shutdown
```

All command exits0 mean their declared positive/negative assertions held. The kill-failure controls are still negative production outcomes. Do not reuse existing output directories. `artifact-manifest.json` hashes 164 parent integration artifacts; the review manifest separately retains the independent raw fault/normal observations and commands. Original profiles were removed, stores remain at their local paths, and the temporary test pane was closed.

Actual paid usage is **5 calls / US$0.09113535**, no active reservation. Next is one fresh merged proving-worktree build/pin/preflight, then a named parent allocation and actual differently guarded controller→browser attachment plus native TLS checks before invocation. This is no real-provider/genuine/utility or Step A/B acceptance. Nothing was pushed.
