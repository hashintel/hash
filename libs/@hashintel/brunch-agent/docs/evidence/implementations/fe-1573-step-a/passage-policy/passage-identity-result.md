# Preliminary synthetic passage-policy result — Partial

> Historical result; raw contract-establishment payloads were retired in the [bounded reduction](../landing-20260909/evidence-reduction.md). Original commands, manifests and payload links below describe the recorded run, not a maintained replay or a claim that those bytes remain local.

## Boundary and verdict

**Selected existing branch: Partial — unsupported continuity classes explicitly lose automatic relations; use immutable revision-local fallback.** The edit itself settles successfully; refusal here means no automatic evidence carry, not an `update_workpiece` error. No cross-revision introduced-by, identity reuse, predecessor/successor sets or source relevance is earned. This is the Mission 7 Step A preliminary synthetic matrix, not the final genuine passage-policy or semantic/utility verdict and not Step A/Step B acceptance.

The oracle is `apps/brunch-agent/test/passage-policy.integration.ts`, through the actual built ChatAgent `/agents/chat/:instanceId`, ownership headers, SDK send/read/history, mounted `brunch_workpiece` and `update_workpiece`, and existing native Anthropic SDK synthetic-response helper. The labelled `validated-fixture-mutation` initial browser binding is needed by the current mount; it is not an observed browser base or browser success. No browser operation was issued. All user sources and operational prose are TEST-authored, not genuine testimony.

Final artifacts: [`final-replay/result.json`](final-replay/result.json) contains every exact model-facing candidate/current response, old revision, successful revision input/output and negative output. [`final-replay/native-contexts.json.gz`](final-replay/native-contexts.json.gz) retains all actual model contexts and native serialized tool schemas/requests. [`final-replay/histories.json.gz`](final-replay/histories.json.gz) retains queried histories. Final replay: **58 rows, 215 synthetic native requests, zero paid calls**. Rows include seed controls, not 58 independent edit classes. The `final/` run independently passed the same 58-row/215-request assertions before an assertion-preserving unchecked-index cleanup; `instrument.json` pins the exact final replay source.

## Fixture and interpretation

Baseline Markdown is `# TEST account\n😀 Reserve one crew.\n\nTiming remains unknown.`. Baseline declarations are elicited and formalism-constraint at `[18,35)`, overlapping inference at `[26,34)`, and disjoint default at `[37,60)`. UTF-16 offsets deliberately differ from UTF-8 byte offsets because of the emoji. IDs and successful spans come from actual `brunch_workpiece` outputs; no private string-index helper supplies the success path. Hash/slice comparisons are independent assertions. Candidate lookup has hash/length but no revision identity and must leave current state exactly unchanged. Each settled edit must have its own tool-call revision ID, exact SHA-256/content, ordinal increment and expected evidence. Omitted evidence stays omitted in raw input even when output carries relations.

`All four` below means the baseline declarations above; `none` means the evidence field is absent, not an invented empty causal relation. JSON row indices refer to `final-replay/result.json.rows` (zero-based). Pass is bounded mechanical success/refusal, not semantic endorsement. Partial explicitly names continuity that is not provided.

## Complete edit matrix

| Case | Actual product response | Bounded outcome | Row |
| --- | --- | --- | --- |
| Unchanged passage + unrelated append | All four relations carry at original spans. | Pass: same-span unique unchanged carry. | 1 |
| Heading rename, same width (`account` → `renamed`) | All four carry; passage positions/text unchanged. | Pass locally, not heading identity. | 3 |
| Heading rename changing offsets | New revision, no evidence. | Partial: no offset migration. | 5 |
| Move via prepended text | New revision, no evidence. | Partial: no moved-passage continuity. | 7 |
| Paraphrase (`Hold a single crew.`) | New revision, no evidence. | Partial: no paraphrase continuity. | 9 |
| Ambiguous paraphrase (two possible formulations) | New revision, no evidence. | Pass refusal / Partial continuity. | 11 |
| Split into `Reserve.` / `One crew.` | New revision, no evidence. | Partial: no predecessor/successor sets. | 13 |
| Merge by rewording both sentences | New revision, no evidence. | Partial: no merge ancestry. | 15 |
| Merge by replacing two newlines with two spaces | All four carry unchanged at the same offsets. | Pass local spans only; paragraph boundaries/meaning are not assessed. | 17 |
| Deletion | New revision, no evidence. | Pass: no stale relation. No passage identity API exists. | 19 |
| Reintroduction after deletion, no declaration | Text/hash return to baseline; fresh revision ID and no evidence. | Pass no automatic resurrection / Partial identity policy. | 20 |
| Reintroduction with explicit new declaration | New elicited relation at `[18,35)` using authorized old source ID. | Pass new revision-local declaration, not recovered continuity. | 21 |
| Duplicate wording appended | Three quote-related relations disappear; only default `[37,60)` carries. | Pass ambiguity refusal. | 23 |
| Duplicate wording inside a quote block | Same: only disjoint default carries; quoting does not disambiguate literal carry. | Pass ambiguity refusal. | 25 |
| Duplicate heading only | All four passage relations carry; heading duplication alone does not duplicate passage text. | Pass local-span carry, not heading identity. | 27 |
| Duplicate previous wording → unique current wording | Only default carries despite current quote becoming unique. | Pass: uniqueness is required in old and new content. | 29 |
| Explicit declaration after move | Correction `[31,48)`. | Pass new declaration; automatic continuity remains unearned. | 31 |
| Explicit paraphrase declaration | Correction `[18,37)`. | Pass structurally; semantic correspondence unassessed. | 33 |
| Explicit split declarations | Two corrections `[18,26)` and `[27,36)`. | Pass local declarations, not predecessor/successor sets. | 35 |
| Explicit merge declaration | Correction `[18,60)`. | Pass local declaration, not merged identity. | 37 |
| Explicit duplicate selection | Second occurrence correction `[79,96)`. | Pass explicit selection only, not automatic disambiguation. | 39 |
| Explicit whole-document broad basis | Elicited `[0,103)` accepted, including unrelated weather/staffing prose. | Partial mechanical; cannot pass semantic overbroad-basis/utility on this evidence. | 41 |
| Explicit narrow overlapping correction | Correction `[26,34)` replaces all three intersecting old relations; disjoint default survives. | Pass explicit override versus automatic conservation. | 43 |
| Deliberately stale in-bounds explicit span | Old `[18,35)` accepted on `Discuss the rain.`; default separately declared. | Counterexample to hash-bound/relevance interpretation of declarations, **not** automatic carry. | 56 |

### Semantic limits exposed, not patched

The broad `[0,103)` range mechanically includes the narrower available `[18,35)` support and unrelated material. Its validity is not relevance or sufficient utility. Recognizing that this is overbroad is a disclosed semantic judgment about the TEST fixture, not an implemented heuristic and not Lu's final adjudication.

The stale explicit-span control deliberately does **not** preserve old content/hash scope: it is a negative counterexample. The existing evidence envelope has only current-local numeric spans, source IDs and standing, not an originating lookup hash/revision. `update_workpiece` therefore accepts the deliberate old coordinates as a **new current-local declaration**, not as authenticated old scope. The accepted text is exactly `Discuss the rain.`. All ordinary successful declarations use newly returned candidate spans; this exception is clearly segregated and earns no source relevance. No new identity/hash field or production fix is proposed.

## Negative controls and current authority

| Control | Actual result | What absence would admit | Rows |
| --- | --- | --- | --- |
| Assistant ID, prepared signal ID | `output-error`; exact seed revision/content/hash/evidence remains. Neither ID appears among eligible returned sources. | Non-user material laundered into elicited support. | 46–47 |
| Actual foreign-conversation source ID, unknown ID | `output-error`; seed remains. | Wrong-conversation or invented-source attribution. | 48–49 |
| Empty elicited source list | `output-error`; seed remains. | Unsupported elicited standing. | 50 |
| Out-of-bounds stale span | `output-error`; seed remains. | Mechanically invalid revision-local range. | 51 |
| Invented `revisionId` selector on read | Strict input rejects as `output-error`. | Treating this current-only locator operation as a historical selector. | 52 |
| Foreign principal / foreign conversation headers against same owned history URL | Both HTTP 403; provider call count unchanged. | Wrong-owner access. | 53–54 |
| Reload original application/store after refusals | Actual mounted current response remains `seed-45-revision`, original four relations and current quote lookup. | Promoting failed candidate or latest attempted write to authority. | 57 |

A candidate lookup never settles state, and successful edit observations compare against precisely one current WorkpieceRevision. Duplicate, moved, paraphrased, deleted and reintroduced automatic cases are the wrong-revision/ambiguous-carry controls. There is no diagnostic-only positive source lookup or saved-history import.

This focused reload is in one OS process; it does not re-earn compaction/process restart. Existing `reopened-why-retention*` and its corrected original-store process proof were read, not rerun or expanded here. That accepted baseline separately demonstrates original entries/cached answers absent from folded context while current state remains authoritative. No claim here depends on rebuilding current state from earlier context/history. Source-window truncation, unavailable history recovery, arbitrary missing state, browser why, real model interpretation and semantic relevance are not new passes from this matrix.

## Red/green and oracle correction

1. `run-1` failed because the first instrument omitted the existing initial browser binding and thus did not mount `brunch_workpiece`. The retained native tool inventory exposes that mistake. The correction used existing TEST initialData wiring, not production changes.
2. `run-2` reported success but **is not valid matrix proof**. Parent independently mutated only the final seed assertion from ordinal 1 to 999; both original and mutant exited zero. Complete original/mutant source and product results are retained under `owner-false-green/`, copied from `/tmp/m7-passage-owner.0y3RT4` without editing those artifacts.
3. The existing native synthetic-response helper can normalize a faux response-factory exception to a normal native stop. Assignment-before-assertion presence checks were inadequate. The focused test now wraps every response factory, retains any exception, and asserts no exception occurred **outside** that boundary after SDK completion. All existing content/identity/relations assertions remain. Shared helper and production behavior are unchanged. `run-3` is the first corrected green.
4. On the final pinned source, the exact ordinal-999 mutant exits **1**. A later carried-evidence comparison forced to `[]` exits **1** after the seed; a later current-lookup comparison forced to the hash of `WRONG CONTENT` also exits **1**. See `final-mutation-tests.json`, corresponding `.diff`, failure JSON and gzipped logs/native contexts. Temporary same-package mutant files were session-owned, removed after execution, and the original source hash was verified unchanged. These are mutations of the actual matrix assertions, not `assert.throws` demonstrations.
5. Final green replay produces 58 rows / 215 requests; the final source/build/runtime manifest remains unchanged through green and all three red runs. Initial typecheck failures (closure narrowing, then omitted faux-factory arguments) remain in retained logs; final typechecks pass.

## Verification and handoff limits

- Exact starting branch `ln/fe-1573-passage-policy`; exact base `b5a8b6224db49f4960d2269c0150a6b5e81340dc`; initial `git status --porcelain=v1 --untracked-files=all` was empty.
- Parent-only admission commit `a45f9b2777c882fc398bec4a6bd13e770b7247a1` adds this concrete importer to the architecture inventory and optional app script. Test implementation commit `8b1a110436` contains final source SHA-256 `540387183ed687ef7949792816edf0472df84f5494d00f496d88ef2640746fa2`. This session changes only the new test and this evidence directory.
- `verify-network-guard.mjs` passed shell → Java/Node external denial and loopback allow/deny checks before install/build/probes. Actual install/build/test/probe commands use retained `deny-network.sb`; no listener/browser is needed. Global fetch additionally refuses fallback in the focused test. No provider/auth-validity calls or downloads occurred.
- Immutable cache-only installation used `YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 YARN_ENABLE_SCRIPTS=0 yarn install --immutable --immutable-cache`. OpenAPI 6.6.0 JAR regular-copy source/destination identity, 27,103,489 bytes and SHA-256 `9718ff7844e89462c75dcd9b20a35136f6db257bfe1b874db1e3002e99de4609` are recorded in `generator-artifact.json`; no links/overwrite/download/runtime edits.
- Full app build: `yarn exec turbo run build --filter=@apps/brunch-agent --concurrency=1 --force`; app server **and client** scripts completed. Native tools are the actual installed patched Flue 2.0.3 / Pi AI and Agent Core 0.83.0; Node 22.21.1 and Yarn 4.16.0. `instrument.json` pins source, guidance, build artifacts, installed runtime, lockfile and patches; native captures pin the actual model-facing catalog per request. Synthetic selected model name is `anthropic/claude-sonnet-4-6`, not a provider fidelity claim.
- Final uncached scoped portfolio: `yarn exec turbo run test:unit lint:tsc lint:eslint --filter=@apps/brunch-agent --filter=@hashintel/brunch-agent --concurrency=1 --force`: **39/39 tasks; core 128 tests, app 287 tests**; existing lint warnings, zero errors. Final source cleanup was checked again via `verification-replay.log.gz`. This is not a whole-Brunch portfolio claim.
- Final mounted command (root): `PASSAGE_POLICY_OUTPUT=<fresh-absolute-path> yarn workspace @apps/brunch-agent test:passage-policy`, prefixed by `sandbox-exec -f <native-local-delivery/deny-network.sb> env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0`. Results are intentionally written to a new path; no existing packet is overwritten.
- Markdown prose is not hard-wrapped. No production semantics, guidance, dependencies, runtime patches, paid ledgers, MISSION, shared helper or owner-held acceptance is changed. No push, history rewriting or external publication.

The return is a discriminating preliminary **Partial**, with legitimate bounded carry and honest local declarations/refusals. Full passage lifecycle, introduced-by semantics and genuine useful support remain unearned; Step B and owner utility acceptance remain unauthorized.
