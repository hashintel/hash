# Native-schema local delivery handoff

> Historical result; raw contract-establishment payloads were retired in the [bounded reduction](../landing-20260909/evidence-reduction.md). Original commands, manifests and payload links below describe the recorded run, not a maintained replay or a claim that those bytes remain local.

## Result

**Delivered locally:** three pinned Yarn patches, explicit Standard Schema dependency resolution, native canonical root `addArc` plus previously earned `addType`, and the real built ChatAgent → Anthropic preparation/SDK serialization → native validation → real-Chrome record/result continuation path. Strict generation remains off. The final guarded portfolio passes **63/63 uncached tasks and 1,516 tests**. This is synthetic local delivery evidence, not real-provider acceptance, genuine Vestera construction, paid readiness or Mission acceptance.

**Network-policy incident:** an initial unguarded transitive build downloaded OpenAPI Generator 6.6.0 despite Yarn/Corepack/Cargo offline settings. This campaign **cannot claim zero external requests**. Execution stopped, the owning parent authorized guarded continuation under the same constraint, and subsequent builds/browser witnesses used demonstrated OS-level descendant denial. The exact original log, artifact identity, uncertainty and guarded retries are retained in [network-incident.md](network-incident.md). There were **zero application/provider paid calls**, no ledger allocation/change and no invoice claim. No upstream source acquisition, contribution, release waiting or version upgrade occurred.

## Commits and ownership

- Base/reconciliation: `113c8ada6f9a74d097bfbeb1b944db8c39223759`, branch `ln/fe-1573-local-native-schema`, assigned `m7-native-delivery` worktree only.
- `6eae999b8f` — **Install pinned native schema runtime patches**: reviewed candidate port, centralized resolutions, explicit package extension and first real install/type proof.
- `fa0537fed6` — **Join canonical native inputs to the prepared browser path**: product/schema/parsing join, test-only converter retirement, native/browser oracles, and the required synchronous built-in preparation correction discovered by the full suite.
- The following final packet commit retains this evidence and strengthens the mounted oracle to require nonempty native carriage for each provider method and a successful correlated addType awaiting-client result. `source-and-build-manifest.json` pins those exact tested source bytes and the final browser build; its implementation-commit field names the preceding product milestone, not a claim that the supplemental assertion was already in that commit.

`changed-files.txt` lists the production/configuration/test write set; `artifact-manifest.json` inventories this packet. `apps/brunch-agent/src/app.ts`, `src/provider-admission.ts`, ChatAgent composition, core model/marker/revision semantics, A4 history-retention files, `MISSION.md`, both paid ledgers and the unchanged ordinary mixed-batch oracle match the base. No accounting or A4 seam was edited. Nothing was pushed.

## Package-manager delivery

Exact targets:

| Package | Maintained patch |
| --- | --- |
| `@flue/runtime@2.0.3` | `.yarn/patches/@flue-runtime-npm-2.0.3-192c31f50c.patch` |
| `@earendil-works/pi-ai@0.83.0` | `.yarn/patches/@earendil-works-pi-ai-npm-0.83.0-c607801251.patch` |
| `@earendil-works/pi-agent-core@0.83.0` | `.yarn/patches/@earendil-works-pi-agent-core-npm-0.83.0-6cd8fe314f.patch` |

Yarn 4.16.0 `patch`/`patch-commit` machinery produces the patches; root `resolutions` covers the existing exact/range consumers. `.yarnrc.yml` adds `@standard-schema/spec@1.1.0` through `packageExtensions`, not merely the patched manifest. `final-standard-resolution.log.gz` confirms the exact edge from the patched Flue locator. The plugin explicitly depends on already-pinned Zod 4.4.3 and externalizes it in its library build. Unrelated versions/resolutions and workspace Flue version declarations remain unchanged. Valibot remains 1.4.2 and Pi's nested Anthropic SDK remains 0.91.1; the complete source/version/build pins are in `source-and-build-manifest.json`.

The original candidate had one product-regression gap: Flue's `abandonToolOnAbort` calls `.then` on its callback result, but built-in tool preparation is synchronous. The copied-package diagnostic exercised native/Valibot custom tools, not `activate_skill`. The unchanged product tests failed with `pending.then is not a function`; `skill-preparation-failure.log.gz` retains the synthetic tool-result context. The maintained patch changes only that newly introduced call to `abandonToolOnAbort(async () => prepare(...), signal)`. Native async parsing, cancellation and the original built-in preparation result now share the same existing abort contract. The unchanged legacy transport/skill and headless oracles pass, alongside all native tests.

A zero-context re-render of the patch was tried to avoid Git treating unified-diff context prefixes as whitespace; Yarn correctly refused one insertion hunk. `zero-context-patch-refusal.log.gz` is retained. Final patches are Yarn's supported ordinary unified output; no failed variant is installed. Do not run code formatters over generated patch payloads. The final correction diff and intentional source formatting pass; whitespace-only context prefixes in the initial generated patch commit are patch syntax, not source edits.

Installation/reproduction procedure: [installation.md](installation.md). Use only existing local caches/artifacts, including the now-local generator JAR for the transitive full build. Missing artifacts must fail under the guard and return to the owner, not trigger another download. Source maps are published upstream maps, not regenerated mappings of changed bundle lines. There is no dependency upstream source-build or upstream-regression-suite claim.

## Product change, without new admission

- `packages/plugin-sdcpn/src/root-arc.ts` composes `canonical.safeExtend({ brunch: rootArcEnvelopeSchema })`. It does not copy Petrinaut fields. `safeExtend` retains native canonical `.check()` rules; the existing root-place/single-base restriction stays explicit. Native input export preserves the root description, strictness, nested schema and default semantics. Brunch basis is now Zod-owned in `declared-basis.ts`; the shared hash pattern also keeps the existing Valibot initial-data check unchanged in meaning. Core-owned output/result/initial-data schemas are not migrated.
- Joined `addArc` uses Flue's public native schema input plus the explicit `prepareArguments` normalizer. `addType` uses the exact canonical Zod schema object. Canonical validation runs before Pi's validated-argument hooks without lossy generic JSON-Schema coercion; parsed output reaches run once. The runtime diagnostic proves defaults/transforms, hook blocking and late successful async validation after cancellation without execution.
- `parseJoinedRootArcInput` remains the single explicit raw-call/browser compatibility boundary. The app's receiving boundary now calls it instead of Valibot parsing. The existing website already used that public parser, so no website production edit was needed. Raw numeric-string input and the complete basis remain in canonical history; only the detached execution input is normalized and stripped of `brunch`.
- The converter is physically retired from production: unchanged implementation moved from `src/tools/canonical-schema-carrier.ts` to test-only `test/schema-carrier.ts`. Its historical structural/refusal controls remain intact and are no longer used as native acceptance. Architecture tests permit Zod only at the reviewed plugin native input/basis ownership sites and forbid production converter imports. No application private dependency imports were introduced.
- Ordinary, legacy prepared, opt-in joined and headless mode boundaries remain unchanged. Legacy loose/generic inputs stay on their existing path. Native schema diagnostics for scenarios/transitions/defaults/recursion do not mount new operation classes. The question marker remains core-owned/noninteractive; `update_workpiece` remains durable/nonterminating; there is still one model/compaction declaration. Existing validation-before-client-publication, issued incarnation/base, Voice/Stop/cancellation and stock-host contracts pass their regression oracles.

## New evidence and discriminators

| Boundary | Oracle and retained result |
| --- | --- |
| Real installation and public types | `final-immutable-install.log.gz`, `final-types.log.gz`, `final-standard-resolution.log.gz`. Actual installed patch/type resolution passes; unchanged strict historical type fixture checks native input versus parsed output, defaults/transforms, inline normalizers, Pi parsed-argument generic and Valibot output errors. The original pin file was not regenerated. |
| Native ownership TDD | `native-red.log.gz`: 3 failed / 1 passed before product wiring. `native-green.log.gz`: 4/4 pass after wiring. Final plugin suite: 69/69. Native input-schema equality, exact addType object identity, strict extra-key/boolean/refinement/root-only refusals and explicit string normalization. |
| Installed runtime/schema stratum | `runtime-final/candidate.json.gz`, `runtime-final/native-input-schemas.json.gz`, `final-runtime.log.gz`. New installed-package execution of the unchanged diagnostic assertions, **19 named observations / 26 synthetic SDK responses / zero intercepted network attempts**, no copied or ad-hoc modified dependency. Both provider entrypoints, strict omitted/false, scenario input defaults, recursive `$defs`/references, absent empty `required`, runtime-only checks, explicit normalization, hooks, parsed-once transforms, late async-validation cancellation, raw history, termination/continuation and unchanged generic/Valibot controls pass. Durable re-execution is method-level synthetic coverage, not a crash witness. |
| Actual built mount and both SDK entrypoints | `apps/brunch-agent/test/native-schema-carriage.integration.ts`, invoked by `test:native-schema` and `test:unit`; `mounted-final/{native-sdk-requests,histories}.json.gz`, `mounted-final/run.log.gz`. **20 synthetic SDK responses**. The built ChatAgent settles a real revision before joined controls; native root/addType schemas survive actual provider preparation and serialized request bodies for both `stream` and `streamSimple`. Invalid boolean/endpoints/output type refuse, numeric strings normalize, raw input remains unchanged, and successful native calls await the client without an extra model request. The first `mounted-1` correctly failed because it had not settled the prerequisite revision; its failed non-admission is not credited as native validation. |
| Actual browser throughline | `browser-final/observations.json`, `native-sdk-requests.json.gz`, `canonical-pre.browser.json`, `canonical-post.browser.json`, `transition-records.json`, `history.json`, `boolean-refusal-history.json`, `refused-history.json`, `conflicting-history.json`, `http-deliveries.json` and screenshots. **Real local Chrome, ten synthetic native SDK responses, no outbound provider calls**. The exact mounted native root schema is preserved without strict generation; boolean refusal yields no browser result/effect. The unchanged live-read → normalized root arc → independently recorded effect → one correlated continuation succeeds. Same-key duplicate and reload do not reapply; a distinct contradictory result fails before another model request. Browser/listener errors and origin-level blocked-request counters are empty. The expected conflict error printed by Flue is an asserted control. |
| Rendered inspection | Inspected `browser-final/browser.png` and the Boolean refusal state (`browser-final/boolean-refusal.png`, with its preceding run also inspected). The prepared-only banner remains visible, Boolean rejection names expected number/received Boolean, the unknown citation refuses, and the final panel shows “Added input arc” plus its correlated continuation. No new why pane, microphone/audio-provider witness or semantic explanation is claimed. |
| Protected regression portfolio | `guarded-portfolio-final.log.gz`: **63/63 tasks, zero cache hits, 1,516 tests** — core 107, plugin 69, binding 20, transport 44, app 205, Petrinaut 692, website 379. All affected build/type/lint/unit tasks pass. `guarded-portfolio-3.log.gz` is the preceding full pass; earlier failures remain retained. |
| Architecture/formatting | `final-architecture.log.gz`: 70 layers / 356 edges. `final-format.log.gz`, `evidence-lint.log.gz`, `git diff --check`. Only intentional source/config files were formatted; captured request bodies/logs are retained losslessly. |

The provider helper changes only the response source: scripted faux messages become in-memory Anthropic SSE responses consumed by the real SDK. It uses synthetic auth rather than environment credentials. `onPayload` only observes; returned payload replacement is forbidden; serialized bodies equal the observed payload and supplied parameter schema. Browser mutation results are produced by the actual browser, never fabricated by this helper. This is native carriage proof independent of strict generation, not a fake-payload workaround.

The first guarded full run exposed required `tsx` IPC, outdated architecture assertions and the built-in preparation bug. The second run exposed the known five-second legacy test timeout under competing Vitest files. App files now run serially (`fileParallelism: false`), retaining every assertion and timeout; both final complete forced portfolios pass. Root Turbo still uses the required `--force --concurrency=1`. No timeout was increased, test skipped, codegen stubbed or failure silently relabelled.

## Replay commands

From repository root after installing the pinned dependencies, with `E=libs/@hashintel/brunch-agent/evaluations/protocols/network-guard` (the byte-identical relocated guard; historical manifests are unchanged):

```sh
node "$E/verify-network-guard.mjs"
sandbox-exec -f "$E/deny-network.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 YARN_ENABLE_SCRIPTS=0 yarn install --immutable
sandbox-exec -f "$E/deny-network.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 CARGO_NET_OFFLINE=true yarn exec turbo run build test:unit lint:tsc lint:eslint --filter=@hashintel/brunch-agent --filter=@hashintel/brunch-agent-plugin-sdcpn --filter=@hashintel/brunch-agent-binding-flue --filter=@hashintel/brunch-agent-transport-aisdk --filter=@apps/brunch-agent --filter=@apps/petrinaut-website --filter=@hashintel/petrinaut --continue=always --force --concurrency=1
sandbox-exec -f "$E/deny-network.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 node "libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/native-local-delivery/installed-runtime.mjs" "$PWD/<fresh-runtime-output>"
sandbox-exec -f "$E/deny-network.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 M7_NATIVE_OUTPUT="$PWD/<fresh-mounted-output>" yarn workspace @apps/brunch-agent test:native-schema
sandbox-exec -f "$E/deny-network.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 VITE_BRUNCH_CHAT_ENDPOINT=/agents/chat yarn workspace @apps/petrinaut-website build
sandbox-exec -f "$E/loopback-only.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 M7_BROWSER_OUTPUT="$PWD/<fresh-browser-output>" yarn workspace @apps/brunch-agent test:browser-tracer
sandbox-exec -f "$E/deny-network.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 yarn workspace @local/petrinaut-arch-docs lint:arch-docs
sandbox-exec -f "$E/deny-network.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 yarn lint:format
```

The guarded browser uses local Chrome (`/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`); no browser download is permitted. Fresh output directories are required. SQLite files remain local ignored diagnostics, not portable history exports. The final browser build uses the same-origin `/agents/chat` setting; later ordinary website builds may replace local `dist`, so replay the configured build rather than claiming current build bytes are the retained witness.

## Historical evidence and remaining gates

Original native baseline/Flue-only/two-boundary/complete probes remain immutable at `dd79496585b0af71b5fa1a19de931f561fb137a9`; replay their original pins/build prerequisites at that commit, copying evidence to fresh output as the integration owner did. Their safety gates remain 1/1/1/0 for those instruments, not repinned green against this installation. The original joined-browser converter witness remains replayable at its implementation/evidence commits `1067dedee4a31a2b0935166eefcc5a961f00ec08` / `f458eab7ab51ea86d8afa3bb66877461bf2ed3ca` (or their reconciled integration commits recorded in its handoff). Paid A1 evidence and its one-use instrument are not regenerated; the active unpaid headless addType regression now asserts full native input export rather than dialect-stripped converter output.

Remaining owner gates: independent integration review; repeat relevant accounting/A4 probes on the combined patched runtime; real-provider dialect/keyword acceptance and below-admission paid accounting; broader scenario-class admission; genuine Vestera, evidence relevance/authorization, useful basis, A5 why/pane and Step A acceptance. Existing overflow continuation and crash-recovery gaps are not repaired. The root browser base remains fixed to one mutation; no broad sequential-base, concurrency, cross-process exactly-once or headless/browser parity claim is added. No genuine construction, invoice, A5, Step B or acceptance is claimed.

All changed application/Brunch workspaces are private. No published Petrinaut/core implementation/API or user-facing library behavior changed, so no changeset or public-guide edit is required. The prepared route remains explicitly synthetic; its new labelled screenshots are evidence, not replacement public-guide screenshots.
