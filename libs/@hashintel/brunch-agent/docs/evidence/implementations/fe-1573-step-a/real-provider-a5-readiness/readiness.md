# Bounded current-A5 real-provider readiness

## Status and authority

**Unpaid driver/preflight implemented and proved; ready for integration-owner review, concrete egress decision and named reservation. No paid provider request, reservation allocation or actual-ledger write occurred.** This packet does not award real-provider acceptance, genuine testimony, semantic usefulness, Step A or Step B acceptance.

- Worktree: `/Users/lunelson/.herdr/worktrees/hash/m7-real-provider`; branch: `ln/fe-1573-real-provider-a5`; exact base: `c41aeb54ca83d3aeb6f6678c9e2d318d120b5e33`.
- Implementation: `efeb4921ca39d7da8878016b022ed8628b8b5416` — **Add explicit bounded A5 provider driver and fail-closed preflight**.
- `manifest.json` pins **3,310 files**, including application/browser source and built artifacts, core/plugin guidance/resources and canonical tools, fixture, evaluation source/tests, maintained patches/lock wiring, installed Flue/Pi/native Anthropic SDK implementation, Node executable, Chrome entrypoint and sandbox executable. Manifest SHA-256: **`030ad3c7bc6886687d9b8537e28788d829c920f544d7c67bde2bf42dcef11c17`**. The manifest verifies again after final tests; no failed instrument was silently repinned as a pass. Repository-required JSON formatting changes the manifest's byte hash, not its 3,310 file identities; the original generated JSON and preflight are preserved in `manifest-generated.json.gz` / `preflight-generated.json.gz`.
- `rendered-tools-and-guidance.json.gz` retains the actual serialized initial and settled tool/system projections from the TEST transport. Initial mount has nine tools; settled mount adds `getLatestNetDefinition` and joined native `addArc`. The marker remains mounted. Full skill sources are pinned, but this does not pretend the dry model activated every resource. Real activation payloads will be retained unchanged.
- Current real shared totals remain **5 calls / US$0.09113535**, with no active reservation. The cloned real ledger and attempt journal remain byte-identical to base: SHA-256 `d5041ddc422a1889ce013b002ce6ed0250a2992ae4ab19501188d51a9c6eedf4` and `da55752de0b939ac817f7679ab1dadd9ff058fdcaa2e602063e04d188a5d0833` respectively.

## The actual path

Entry: `apps/brunch-agent/src/evaluations/real-provider-a5.ts`, with explicit **`preflight`**, **`dry`**, or **`real`** process mode. No automatic mode inference, key-presence success, credential-missing synthetic fallback, replacement provider registration, extra production route/agent or generic daemon exists.

The bounded ephemeral listener serves the already built website and forwards the actual ownership-guarded `/agents/chat/:instanceId` requests to `loadBuiltBrunchApplication()`. Unimplemented `/api/` requests go to the existing application, returning its actual response (including the unavailable voice route's 404), not a fake voice/API implementation. Chrome opens the existing labelled prepared root-arc fixture. The driver submits the two pre-authored synthetic-user messages through the actual panel. It retains their actual admission receipts, uses SDK `wait()` on those receipts, and waits for the product's busy state to settle before inspecting canonical history. Browser tools execute through the existing website host and causal client-result path, not a fabricated positive executor.

Real mode leaves the built `app.ts` provider/accounting/admission registration unchanged. The existing accounting instrument receives the **owner-provided authoritative absolute ledger path and runId**, not a reservation invented by the driver. The native SDK's configured `fetch` boundary reaches the evaluation's restricted HTTPS transport. It forwards the native serialized body without replacing its payload or assistant answer. Raw native response bytes are retained before the unchanged SDK consumes them; this adds evaluation buffering/latency and is not a streaming-latency benchmark.

The dry process alone imports `synthetic-transport.ts` and replaces ambient provider auth with a TEST sentinel. Its five synthetic responses acknowledge preparation, settle a deliberately unsupported TEST workpiece, request one real browser read, consume its actual result and acknowledge the why question without answering it. This is intentionally **not a scripted A5 construction/explanation pass**. The inspected screenshot shows the TEST label, settled workpiece tool, real checked-net result and explicit non-claim; the current-workpiece/why pane honestly remains unqueried.

## Frozen scenario and expected observations

`src/evaluations/real-provider-a5/scenario.json` is the complete pre-authored user instrument. It uses the existing admitted fixture, not Vestera or genuine testimony. The first user statement confirms that final inspection reserves one available dispatch crew until sign-off, explicitly leaves duration/failure/recovery unknown, requests evidence-aware workpiece settlement, then only the missing crew connection. The second asks why that input connection, multiplicity and kind exist, requesting recorded explanation and a fresh observation with honest distinctions/refusal.

The fixture's existing prepared topology and prior hypothesis remain labelled external test preparation. No prepared workpiece is injected as elicited evidence. The driver never calculates or supplies successful source IDs, UTF-16 spans, revision IDs, base hashes or observation references to the model. Those must come from mounted product affordances and the existing construction-context delivery. There is no persona or model-assisted evaluator: only the production ChatAgent is a paid participant.

Expected evidence, **not an answer key**: actual Anthropic schema acceptance or rejection; unscripted tool selection; product-returned source/locator/settled identity; independently observed browser read/mutation/result; actual `brunch_why` response and assistant interpretation; source standing/unknown/refusal fidelity. Missing support, missing construction/why, refusal, correction strain or stop are Partial/Fail observations, never automatic success. Deterministic linkage is not semantic relevance or utility. No broader catalogue/addType, construction-progression, genuine Vestera or durability claim is added by this instrument.

## Bounds and accounting

The actual installed catalogue is `anthropic/claude-sonnet-4-6`, `anthropic-messages`, base URL `https://api.anthropic.com`, context window **1,000,000**, native maximum output **128,000**. Rates per million tokens: input **US$3**, output **US$15**, cache read **US$0.30**, cache write **US$3.75**. Reserving the entire context at the highest input/cache rate, including one-hour cache writes at twice input rate, gives:

```text
input hold = 1,000,000 × max(2×3, 3.75, 0.30) / 1,000,000 = US$6
output hold = 4,096 × 15 / 1,000,000 = US$0.06144
catalogue worst-case per request = US$6.06144 <= proposed US$7 hold
```

The requested first allocation is **at most 20 underlying calls / US$15** inside total **200 calls / US$100**. Twenty is a ceiling, not a promise of twenty launches: every next call still requires a full US$7 unencumbered hold within both remaining run and shared ceilings. The existing `RequestLedger` validates every row/total and writes its durable unknown marker before native invocation and SDK dispatch. Successful native catalogue estimates release the unused hold; invoice amounts are not available or invented. Exceeded/missing/partial/uncertain usage or pending journal state blocks further requests; no driver settlement or journal repair exists.

The evaluation's read-only reservation preflight checks active named allocation, exact 4096/7 per-call fields, 20/15 run ceilings, totals, unknown/pending state and remaining available hold. It does not replace production row/usage validation. At the HTTP boundary the driver requires the already-persisted current run's unknown/transport-started row and rejects a repeated sequence. This also refuses an unaccounted participant/compaction request rather than letting it escape accounting. Preparation, continuation, compaction, failures and retries that reach the accounted underlying boundary consume the same authority. No second real ledger is introduced; `accounting-observations.json` is a read-only evidence snapshot, not an authority.

The existing accounting wrapper forces `maxRetries: 0`, caps the serialized output request and refuses repeated turns/dispatches. The driver additionally checks the native retry header and a once-per-sequence dispatch set. The repair guard conserves unique rejected canonical tool-call IDs across cumulative requests and browser-result deliveries, and refuses **all further provider dispatch after three rejected attempts of any one canonical operation**. Browser failed/stale/unknown/no-op outcomes are not successes. A failed submission stops the driver; it is not automatically resubmitted. Unknown accounting is retained and returned, not made free because transport or UI failed.

Single writer: the parent must delegate the one authoritative shared ledger exclusively, with no other actor writing it during the run. A sibling exclusive-create lock prevents two instances of this evaluation driver using the same canonical path; stale locks refuse and are not auto-cleared. This supplements, **does not replace**, the parent's global writer coordination; the production ledger is not a distributed locking service.

Native identity: Pi's normalized `AssistantMessage.model` is initialized from the requested model, not copied from native `message_start.model`. Therefore this driver separately retains and checks the raw provider-reported model. A successful native response with missing, mismatched, duplicated or malformed reported identity is withheld from SDK settlement and leaves conservative unknown accounting. No model alias/downgrade or native payload rewrite is allowed.

## Credentials and network gate

**Configured credentials are available** through the same default environment-backed Anthropic `Models.checkAuth()` resolution used by the built registration. Only this boolean is retained. No secret/auth source/environment dump, refresh, provider key-validity request or paid call was made. Availability is a technical prerequisite, not provider acceptance. Chrome receives only PATH/HOME/TMPDIR, never provider auth environment, headers or request artifacts; it uses an isolated Playwright profile.

All installation/build/application/dry commands used the previously verified native-local-delivery process-tree deny/loopback profiles. Java and Node descendant controls passed again; `network-guard-final.log.gz` retains them. The driver additionally requires an actual EPERM negative control for non-loopback TCP port 80 before it can load the application. Chrome is always launched through `chrome-loopback.sh` under the stricter existing loopback-only sandbox, with Playwright origin routing, blocked service workers and closed WebSockets. Final dry browser errors and non-origin attempts are zero.

**Concrete egress decision required from parent:** macOS `sandbox-exec` rejects a numeric remote destination with `host must be * or localhost in network address`. It cannot honestly be described as enforcing an Anthropic hostname/IP filter. The candidate `paid-provider.sb` therefore allows OS-level **outbound TCP 443 only** plus required loopback/Chrome IPC. Application native transport independently pins the exact HTTPS URL, POST, native model/token bounds, owner-supplied numeric address via `lookup`, TLS certificate verification and SNI `api.anthropic.com`; it performs no DNS lookup, proxy request, redirect follow or retry. Tests verify these HTTPS options and refusal controls with an explicitly synthetic socket implementation.

Attempting to apply the broader TCP443 profile inside the current loopback-only sandbox returned `sandbox_apply: Operation not permitted`; no egress was opened. The paid profile has **not** been run outside the denied parent or tested against Anthropic. This is not concealed as an OS destination-filtering pass. Parent must either explicitly accept this frozen-code, TCP443-plus-pinned-native-TLS layered restriction for the one reserved run, or supply a stronger concrete egress arrangement. The activation file requires the exact policy label `os-tcp443+pinned-native-tls+loopback-browser-v1` and recorded parent decision. Builds/codegen must never run under that profile. Native endpoint reachability/identity still requires the activated probe; no pre-paid validity request is authorized.

## Verification and retained failures

| Boundary | Observed result |
| --- | --- |
| Guarded immutable install | Pass from existing caches, no package/lock change. All install scripts disabled. |
| Guarded production build | **40/40 uncached tasks pass** for app/website dependency closures. First build process timed out during local Rust compilation; retained, then rerun under the same guard. No external acquisition fallback. |
| New preflight/transport/repair controls | **22/22 tests pass**: exact catalogue bound; named/missing/released/exhausted/underfunded/pending/unknown/token/total refusals; exclusive writer; three-rejection conservation; unattributed rejection; TLS/IP pin; raw byte preservation; redirect/429 one-dispatch behavior; wrong endpoint/model/output refuses. HTTPS transport tests are synthetic, not provider acceptance. |
| Entire application suite | **39 files / 309 tests pass** after the committed implementation; ordinary hermetic credential policy and shared inventory unchanged. |
| Built accounting | **13 outcomes pass**, six main-process synthetic starts/dispatches. Original production reservation, journal, unknown/restart and admission-independent usage behavior is preserved. |
| Built source/locator/evidence | Ten observations, 29 synthetic native SDK requests, zero paid calls. |
| New browser driver | Final explicit TEST process: **5 synthetic native SDK requests**, one actual browser read/result continuation, no rejected operation, no browser/listener/non-origin errors; TEST estimate US$0.003. Actual shared usage remains unchanged. |
| Type/lint/format | Typecheck and formatting pass; lint has zero errors and 18 existing warnings. `git diff --check` passes. |
| Real-mode no activation | Explicit `real` launch under deny-network refuses before application load/provider invocation. |
| Manifest | 3,310 exact file hashes verify after final tests. |

Two earlier dry controls requested a browser read before a current revision existed and received `Tool getLatestNetDefinition not found`. The first completed only a rejection, not the required browser crossing; the second timed out waiting for product busy-state settlement. Their logs and second control's raw requests/history-delivery evidence are retained in `dry-failed-unsettled/`. The corrected **dry-only** response sequence settles an explicitly TEST workpiece first; no product semantics, production guidance, frozen real-user scenario or checker was retuned. The final dry control asserts an actual `client-tool-result` signal and no tool output-error. It does not fabricate a positive mutation or explanation.

Local OpenAPI prerequisite: after guarded immutable installation, the authorized alpha JAR was checked at **27,103,489 bytes**, SHA-256 **`9718ff7844e89462c75dcd9b20a35136f6db257bfe1b874db1e3002e99de4609`**, then regular-copied into this worktree's ignored installation. Source/destination inodes were distinct (108379967 / 114522208), each link count 1; destination hash/size match. Source untouched, no hard/symlink, no download. Native-local-delivery's earlier acquisition incident remains disclosed by its original handoff; this later guarded copy does not retroactively erase it.

## Exact commands and activation handoff

Run from repository root. `E=libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/native-local-delivery`.

```sh
node "$E/verify-network-guard.mjs"
sandbox-exec -f "$E/deny-network.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 YARN_ENABLE_SCRIPTS=0 yarn install --immutable
sandbox-exec -f "$E/deny-network.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 CARGO_NET_OFFLINE=true VITE_BRUNCH_CHAT_ENDPOINT=/agents/chat yarn exec turbo run build --filter=@apps/brunch-agent --filter=@apps/petrinaut-website --force --concurrency=1
sandbox-exec -f "$E/deny-network.sb" yarn workspace @apps/brunch-agent test:unit
sandbox-exec -f "$E/deny-network.sb" yarn workspace @apps/brunch-agent lint:tsc
sandbox-exec -f "$E/deny-network.sb" yarn workspace @apps/brunch-agent lint:eslint
sandbox-exec -f "$E/deny-network.sb" env HASH_OTLP_ENDPOINT='' node --experimental-strip-types apps/brunch-agent/test/provider-accounting.integration.ts
sandbox-exec -f "$E/deny-network.sb" env HASH_OTLP_ENDPOINT='' yarn workspace @apps/brunch-agent test:workpiece-evidence
sandbox-exec -f "$E/deny-network.sb" node --experimental-strip-types apps/brunch-agent/src/evaluations/real-provider-a5.ts preflight /absolute/fresh/readiness-directory
sandbox-exec -f "$E/loopback-only.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 node --experimental-strip-types apps/brunch-agent/src/evaluations/real-provider-a5.ts dry /absolute/fresh/TEST-directory
```

Proposed runId: **`a5-real-provider-20260909-r1`**. The parent supplies an activation JSON, funds the existing shared ledger reservation (20 calls/US$15 maximum, perCall 4096/US$7), and exclusively delegates its writer. This packet does not write such a reservation. Required activation shape (placeholders are not executable authority):

```json
{
  "runId": "a5-real-provider-20260909-r1",
  "ledgerPath": "/OWNER/AUTHORITATIVE/usage-ledger.json",
  "manifestPath": "/Users/lunelson/.herdr/worktrees/hash/m7-real-provider/libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/real-provider-a5-readiness/manifest.json",
  "manifestSha256": "030ad3c7bc6886687d9b8537e28788d829c920f544d7c67bde2bf42dcef11c17",
  "providerIp": "OWNER-APPROVED-NUMERIC-ANTHROPIC-IP",
  "singleWriter": true,
  "egressPolicy": "os-tcp443+pinned-native-tls+loopback-browser-v1",
  "egressDecision": "OWNER RECORDED DECISION REQUIRED"
}
```

**Only after parent review/activation and concrete egress acceptance**, proposed exact process command, with the parent-provided activation path substituted:

```sh
sandbox-exec -f apps/brunch-agent/src/evaluations/real-provider-a5/paid-provider.sb env BRUNCH_CHAT_MODEL=claude-sonnet-4-6 HASH_OTLP_ENDPOINT='' YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 node --experimental-strip-types apps/brunch-agent/src/evaluations/real-provider-a5.ts real "$PWD/libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/a5-real-provider-20260909-r1" /OWNER/PROVIDED/activation.json
```

No build/install/codegen is included. Keep credentials in the existing provider configuration, never argv or activation JSON. If the profile, pinned IP/TLS, auth, model identity, ledger or repair budget fails, retain the actual outcome and stop; do not run unguarded, change model, silently retry, retune teaching or invent a settlement. Reopening a stopped run needs parent instructions, not a new independent allocation.

## Ownership and parent insertions

Only new evaluation driver/helpers, its focused test and this evidence directory changed. Product core/plugin/browser semantics, runtime patches, admission/accounting/registration, packages/lockfiles, MISSION, ordinary hermetic credential policy and actual ledgers are unchanged. No push.

No hermetic-inventory insertion is needed: the new test imports evaluation helpers, not substrate packages directly, and the unchanged architecture inventory passes. No shared script was edited. Optional parent-reviewed convenience script in the app package (root-relative runner): `"eval:a5": "cd ../.. && node --experimental-strip-types apps/brunch-agent/src/evaluations/real-provider-a5.ts"`. It must still be invoked within the matching external network guard; no implicit paid/default mode should be added.
