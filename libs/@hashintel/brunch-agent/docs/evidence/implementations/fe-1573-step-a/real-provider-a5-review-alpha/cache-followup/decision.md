# Raw input/cache cost evidence — activation remains withheld

> Historical result; raw contract-establishment payloads were retired in the [bounded reduction](../../landing-20260909/evidence-reduction.md). Original commands, manifests and payload links below describe the recorded run, not a maintained replay or a claim that those bytes remain local.

The focused re-review confirms the original missing-terminal-output and retention-callback corrections: 26 focused tests and a separate 13-control/15-dispatch built replay pass. These use actual native parsing, built registration, admission and RequestLedger with synthetic HTTPS/auth; they are not real TLS or provider calls. The correction/refreeze through worker `12e27bc2217d620654b5cc02832f35c784df9889` remains unactivated.

A further cache-cost counterexample is independently reproduced by both reviewer and parent. Initial native usage is input100/output1; terminal output20 adds `cache_creation_input_tokens:100` and `cache_creation:{ephemeral_1h_input_tokens:100,ephemeral_5m_input_tokens:0}`. The raw attester accepts. Pi updates `cacheWrite` but sets its one-hour subset only from the initial event, losing the later distinction. The actual ledger records input100/output20/cacheWrite100/cacheWrite1h0, settles **US$0.000975**, releases the US$7 hold and permits a second HTTPS dispatch. The frozen catalogue applied to the explicit one-hour information would yield **US$0.0012**. Numeric validity alone has not preserved the raw cost-bearing information.

The parent ran the reviewer's exact `bypass.integration.ts` unchanged under deny-network with fresh `TMPDIR=/tmp/m7-paid-cache-owner.YHw4uS`. The complete native bytes, first/second histories and ledger snapshots are retained here. This diagnostic's `passed:true` means execution completed, not that safety passed. No paid call, DNS lookup, credential probe or actual-ledger write occurred.

The same replay shows initial input100 replaced by terminal input0 without compensating cache fields, resulting in input0/output20 settlement and another dispatch. **Interpretation of that replacement is a separate protocol question.** Do not infer universal per-field monotonicity from the example or reject legitimate cache-partition updates. The confirmed cache-tier loss independently blocks readiness; unsupported/ambiguous input loss needs explicit treatment rather than silent settlement.

Authorize only a bounded correction to the existing raw attester and focused tests: reject cost-bearing input/cache changes or contradictions that the unchanged native parser cannot faithfully represent, before releasing bytes. Preserve legitimate initial one-hour/five-minute reporting, nullable/omitted delta fields and supported updates. Add the exact mounted negative requiring unknown, no actual cost, US$7 held and no next dispatch, plus legitimate cache positives. Do not repair bytes, synthesize normalized counters, substitute a settlement, change generic accounting/runtime ownership, enlarge the budget or retune teaching. Preserve v1–v4/failure packets and request another focused review before repinning a corrected instrument for activation.

The final v4 manifest candidate (`1de43141dfbe68902af2bf1d62f52bbdaf72c2f10e6277abc6c0fa759b62b243`, 3,312 files) and 313 passing app tests do not override this counterexample. No allocation, provider address, egress acceptance or sole-writer paid lease is granted. Actual shared usage remains **5 calls / US$0.09113535**. Normalized-only generic accounting remains explicitly unfixed outside this raw-gated instrument; real-provider, invoice, utility and mission acceptance remain unearned.
