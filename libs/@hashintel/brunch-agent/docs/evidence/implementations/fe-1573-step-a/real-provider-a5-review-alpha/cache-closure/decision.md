# Raw cache/input attestation — bounded review closure

> Historical result; raw contract-establishment payloads were retired in the [bounded reduction](../../landing-20260909/evidence-reduction.md). Original commands, manifests and payload links below describe the recorded run, not a maintained replay or a claim that those bytes remain local.

Independent re-review of implementation `008b576b6107175ee02d80ed91bd55ba6d79c0f0` at worker HEAD `862f6d72345d3313ff09c8f00d986e9577abe4d1` found **no remaining concrete blocker at this bounded seam**. The original exact tier-loss and aggregate-input-loss discriminator was rerun unchanged: both now retain one unknown request, no actual cost, US$7 held and no second HTTPS dispatch. Prior failing packets are retained, not retrospectively relabelled.

The review reran 26 focused tests, then independently extended the actual built registration/native SDK/pinned transport/RequestLedger probe to **27 controls / 37 synthetic dispatches**. It checked raw effective input/read/write/one-hour/output counts against actual native ledger usage and separately computed catalogue costs, including both completed rows after a permitted second request. Initial one-hour, five-minute, mixed cache, nullable deltas and supported redistribution settle correctly. An added supported write decrease passes, while writes below the retained one-hour subset refuse. The full report and exact checker/probe are retained here.

The gate uses comparison registers only. It refuses raw tier changes that the unchanged parser cannot preserve and contradictory partitions, without replacing bytes or counters or calculating an alternative settlement. The aggregate nondecrease rule is a conservative interpretation of the pinned SDK's cumulative, disjoint input categories, not universal scalar/vendor monotonicity. Compensated supported redistribution remains possible. If a provider later supplies aggregate retractions/provisional estimates or new billing fields, the instrument must refuse or return for an explicit contract decision; this review does not infer their meaning.

All five bounded source/runtime/type pins matched. Only the two intended correction files differ from the 3,312-entry v4 manifest; **v4 is not a final current instrument**. No generic normalized-only accounting repair, real TLS, provider acceptance, invoice verification or paid activation is established. The integrated product/driver must receive a new build/pin/preflight before the owner allocates the named run and accepts its concrete egress/sole-writer boundary.

All review execution was network-denied and used TEST auth/HTTPS events, not real credentials or provider connections. The attached artifact manifest preserves exact original bytes. Actual shared usage remains **5 calls / US$0.09113535**, with no active reservation. The integration owner accepts this bounded correction for integration; final freeze, allocation and mission acceptance remain separate gates.
