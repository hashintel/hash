# Final merged current-A5 instrument preflight

## Disposition

**Unpaid final freeze ready for parent review. No allocation, paid-profile activation, DNS/provider connection, credential-validity request or paid invocation occurred.** This is the current merged product at exact code/base commit **`3f86c1461c2654b80b5eca0e7b5e65580146a9e0`**, branch `ln/fe-1573-provider-proving`, in `/Users/lunelson/.herdr/worktrees/hash/m7-provider-proving`. The initial worktree was clean. No production/evaluation code, test, schema, guidance, fixture, package/lockfile, runtime patch, shared inventory/script, MISSION or real ledger was edited. The evidence-only commit containing this document is the handoff commit; obtain its exact identity with `git log -1 --format=%H -- <this-directory>`.

Read authorities: repository/application/Brunch/evaluation/Petrinaut AGENTS, the full current `MISSION.md`, relevant Git workflow and current core/plugin skills, the integrated driver and manifest/transport/credential/repair guards, and the requested integration/readiness/terminal/cache/parent cache-closure evidence. Historical v1–v4 manifests and failure packets remain untouched and are not current pins. One initial broad `find .. -name AGENTS.md` reconnaissance command timed out after ten seconds; scoped discovery completed. There was no failed install/build/probe or network-enabled retry in this final-freeze campaign. Expected rejected-submission traces from negative controls remain in the exact logs.

## Current immutable instrument

- **Final manifest:** `final-freeze/manifest.json`, SHA-256 **`8a47d3335bf5cd032b5f25efab5a53973d185e2f2c81ce1e691dc61fd4937381`**.
- **Final preflight:** `final-freeze/preflight.json`, emitted in the same existing driver invocation after all installation/build/test/browser commands. These are exact driver-generated bytes, not a historical manifest or a hand-built replacement.
- **3,344 file pins**, including source/tests, compiled app and website, core/plugin resources, canonical schemas/tools, scenario, maintained runtime patches and installed Flue/Pi/native SDK, lock wiring, Node, Chrome entrypoint and sandbox profiles. **106 app/website build files**, including `apps/brunch-agent/dist/client/`, are present and verified.
- `verify-final.mjs` invokes the existing `verifyManifest()`, verifies every pin and current catalogue, checks the 46-name canonical registry, and compares all 52 original/archived observation files and both unchanged cloned-ledger hashes. `final-verification.log` retains its pass. This is read-only evidence verification, not a replacement driver, model response or accounting mechanism.
- Exact native dry request bodies retain actual system/tool projections. `actual-dry-tool-guidance-projections.json` extracts these without changing them: nine initial tools, then eleven after TEST workpiece settlement, adding only `getLatestNetDefinition` and joined `addArc`. `brunch_mark_question` remains. All five requests select `claude-sonnet-4-6` with `max_tokens: 4096`. Full skill sources are pinned; this dry model did not activate every resource.

From repository root, the exact final preflight command was:

```sh
sandbox-exec -f libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/native-local-delivery/deny-network.sb env BRUNCH_CHAT_MODEL=claude-sonnet-4-6 HASH_OTLP_ENDPOINT='' YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 node --experimental-strip-types apps/brunch-agent/src/evaluations/real-provider-a5.ts preflight /Users/lunelson/.herdr/worktrees/hash/m7-provider-proving/libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/real-provider-a5-final-merged-preflight/final-freeze
```

No later command rebuilds or modifies a pinned instrument file. Evidence commits disable rewriting hooks to preserve raw JSON/log bytes; the complete product portfolio and explicit boundary checks have already run. Content identity, not the later evidence-only commit SHA, is the activation invariant.

## Fresh local evidence

| Boundary | Result and retained oracle |
| --- | --- |
| Process-tree network guard | Java and Node descendant controls passed for deny, commit and loopback profiles before installation. External attempts returned EPERM; only the loopback profile connected locally. `network-guard.log`. |
| Local installation | `yarn install --immutable` passed under deny-network with Yarn/Corepack network disabled and all install scripts disabled. Existing peer/deprecation warnings retained in `install.log`; no package/lock changes. No JS artifacts copied from another worktree. |
| Authorized local OpenAPI JAR | Source and target both regular files, link count 1, 27,103,489 bytes, SHA-256 `9718ff7844e89462c75dcd9b20a35136f6db257bfe1b874db1e3002e99de4609`. Source inode108379967, target115890012, same device/distinct inodes. Target was absent; exclusive-create copy; source unchanged. `authorized-jar-copy.log` and exact one-time evidence command `copy-authorized-jar.py`. No download or runtime editing. |
| Full serial portfolio | **63/63 uncached tasks, 1,669 tests, exit0**. Full `build test:unit lint:tsc lint:eslint` for the seven mission packages with `--force --concurrency=1 --continue=always`. App server **and client** and website built from current source; dependency codegen/Rust ran offline under process-tree denial. `portfolio.log` and `portfolio.exit`. |
| Unit breakdown | Core128, plugin76, binding20, transport45, app315, Petrinaut699, website386. Existing compiler/lint warnings remain; no lint errors. App lint reports20 warnings on this merged instrument. |
| Explicit native terminal probe | **25 controls / 34 synthetic HTTPS dispatches, pass** through the built ChatAgent, actual native SDK parser, pinned transport and production RequestLedger. Negative terminal/status/cache/input/retention controls keep unknown/US$7 hold and refuse another dispatch. Supported cache/redistribution positives settle native catalogue estimates. `terminal-25.log`, `terminal-summary.json`, and all raw bodies/history/ledger outcomes under `observations/TEST-a5-terminal-sJwsgN/`. This is not a real socket/TLS or provider call. |
| Actual Chrome dry driver | **Five synthetic native requests**, actual browser read and causal client-result continuation, no rejected operation, blocked origin or browser/listener error. Fresh original store at `/tmp/m7-final-merged.zsW7IK/dry5-chrome/conversation.db`. `dry5-chrome.log` and complete captured requests, histories, HTTP deliveries, document states, screenshots and TEST ledger under `observations/dry5-chrome/`. |
| Visible dry interpretation | Inspected `pane-1.png`: TEST/prepared labels, settled workpiece tool and checked latest-net card, explicit no-construction/no-explanation prose, current-workpiece/why surface honestly unqueried. The dry path does **not** mutate the A5 arc or answer why; this is not a canned A5 success. |
| Final credentials/catalogue | Existing `credentialsAvailable()` resolver returned **true**; only availability is retained. No auth material/source/environment output, validity/API request or paid call. `final-freeze/preflight.json`. |
| Final identity verification | All3,344 instrument pins, 46 canonical names, 52 original/archived files and both cloned-ledger hashes verify. `final-verification.log`. |

`commands.log` records exact executed verification commands and output roots; every command has a retained exit status. The originals remain at **`/tmp/m7-final-merged.zsW7IK`**, including both SQLite stores. Gzip copies preserve original bytes; `original-observation-pins.json` binds every source/copy. These are evidence copies, never imported state. The portfolio's own test lifecycle is unchanged; the explicitly retained original stores are the terminal and Chrome runs named above. No cleanup of another worktree, push or history rewrite occurred.

Toolchain observed: Node22.21.1, Yarn4.16.0, Corepack0.34.5, Cargo/rustc1.99.0-nightly, OpenJDK25.0.2, macOS26.6.2 (25G83). Exact versions are in `toolchain-versions.log`; actual Node and Chrome entrypoint hashes are in the final manifest.

## Model, holds and shared authority

Actual selected **catalogue**, not provider-reported live identity: `anthropic/claude-sonnet-4-6`, `anthropic-messages`, base `https://api.anthropic.com`, context1,000,000, native output maximum128,000. Catalogue rates per million: input3, output15, cache-read0.30, cache-write3.75 USD. The existing preflight includes one-hour writes at twice input rate:

```text
1,000,000 × max(2×3, 3.75, 0.30) / 1,000,000 = US$6
4,096 × 15 / 1,000,000 = US$0.06144
worst-case catalogue request bound = US$6.06144 ≤ US$7 hold
```

Proposed first allocation remains **at most20 underlying calls / US$15**, within the shared **200 calls / US$100**. **No allocation is active.** Each next request needs a full unencumbered US$7 hold; the call ceiling does not promise twenty requests. Preparation, continuation, compaction, failures and retries count at the underlying boundary. Only the built ChatAgent participates; no persona, evaluator or other model is introduced. The three-rejection limit covers all46 canonical registry names, including unavailable ones, without mounting them. Unknown usage or `journalPending` is a hard stop, never guessed settlement or self-top-up.

Actual shared totals remain the owner's **5 calls / US$0.09113535**, no outstanding reservation; this lane made zero paid calls. The cloned usage ledger and attempt journal are byte-identical to base, hashes `d5041ddc422a1889ce013b002ce6ed0250a2992ae4ab19501188d51a9c6eedf4` and `da55752de0b939ac817f7679ab1dadd9ff058fdcaa2e602063e04d188a5d0833`. `cloned-ledger-unchanged.json` is a hash observation, not a second paid authority. Dry TEST spend isUS$0.003, not added to real shared totals. Catalogue estimates are not invoices.

## Parent review and pending activation gates

Please independently verify the code/evidence commit, current manifest and bounds before activating anything. Required parent-owned decisions and artifacts:

1. Accept this exact final manifest path/hash and the current synthetic-user/prepared-root-arc scope. Do not activate against v1–v4 or infer broader construction/genuine Vestera admission.
2. Supply the **authoritative absolute ALPHA ledger path, not this clone**, allocate the named run inside the shared envelope, and explicitly delegate the exclusive single writer with no competing ledger activity. The driver’s exclusive-create lock supplements rather than replaces that coordination.
3. Supply the approved numeric provider IP and concrete recorded egress decision. The candidate OS profile allows outboundTCP443, **not destination filtering**; native transport layers exact HTTPS endpointPOST, pinned numeric lookup, verifiedTLS/SNI, no proxy/DNS/redirect/retry, with nested loopback-only credential-free Chrome.
4. At named activation, independently verify the standalone paid profile and real pinned TLS boundary. Synthetic HTTPS request-option tests and the unpaid dry run do not establish either. The worker has not launched the standalone profile or probed provider reachability. Unsupported policy/TLS must stop, never fall back.
5. Supply activation JSON with `runId`, `ledgerPath`, `manifestPath`, `manifestSha256`, `providerIp`, `singleWriter: true`, `egressPolicy: "os-tcp443+pinned-native-tls+loopback-browser-v1"`, and the concrete `egressDecision`; then explicitly instruct the bounded real invocation. No activation JSON is fabricated here.

**Proposed runId:** `a5-real-provider-20260909-r1`.

**Proposed future output path (not created or run):** `/Users/lunelson/.herdr/worktrees/hash/m7-provider-proving/libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/a5-real-provider-20260909-r1`.

Only after those named parent instructions, the proposed real command is:

```sh
sandbox-exec -f apps/brunch-agent/src/evaluations/real-provider-a5/paid-provider.sb env BRUNCH_CHAT_MODEL=claude-sonnet-4-6 HASH_OTLP_ENDPOINT='' YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 node --experimental-strip-types apps/brunch-agent/src/evaluations/real-provider-a5.ts real /Users/lunelson/.herdr/worktrees/hash/m7-provider-proving/libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/a5-real-provider-20260909-r1 /PARENT/PROVIDED/activation.json
```

Do not run build/install/codegen under paid egress. Preserve the native real model/schema/raw responses, actual canonical UI/results/why/refusals and exact accounting, including failures. A refusal, missing mutation/why, unsupported evidence or stop remains an observation, not success. No teaching/pane/scenario retune, autonomous repair, broader construction or genuine Vestera claim, provider/utility/mission acceptance, or StepB follows from this preflight or eventual mere completion.
