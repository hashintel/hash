# Bounded A5 provider driver — integrated, not activated

> Historical result; raw contract-establishment payloads were retired in the [bounded reduction](../landing-20260909/evidence-reduction.md). Original commands, manifests and payload links below describe the recorded run, not a maintained replay or a claim that those bytes remain local.

## Disposition

Accept the reviewed driver, terminal-usage/cleanup correction and cache/input attestation for the bounded synthetic-user A5 instrument. They are integrated with the current construction/renderer and passage/retention code. **No paid run, allocation, provider egress or final instrument freeze is claimed here.** The next proving worktree must build/check/pin this merged product before the parent activates its sole shared reservation.

The parent verified all five bounded correction pins and all 3,312 v4 entries except the two explicitly corrected source files. Prior v1–v4 manifests, red controls and independent review packets remain retained at their original commits; none was silently promoted to a current final manifest. The [focused cache closure](../real-provider-a5-review-alpha/cache-closure/decision.md) states the raw-versus-normalized and protocol limits.

| Worker commit | Alpha commit |
| --- | --- |
| `efeb4921ca` | `18ffa4e0ca` |
| `9afe0bcb81` | `0a34f59c95` |
| `f93d62a540` | `bf8adc82c7` |
| `7b167f5328` | `c232ad20d7` |
| `2610b4d3f3` | `c0cb313b76` |
| `7d86df283b` | `216e2860b2` |
| `9cef631081` | `7a27bb0954` |
| `12e27bc221` | `20bd522561` |
| `008b576b61` | `727cff2b21` |
| `862f6d7234` | `923394c119` |

The only integration conflict was two independently approved importer entries at the same position in `test/architecture/boundaries.integration.ts`. Both the passage-policy and terminal-accounting entries were preserved, all prior entries remained, and the architecture wrapper passed before continuing the cherry-pick. No source, assertion or credential policy was dropped.

## Integrated checks and one final guard closure

At `923394c119`, forced serial combined build/unit/type/lint passed **63/63 uncached tasks / 1,668 tests**. The explicit built terminal probe passed **25 controls / 34 synthetic HTTPS dispatches**. The merged actual-Chrome dry driver passed its five native synthetic responses and real read/result path. This dry driver still does not construct or explain the A5 arc; it remains a transport/control preflight, not canned A5 success.

Before final freeze, the parent found a small caller-configuration omission: the repair guard received the six-name legacy construction catalogue. Three rejected `updateArcWeight` calls—a known canonical operation absent from that list—left its rejected map empty and `check()` did not refuse. The direct discriminator and unit red retain this actual assertion failure, not a loading error.

Parent commit **`4d5a5ef672`** makes `repairBudget()` own the canonical registry from `petrinautAiTools`, removing the caller-selected subset. The actual driver uses that zero-argument factory. It counts all **46** canonical names, including unavailable ones, without mounting/admitting any new tool or changing the rejection parser, deduplication, three-attempt threshold, payloads or generation policy. The new test checks each actual canonical name allows the first three attempts and refuses after the third rejection.

After that small correction, the full app suite passes **315 tests**, types/lint pass with no errors, and a fresh actual-Chrome dry run passes again. Independent focused review passes **27/27** tests and directly repeats the previously omitted name: three rejected IDs retained, refusal true. The [review](repair-review/review.md.gz) accepts only this counting/wiring closure, not a model-generated paid loop or class admission. The 1,668 whole-portfolio run predates this one additional test; it is not relabelled as a new 1,669-test portfolio.

## Boundaries that remain load-bearing

- Real mode uses the existing built ChatAgent, registration, admission and browser execution; no faux fallback or repaired assistant response is available there. The exact pre-authored user scenario remains synthetic, not Vestera or genuine testimony.
- Raw model identity and complete terminal usage must be attested before successful response bytes reach the unchanged native parser. Unsupported late one-hour changes and contradictory/unexplained input/cache evidence refuse; legitimate initial tiers, omitted/null deltas and representable redistribution pass. The registers compare only; they do not replace native counters or settle the ledger.
- The generic normalized-only accounting path remains insufficient to establish raw usage presence by itself. Unknown, partial, failed, pending-journal or over-bound usage remains a stop, not a free request or invented settlement. Catalogue estimates are not invoices.
- The proposed `anthropic/claude-sonnet-4-6` run remains at most **20 calls / US$15**, with **4,096 output tokens / US$7 per-call hold**, inside the existing total 200-call/US$100 envelope. The current catalogue worst case is US$6.06144 per request. No budget was multiplied by worker count.
- OS TCP443 plus exact native endpoint/numeric-IP/TLS and nested loopback-only Chrome is a proposed layered trusted-driver boundary, **not OS destination filtering**. Standalone policy/remote TLS, final source/build pins, owner-approved IP, explicit sole writer and actual ledger allocation still gate execution.

## Evidence and commands

`artifact-manifest.json` hashes **141** retained integration, guard-red/green and independent-review artifacts: exact command logs, raw native first/second outcomes, ledger observations, dry HTTP/history/results and reviews. Gzip preserves source bytes. Original live stores and remaining local screenshots stay under `/tmp/m7-provider-integration.78Vp1L` and `/tmp/m7-repair-catalogue-owner.ltm6mQ`; no saved projection becomes state authority.

The combined portfolio used the same seven-package forced serial command as [construction integration](../construction-progression-integration-alpha/integration.md), under a freshly verified deny-network profile. Focused commands from repository root used:

```sh
sandbox-exec -f "$E/deny-network.sb" env TMPDIR=/tmp/m7-provider-integration.78Vp1L HASH_OTLP_ENDPOINT='' node --experimental-strip-types apps/brunch-agent/test/real-provider-a5-terminal.integration.ts
sandbox-exec -f "$E/loopback-only.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 node --experimental-strip-types apps/brunch-agent/src/evaluations/real-provider-a5.ts dry /tmp/m7-provider-integration.78Vp1L/dry
sandbox-exec -f "$E/deny-network.sb" yarn workspace @apps/brunch-agent test:unit
sandbox-exec -f "$E/deny-network.sb" yarn workspace @apps/brunch-agent lint:tsc
sandbox-exec -f "$E/deny-network.sb" yarn workspace @apps/brunch-agent lint:eslint
```

Here `E=libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/native-local-delivery`. The post-counting-correction dry run used the fresh directory `/tmp/m7-repair-catalogue-owner.ltm6mQ/dry`; do not overwrite these paths. All owned test processes exited, and the temporary integration pane was closed.

Actual shared ledger hashes remain `d5041ddc422a1889ce013b002ce6ed0250a2992ae4ab19501188d51a9c6eedf4` (usage) and `da55752de0b939ac817f7679ab1dadd9ff058fdcaa2e602063e04d188a5d0833` (attempt journal): **5 calls / US$0.09113535**, no reservation. Nothing was pushed. Final freeze/paid execution, genuine construction, semantic utility and Step A/B acceptance remain unearned.
