# Final sibling-browser current-A5 instrument freeze

## Disposition and immutable identity

**Unpaid final freeze ready for parent verification. Pause for named parent activation; no allocation or paid permission is inferred.** The fresh worktree `/Users/lunelson/.herdr/worktrees/hash/m7-provider-execution`, branch `ln/fe-1573-provider-execution`, was clean at exact source/base commit **`f2b9bfd040ec989f06c7b72a8973f88aab3b42ca`**. Every tracked source, test, model/guidance/scenario, dependency/runtime/profile, MISSION and cloned ledger file remains byte-identical to that base. This commit adds only this evidence directory. The evidence commit identity is available from `git log -1 --format=%H -- <this-directory>`.

- **Exact manifest path:** `/Users/lunelson/.herdr/worktrees/hash/m7-provider-execution/libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/real-provider-a5-final-sibling-preflight/final-freeze/manifest.json`.
- **Manifest SHA-256:** `1c71ed054d89a7a0183df5b8877faba042982638a58c6696151fc119df439586`.
- **Generated preflight:** `final-freeze/preflight.json`; actual **`credentialsAvailable: true`**. Validity was not probed. No credential value/source/environment dump, auth refresh or API request occurred.
- **3,916 file pins**, including **106 app/website build files** with app server and client, and **563 Playwright library files**. The manifest binds current source/tests/builds, actual guidance/tool/scenario, installed native SDK/Flue/Pi patches, lock wiring, Node/Chrome/sandbox, and new launcher/owner/session/profile identities.
- Existing `verifyManifest()` passed, the full generated object matched a fresh in-memory `instrumentManifest()`, and an independent recursive filesystem enumeration plus SHA-256 sweep matched every path and byte. Current model catalogue and all **46 canonical operation names** were checked afterward. `observations/final-verification.log.gz` is the oracle.

This is the sole final freeze produced here, after all install/build/test/browser commands. No pinned file was subsequently rebuilt, formatted or edited; raw manifest/preflight bytes are preserved. Earlier v1–v4, the 3,344-pin `8a47d333…7381` instrument and r1 startup allocation remain historical, not this instrument. No protected-code defect was found or repaired in this lane.

Read scope: applicable repository/Brunch/evaluation/app/Petrinaut AGENTS, Git workflow skill and guidance, the full current `MISSION.md` as sole execution authority, current elicitation/SDCPN skills, and all five specifically requested sibling/shutdown/provider/startup evidence files. Current driver, launcher, session/owner, manifest/preflight and lifecycle/fault source were inspected. Two initial skill-path reads omitted `src/` and returned ENOENT; scoped discovery found and read the actual files. No implementation or test failure was retried or hidden.

## Fresh verification

| Boundary | Result and exact retained oracle |
| --- | --- |
| Network guard | Java/Node descendant deny, commit and loopback controls passed before installation. External attempts returned EPERM; local connection succeeded only under loopback. `network-guard.log.gz`. |
| Immutable cache-only installation | `yarn install --immutable --immutable-cache`, scripts disabled, Yarn/Corepack network disabled, under OS deny-network; exit0. Existing peer/deprecation/disabled-script warnings remain in `install.log.gz`. No dependency/lock change. |
| Authorized OpenAPI prerequisite | Only the already-local ALPHA 6.6.0 JAR was regular-copied after verification: **27,103,489 bytes**, SHA-256 `9718ff7844e89462c75dcd9b20a35136f6db257bfe1b874db1e3002e99de4609`, link count1, source inode108379967 / destination116910249, distinct inodes, target absent, source unchanged. `authorized-jar-copy.log.gz`. No download, overwrite, link or copied JS/build artifact. |
| Full current-source portfolio | **66/66 tasks successful, zero cache hits, 3,254 tests**; eight-package forced serial `build test:unit lint:tsc lint:eslint`, including complete app/server/client, website and core builds. `portfolio.log.gz`, exit0. |
| Unit breakdown | Petrinaut-core1554, Brunch-core128, plugin85, binding20, transport45, app337, Petrinaut699, website386. Existing compiler/lint warnings remain; no lint errors. |
| Explicit native terminal/cache attestation | **25 controls / 34 synthetic dispatches / zero paid calls**, pass through the built application/native parser/accounting path. Raw missing/contradictory terminal/cache/input/cleanup controls retain their negative accounting meaning. `terminal-25.log.gz`, `TEST-a5-terminal-d4HRRr/`. |
| Actual new sibling dry5 | **Five synthetic native requests**, real Chrome read/result continuation, no rejected operation, blocked origin or browser/listener error. `dry5/driver-result.json.gz`, native request/history/HTTP evidence and screenshots. |
| Five actual lifecycle controls | Foreign execution, attach refusal, lease expiry, owner interrupt and real-launcher interrupt all passed with **zero native dispatches** and known process/profile/endpoint cleanup. `lifecycle/result.json.gz`. |
| Four actual shutdown controls | Positive and negative assertions all pass. Failed/hung public kill is explicitly **not production cleanup success**; details below. `shutdown/result.json.gz`, each `production-observation.json.gz` and `test-cleanup.json.gz`. |
| Post-run resource audit | **26 known PIDs gone, 10 known profiles removed, 10 readiness paths removed**, normal binding removed. Ten native/history/HTTP artifacts contain no random library endpoint capability pattern. `resource-audit.json.gz`. No unrelated PID was killed. |
| Freeze and protected-base verification | All3,916 pins, exact fresh path enumeration/browser identity/catalogue, and unchanged tracked files/cloned ledger bytes pass. `final-verification.log.gz`, `protected-base-check.log.gz`. |

The dry screenshot `observations/dry5/pane-1.png` was inspected: prepared/TEST labels remain visible, workpiece settlement and checked-net card are shown, the current-workpiece/why pane remains honestly unqueried, and the synthetic assistant explicitly claims **no construction or explanation**. Dry5 did not add the arc or answer why. Nine initial tools become eleven after TEST settlement, adding `getLatestNetDefinition` and joined `addArc`; `brunch_mark_question` remains. Every raw request selected `claude-sonnet-4-6` and4096 output tokens. No new-node/typed-state scenario or genuine Vestera admission is inferred.

## Actual cleanup, identity and limits

Normal dry identity: run `TEST-a5-driver`, execution **`7d1df147-dae6-45f4-a9b8-5028104aaa2f`**, launcher55063, owner55064, driver55164, Chrome55067. Both children exit0; public close completes, public kill is not needed, owner/driver/Chrome are gone, profile removed, endpoint ECONNREFUSED, readiness and binding removed. Installed Playwright is1.58.2. Full private run/library/profile identity remains in the manifest and lifecycle evidence; no endpoint capability is placed in the page/model/live console.

| Fault | Observed close → real public kill | Production result before any TEST rescue | Separate TEST result |
| --- | --- | --- | --- |
| Close rejects | **0.561ms**; owner55988 / Chrome56006 | Kill completes, cleanupComplete true; processes/profile gone, endpoint ECONNREFUSED | Rescue **not used** |
| Close hangs | **5002.546ms**; owner57358 / Chrome57847 | Close times out, real kill completes, cleanupComplete true; processes/profile gone, endpoint ECONNREFUSED | Rescue **not used** |
| Kill rejects | **0.228ms**; owner95931 / Chrome99278 | Both rejections retained; cleanupComplete **false**; owner/Chrome/profile still present and endpoint connected | Saved-public-method TEST rescue afterward; known resources verified gone |
| Kill hangs | **0.222ms**; owner1407 / Chrome1414 | Kill timeout retained; cleanupComplete **false**; owner/Chrome/profile still present and endpoint connected | Saved-public-method TEST rescue afterward; known resources verified gone |

Delays are monotonic wrapper observations, not a performance claim. Aggregate fault exit0 means these positive/negative assertions held, not that all production cleanups succeeded. The owner uses separately bounded public close and kill attempts; it does not promise unconditional termination. A real failed/hung kill remains an incomplete-cleanup stop requiring owner recovery before further invocation/allocation. No universal PID/descendant, power-loss, hostile-same-UID or blocked-event-loop guarantee follows. TEST fixture metadata and original stores are retained as inert evidence, not live endpoint authority or reconstructed state. Owner/orchestration checks do not settle or clear unknown accounting.

All browser/SDK/app/socket execution used separate loopback-only roots; ordinary launcher/orchestration imported no socket/SDK/application code. Build/unit/native-terminal work stayed under deny-network. No paid profile, provider hostname resolution, pinned TLS, HTTP/provider or auth-validity probe occurred during preparation. Browser background denial is not a claim of zero attempted background networking.

## Budget and parent-owned activation gates

Exact selected catalogue: **`anthropic/claude-sonnet-4-6`**, `anthropic-messages`, base `https://api.anthropic.com`. Context1,000,000; rates per million input3/output15/cache-read0.30/cache-write3.75 USD. The checked one-hour-sensitive input maximum is6, yielding **US$6.06144 ≤ US$7 per-call hold**, with **4,096 output tokens**. This is catalogue estimation, not invoice fidelity or live provider acceptance.

Proposed new runId: **`a5-real-provider-20260909-r2`**. The first actual run must remain the same pre-authored synthetic-user prepared-root-arc current-A5 scenario. It is not new-node/typed-state construction or genuine Vestera. No persona/evaluator/model fallback or teaching/scenario retuning is authorized.

Ceilings remain **at most20 combined underlying calls / US$15** for the named run inside the existing shared200-call/US$100 envelope. The next request always needs a full unencumbered US$7 hold; twenty calls are not promised. Preparation, continuation, compaction, failure and retries count. All46 canonical names—including unavailable ones—remain subject to the three-rejection stop without expanded mounting. Unknown/partial usage, pending journal, budget exhaustion or failed cleanup stops; no guessed reconciliation, silent retry, top-up or unreserved call.

Parent-reported actual shared spend remains **5 calls / US$0.09113535**, **no active allocation**. This lane made zero paid calls. The dry TEST ledger's five calls/US$0.003 are separate synthetic observations, never written into shared spend. Neither ALPHA nor this clone's real ledger/journal was allocated, written or reconciled. Cloned files were only byte-compared to base and hashed. The original r1 startup remains released unused: macOS rejected paid→narrower-loopback nesting before provider contact; no DNS/TLS/provider call occurred there.

Required parent gates, in order:

1. Independently verify this code/evidence commit, exact manifest path/SHA, fresh enumeration and catalogue, sibling topology and synthetic scope. Do not reuse historical activation/pins.
2. Allocate the named run in the **one authoritative ALPHA ledger, never this clone**, and explicitly delegate one exclusive writer. Supply its canonical absolute ledger path; no competing ledger activity.
3. Perform the actual **paid-controller → separately loopback-guarded owner** attachment checks and pinned native TLS checks under that named allocation; supply approved numeric IP and concrete recorded egress decision. OS TCP443 is port restriction, **not destination filtering**; exact native endpoint/IP/TLS and stricter browser roots remain separate layers.
4. Supply all existing model/manifest/ledger/IP/egress/singleWriter fields plus **`browserTopology: "sibling-playwright-v1"`** and a **fresh UUID-v4 `executionId`**. Preserve `egressPolicy: "os-tcp443+pinned-native-tls+loopback-browser-v1"` as the driver's existing accepted literal; the browser placement is now sibling, not nested.
5. **Explicitly instruct the real launcher invocation** from ordinary orchestration. Do not nest the launcher under deny-only or paid policy; its children receive their own profiles. No real command or activation JSON is produced/run here.

Credential presence, earlier allocations, this green packet or later mere completion are not paid permission, provider-class/genuine/utility acceptance, Lu's Step A owner gate or Step B authorization. The independent typed-state worker remains out of scope. Pause here for named parent activation.

## Evidence retention

`commands.md` records exact primary commands and inline audit behavior. **219 original files** are retained losslessly under `observations/`, with original path/size/hash/mode and retained hash in `original-observation-pins.json`. This includes complete logs, raw native bodies/history/ledger observations, screenshots, original-store evidence copies and private lifecycle/failure/rescue metadata. Originals remain under **`/tmp/m7-sibling-freeze.cmMvyS`**. Archived stores are evidence only, never an import/restore route. `artifact-manifest.json` binds this packet, including exact generated freeze bytes. No rewriting hook is allowed to touch raw evidence. No push, history rewrite, source change or other-worktree cleanup occurred.
