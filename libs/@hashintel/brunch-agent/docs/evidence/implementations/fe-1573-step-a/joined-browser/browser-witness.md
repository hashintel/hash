# Actual local-browser witness — joined root arc

## Instrument and claim

Oracle: **“correlates the real browser transition record and resumes without reapplying”** in `apps/brunch-agent/test/transition-records.integration.ts`, explicitly run by `yarn workspace @apps/brunch-agent test:browser-tracer`. Final retained execution: `final-run-4`; implementation `1067dedee4a31a2b0935166eefcc5a961f00ec08`. The positive record is produced by the mounted browser recorder, not written by the test runner or a headless Petrinaut executor.

A real isolated local Chrome instance loads the built website. The runner serves those exact static artifacts and forwards `/agents/chat/:instanceId` to the existing built application's `application.fetch`, preserving production registration, ownership middleware, Flue SSE, the website's real transport, the real editor panel and its synchronous canonical mutation callback. The one listener binds `127.0.0.1` on an ephemeral port. No ownership/origin policy is weakened. The only substituted model component is the underlying faux provider factory below A2's unchanged admission decorator. Non-origin browser requests and external server fetches fail closed. Final observed external-request attempts: zero.

The observer reads canonical browser local-storage snapshots independently of the returned transition record and compares them with the recorder's direct handle observations. The resulting correspondence proves one local structural mutation, not scenario meaning, provider fidelity, cross-tab atomicity, or useful explanatory basis. This is not a genuine interview and uses no microphone, Voice session or audio provider.

## Observed path

1. The browser opens the ordinary existing local-storage-demo route, dismisses the normal first-run tour, sees both fixture links, and clicks **Open the prepared root-arc mechanical tracer** without a forced click or injected navigation. `selector.png` retains the visible clicked selector.
2. The route creates its own prepared document incarnation and conversation, distinct from the fixed legacy fixture. The original requested base is persisted with that document and included in immutable initial data and the existing prepared dispatch. `preparation-request.json`, `http-deliveries.json` and `initial-storage.json` retain this binding.
3. Through the real chat composer, the synthetic model invokes core `update_workpiece` as `m7-browser-revision`. The successful canonical history result settles the full labelled prepared Markdown, its SHA-256 and display ordinal. A later request, not a sibling tool call, cites that settled revision.
4. An unknown-revision control invokes `addArc` as `m7-browser-unknown-revision`. The real server rejects it. The browser shows the matching red tool error, has no client-result delivery for that call, and its canonical definition remains equal to the independently retained pre definition. The transport never exposes executable input for that rejected call.
5. The positive turn calls the existing `getLatestNetDefinition` in the browser as `m7-browser-read`. Its canonical result matches the retained pre definition and the originally requested hash. The next client-result step contains only that read result.
6. The model then issues `m7-browser-arc`, with its settled basis envelope and the model's numeric-string weight. The plugin normalizes before the structural carrier/canonical validation. History retains the original arguments and envelope; the execution projection strips the envelope and passes the canonical numeric weight.
7. The mounted A3 recorder reads the bound handle synchronously before and after the real Petrinaut mutation. It records one expected root input-arc creation and no residual effects. The next client-result step contains only the correlated arc result, with `metadata.transitionRecord`. The app verifies that record against the issued call and binding before the one final continuation.
8. The runner repeats the exact actual client-result delivery using the same delivery key. There is no extra synthetic model request. A real browser reload leaves the canonical post definition unchanged and hydrates no executable arc input. The reopened panel shows the settled revision, refused unknown-citation attempt, live read, successful arc and final continuation; `browser.png` retains this inspected state.
9. The runner submits a deliberately contradictory second delivery under a distinct key. This **negative control is test-authored**, derived from the actual retained observation; it is not a second observed browser no-op or mutation. It claims an internally valid conflicting no-op alongside the first applied attempt while retaining a successful canonical output. The receiving hook fails, no model request occurs, both deliveries remain in canonical history, and the result projection becomes error/unknown rather than success. The actual browser definition remains equal to the post definition.

Eight synthetic model requests total: one preparation acknowledgement; two workpiece-settlement requests; two unknown-citation/refusal requests; then read, arc and final continuation. Zero real provider requests, zero spend, zero duplicate/conflicting continuation requests, zero browser JavaScript errors, zero listener errors, and zero attempted external browser requests. The conflict's expected failed-submission stack is retained in the command log and is not a test failure.

## Exact canonical observation

| Field | Value |
| --- | --- |
| Conversation | `prepared-root-arc:513146c0-27f7-4266-8259-062e241a2fd9` |
| Document | `mission-6-crew-reservation-document-v1:root-arc` |
| Incarnation | `513146c0-27f7-4266-8259-062e241a2fd9` |
| Revision call | `m7-browser-revision` |
| Mutation call | `m7-browser-arc` |
| Requested base / independently observed pre SHA-256 | `a3eeb14f9e84880ce3cbabca5201a05822c17bdd8ad612abaa730b97a92d56b3` |
| Independently observed post SHA-256 | `3c47961d02296c00131644d1aea0dac16a017f470a66aea919fcf324a2bc9e37` |
| Created path | `/transitions/0/inputArcs/1` |
| Created value | `{ "type": "standard", "placeId": "dispatch-crew-available", "weight": 1 }` |
| Updated / deleted / derived effects | Empty |
| Positive record outcome | `applied` |

The file-format byte convention is SHA-256 over UTF-8 `JSON.stringify(definition)`, not sorted-key equality. `final-run-4/canonical-pre.browser.json`, `canonical-post.browser.json`, `transition-records.json`, `history.json`, `refused-history.json` and `conflicting-history.json` retain the independently checkable content. `requests.json.gz` retains actual synthetic model contexts, including tool schemas and per-step signals. `final-source-and-build-manifest.json` pins the exact source, guidance, runtime versions and built artifacts used.

## Render inspection and limits

Actual inspection caught a selector hidden beneath the editor header; the runner did not force the click. It now sits visibly over the canvas, and the final selector click passes. The tracer banner was narrowed so its claim boundary remains visible beside the opened assistant. The final screenshots and DOM assertions show the new selector, preparation status, settled workpiece tool result, unknown-citation error, canonical read, arc result and continuation. No existing published guide screenshot was changed, and no current-workpiece/why pane was added.

The original legacy fixture remains available with its fixed conversation and manifest protocol. Its unit/mounted regressions passed; this witness does not relabel it or claim a new legacy multi-tab/Voice run. The scripted browser controls do not prove superseded-citation override in a separate browser correction, arbitrary hand-edit reconciliation, crash recovery, compaction retention, source authorization/relevance, simulation behavior, provider-root fidelity, or genuine construction. Those remain explicit owner-held boundaries. No Step A acceptance or Step B authorization is asserted.
