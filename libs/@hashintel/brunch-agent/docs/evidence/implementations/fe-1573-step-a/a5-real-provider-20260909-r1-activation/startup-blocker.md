# Startup stopped before provider contact

The parent applied the frozen `paid-provider.sb` as a standalone profile. Its Node process completed the existing direct external-port80 EPERM control and an inherited child port80 EPERM control. Applying the stricter `loopback-only.sb` beneath that paid process failed **before its child program started**: `sandbox-exec: sandbox_apply: Operation not permitted` (subprocess exit71).

A separate no-network `/usr/bin/true` composition table confirms the distinction: paid→paid exits0, loopback→loopback exits0, paid→loopback exits71. Thus prior dry evidence under identical loopback ancestry does not establish the paid-parent/narrower-browser topology. This is not an Anthropic failure or permission to let Chrome inherit provider-capable egress.

No DNS resolution, TLS handshake, HTTP/provider request or model invocation occurred. Parent comparison with the pre-allocation ledger confirms all original five call rows and totals are unchanged; no per-call hold or driver writer lock exists. The allocation was **released unused at 2026-09-09T02:36:38Z**, retaining all allocation/freeze/failure evidence. Actual shared usage remains **5 calls / US$0.09113535**. The old activation shape must not launch.

The next bounded question is whether the chosen browser library can keep browser/control processes under a separate loopback-only OS root and let the driver attach through a local browser-control endpoint, rather than attempting forbidden sandbox nesting. Investigate that locally under ordinary network-denied/loopback guards; do not change the frozen driver, weaken Chrome's OS denial or create a provider proxy/new production service. A concrete proven topology and owner-approved bounded adapter change would require a new instrument freeze and named allocation before provider contact. No unsupported automatic fallback is authorized.

Retained files include the exact `profile-check.mjs`, empty stdout, complete stderr/exit status and `profile-composition.json`. The original probe prints its aggregate JSON only after all checks; its failure therefore leaves stdout empty, not a successful profile report. Initial policy/profile observations remain separate from real TLS and provider acceptance, neither of which was reached.
