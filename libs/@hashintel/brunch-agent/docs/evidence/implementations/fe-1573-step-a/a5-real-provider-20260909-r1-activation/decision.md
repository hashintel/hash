# Named current-A5 real-provider allocation and egress decision

## Owner decision before connection checks

Under Lu's existing AFK paid-work delegation, the integration owner accepts the final merged instrument and allocates **`a5-real-provider-20260909-r1`**, at most **20 underlying calls / US$15**, within the existing **200 calls / US$100** total envelope. The sole authorized participant is the built ChatAgent using **`anthropic/claude-sonnet-4-6`**, with **4,096 output tokens and a US$7 hold per request**. The frozen catalogue bound is **US$6.06144**; a 20-call ceiling is not a promise that every call can launch without enough remaining hold.

Accepted code: `3f86c1461c2654b80b5eca0e7b5e65580146a9e0`. Worker evidence: `34a90935e4ee646a049d8aad6dcf49c866fa9150`, integrated as `03b839415d`. Final manifest SHA-256: **`8a47d3335bf5cd032b5f25efab5a53973d185e2f2c81ce1e691dc61fd4937381`**, at `real-provider-a5-final-merged-preflight/final-freeze/manifest.json` in the frozen provider-proving worktree. Parent verification independently checks all **3,344 pins**, equality with a fresh in-memory enumeration and catalogue, **52 original/archived observation pairs**, the evidence-only write set and clean worker HEAD. No credential-value/validity or provider request was used for this acceptance.

This is the unchanged **synthetic-user/prepared-root-arc current-A5** scenario, not genuine Vestera, broader root-class admission, a teaching/pane retune, semantic utility or Step A/B acceptance. Raw native responses and real browser outcomes determine the result; refusals or missing construction/why are not success.

## Concrete network policy accepted for this allocation only

Accept **`os-tcp443+pinned-native-tls+loopback-browser-v1`** for this single frozen run. The OS sandbox permits outbound TCP443 generally plus required loopback/Chrome IPC; it is **not an Anthropic destination filter** and does not contain arbitrary parent-process HTTPS code. The additional trusted frozen-code boundary requires exact `https://api.anthropic.com/v1/messages` POST, approved numeric lookup, verified TLS/SNI for `api.anthropic.com`, no proxy/DNS/redirect/retry in the native transport, and pre-persisted accounting before dispatch. Isolated Chrome receives no provider environment and runs under the stricter loopback-only profile and origin routing.

Only the following parent-owned preparation is authorized before model invocation: standalone paid-profile validation; negative port/descendant and stricter nested-profile checks; resolution of the single public provider hostname to select an address; and a credential-free pinned TLS handshake to that address. No HTTP request, auth-validity probe, model invocation, install, build or codegen is included in these checks. Resolver and TLS observations must be retained honestly; no OS hostname/DNS filtering claim is inferred. Failure stops without unguarded fallback or model downgrade.

The actual paid invocation remains gated on successful connection checks, an explicit approved numeric IP and activation JSON, and a subsequent parent instruction to the named worker. Allocation alone is not a launch command.

## Accounting and exclusive writer

The only live paid authority remains the canonical absolute ALPHA `usage-ledger.json` and adjacent `attempt-ledger.md`. At allocation, actual spent totals remain **5 calls / US$0.09113535**, outstanding per-call holds remain zero, and all old call rows are preserved. The new allocation replaces the released allocation slot, not historical A1 evidence or call history.

`m7-provider-proving` is the selected future sole writer; the parent retains control through connection checks and will explicitly hand off the writer only with invocation instructions. No other worker may write a cloned ledger or launch paid calls. During the run the parent will not concurrently mutate the live ledger/journal. The driver's exclusive-create lock supplements this coordination; stale locks are not automatically removed.

Count preparation, continuations, compaction, failed attempts and any native request. The canonical-registry three-rejection limit, zero automatic retry, unknown/partial/journal-pending stops and full per-call holds remain mandatory. No budget top-up, guessed cost, response/counter repair, alternate model or automatic resubmission is authorized. Native costs are catalogue estimates, not invoices. Any unresolved usage stops further calls and remains held for owner review.
