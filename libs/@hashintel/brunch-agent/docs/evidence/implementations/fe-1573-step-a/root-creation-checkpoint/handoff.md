# Native root creation — blocked checkpoint

## Disposition and owning need

**Partial, not integration-ready or construction-stratum completion.** The actual built ChatAgent + Chrome path creates root places and a transition from an empty document, connects them with two arcs, obtains a clean canonical compilation result, settles a correction, changes a place field and transition code, and obtains ordinary-name entity/field why. The next actual reopen boundary loses the recorded place capacity. Stop expansion at that published-core ownership boundary; no Petrinaut patch or alternate storage/rehydration workaround was made.

The smallest owning need is in `libs/@hashintel/petrinaut-core/src/types/sdcpn-input.ts`: `SDCPNPlaceInput` and `normalizeSDCPN()` omit `capacity`. `createJsonDocHandle()` calls that normalizer on initial/reopened documents. The original complete document has capacity **3** on the corrected place and explicit **null** on the other place; the reopened handle has neither field. The positive value loss changes behavior, not serialization order. The normalizer documents complete-SDCPN equivalence, but the actual boundary falsifies that claim. The standalone `capacity-reopen.mjs` independently reproduces the loss using the verified Chrome post-definition and the published canonical APIs; it is diagnostic input to a new test handle, never imported conversation authority.

**Return to the integration owner before broadening:** authorize/own the minimal canonical capacity preservation repair, including absent/null/zero/positive input semantics, normalization/handle round-trip regression tests and applicable published-package docs/changeset. Do not drop capacity from the construction claim, substitute another correction, invent defaults, or restore it from Brunch history. Existing published package files and changesets remain unchanged here. After that owning repair, repeat the unchanged Chrome preservation assertion and continue the pending controls/root classes. There is no owner utility, genuine/provider admission, Step A acceptance or Step B authorization in this packet.

## Source, base and ownership

- Verified initially clean at exact base `3f86c1461c2654b80b5eca0e7b5e65580146a9e0`, branch `ln/fe-1573-root-creation`, worktree `/Users/lunelson/.herdr/worktrees/hash/m7-root-creation`.
- `f0ce69ceab`: bounded native node candidate, existing recorder/effect/why join, distinct empty-document route, focused tests and the actual browser witness.
- `ce9978bac1`: independent reopen assertion checks the raw browser observation after hash verification, not the XML-escaped model-context projection.
- `68ca62a4a9`: synthetic user turns are plain operational accounts, distinct from the scripted assistant's settled Markdown outputs; no user-provided answer workpiece bootstraps the path.
- The following evidence commit adds this packet only. `source-build-manifest.json` pins full source commit IDs, all 15 changed source files, relevant protected source/tree equality, actual build bytes, installed runtime files and generator identity. `write-set.json` enumerates the actual write set.
- No changes to MISSION, core evidence/locator/carry, runtime patches, package-manager wiring, paid ledgers/driver/attester/accounting/admission algorithms, `app.ts`, published Petrinaut source, shared witness helpers or existing importer inventory. No push, history rewrite or cleanup of others.

### Serial integration-owner coordination

The concrete new source is `apps/brunch-agent/test/root-creation.integration.ts`. It boots the existing built ChatAgent/website with one ephemeral loopback listener, real isolated Chrome, native synthetic SDK responses, actual canonical client callbacks, original SQLite history and outside assertion/error checks. It imports the existing reviewed helpers without editing them. Review and add its one entry to `SUBSTRATE_INTEGRATION_ENTRY_POINTS` in `apps/brunch-agent/test/architecture/boundaries.integration.ts` serially; the exact-inventory test is intentionally still red until that owner action. A convenience workspace script is optional: the direct command below already runs it. No inventory assertion was weakened and no substrate import was hidden.

## What changed and what remains bounded

The existing plugin-owned `observedConstructionBrowserToolNames` catalogue now includes `addPlace`, `updatePlace`, `addTransition`, `updateTransition` and `getNetCompilationErrors`, alongside its prior read/arc/weight tools. App registration still consumes that same export; `app.ts` and its admission algorithm were untouched. The real mixed node+revision proposal refuses before either sibling is published. This is only candidate mounting, not class admission.

Each mutation still cites an earlier verified full browser read ID/exact raw hash and explicit settled basis. The app checks node identity against the verified current document and retained verified reads/results before publishing an awaiting-browser outcome; duplicate and unknown identity controls do not rely on model courtesy. The browser repeats current identity checks before the synchronous callback. Known-retired identity checks are implemented and unit-tested, but the new actual-browser retired/deletion control lies beyond the failing reopen gate and is not credited. The existing arc helper now also rejects an already-present identity rather than delegating duplicate refusal to the action.

Node input composition uses the canonical Zod schemas, separate Brunch envelope and native input export. A stronger full-export test exposed lost root descriptions after `safeExtend`/`refine`; the final composition reapplies the canonical schema's own metadata, without copying fields. All four full native exports, including their nested definitions, now compare exactly after removing only the Brunch envelope; the actual serialized synthetic SDK entries also match their joined native exports. Canonical input validation and normalization remain authoritative. No JSON Schema → Valibot converter, constrained-generation change or provider exception was introduced.

The existing snapshot recorder now partitions a created node by field, so a generated kernel cannot hide inside one supported created subtree. Direct fields must match the canonical requested values; unrequested/sanitized/generated changes remain disjoint derived effects. For these four synchronous node actions, the verifier runs the canonical action against a detached pre-definition and compares its complete expected content with the independently observed actual post-definition. This is a bounded operation check, not another document store, event observer or provenance engine, and does not count void success as effect. Disabled-extension and changed nested footprints are unavailable. Full before/after observations, requests, raw envelopes and causal outcomes remain retained. Canonical generated-kernel and place-sanitization effects are unit-tested; a browser derived-effect witness and complete field utility are still unearned.

Why uses the existing authorized history/revision/source join. Legacy arc queries retain their accepted input shape; node queries use `kind`, ordinary unique `name`/ID and a field. The tool root remains an object because actual Flue rejects a root union. Historical target lookup uses identity in each recorded definition rather than carrying array indices between observations. Origin, later field change and attempt history are separate. Derived/unmapped fields explicitly refuse request-basis inheritance. The recorded capacity correction resolves to revision two while the place origin remains its creation call. These are operation-level, revision-local links, not source relevance, template completeness or useful semantic coverage.

`?brunchTracer=root-creation` selects a distinct empty document ID and incarnation-scoped conversation. The prior `construction` substrate and legacy fixed modes remain separate; no retained conversation is rebound with ignored initial data. The new route supplies no prepared net or workpiece. All source/user/workpiece text in this witness is explicitly synthetic; no hidden case material was read or added to guidance. The UI remains unpolished: panel overlap is visible, and no layout or utility claim is made.

## Actual evidence and limits

| Oracle | Result |
| --- | --- |
| Initial network guard and immutable cache-only install | Java/Node descendant deny/loopback controls passed; install exit 0 with scripts disabled and existing peer warnings. No download. |
| Forced base app/website build | 40/40 uncached serial tasks; app build includes client. |
| First required-assertion falsifier | Set `M7_FALSIFY_EMPTY_ASSERTION=1`: actual empty read produces `0 !== 1`, retained callback error reaches the outside assertion, process exits 1. A swallowed callback cannot turn this into green. |
| Final same-session native construction prefix | **24 outside-counted checkpoints / 26 synthetic requests / 7 applied records**, four exact native schema classes, two settled revisions, two places + transition + two arcs, capacity/code correction and three why results. Canonical check returns `No errors detected in your model – everything compiles!`. This prefix is inside an overall red run, not a whole-test pass. |
| Controls reached before reopen | Actual node+revision proposal rejects before sibling publication; duplicate node and unknown-read calls fail before browser execution; a same-value node update has a verified no-op record and visible Not applied output. |
| Final actual reopen | **37 total synthetic requests**, then the unchanged required assertion fails: `undefined !== 3`. Stored pre-reopen bytes retain both capacities; the hash-verified raw reopened observation loses both. No deliberate hand edit has run. |
| Canonical owning API reproduction | `capacity-reopen.mjs` independently fails `undefined !== 3`, exit 1. `normalizeSDCPN` and `createJsonDocHandle` both lose capacity from the actual verified post-definition. |
| Last forced serial affected portfolio (final production source; subsequent changes are witness-only) | **62/63 tasks**, zero cached. **1,678 tests: 1,677 pass / 1 fail**. All builds/typechecks/lints pass; the sole failure is the owner-held importer inventory entry for the new witness. Counts: core128, plugin85, binding20, transport45, app314/315, Petrinaut699, website386. |
| Retained first-progression Chrome replay | Exit 0; **9 checkpoints / 23 requests / 2 applied records**, stale Not applied, raw-base refusal, duplicate-result, foreign/conflict and one-browser/mixed proposal controls retained. |
| Retained prepared A3/A5 Chrome replay | Existing `test:reopened-why` exits 0 with its original scoped query/stock/retention controls; no new durability or genuine-record claim. |

Earlier red packets are retained: root-union mount refusal; incomplete root-description preservation; invalid test lambda module syntax; an overly specific expected success string; ambiguous textarea selection; the missing-capacity spinbutton timeout that exposed the actual reopen loss. The first lambda was an invalid test-authored module, not evidence that timing is known; its correction uses the documented `Lambda(...)` wrapper and no invented rate. The final explicit clean-diagnostics assertion was added rather than interpreting tool presence as a compilation pass.

Raw browser delivery/history observations are the canonical effect evidence. The model's XML wrapper escapes characters such as `>` in prose projections. Those extracted model-context objects are retained separately but are not rehashed as canonical definitions. The final raw observation verifies its own reported SHA-256 and shows only the two capacity omissions under content comparison; executable code is unchanged in the raw browser definition. Neither XML escaping nor key-order normalization is misreported as another document mutation.

## Reproduction

From repository root, with the already-installed cache artifacts. `E=libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/native-local-delivery` and `P=libs/@hashintel/brunch-agent/docs/evidence/implementations/fe-1573-step-a/root-creation-checkpoint`:

```sh
node "$E/verify-network-guard.mjs"
sandbox-exec -f "$E/deny-network.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 YARN_ENABLE_SCRIPTS=0 yarn install --immutable
sandbox-exec -f "$E/deny-network.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 CARGO_NET_OFFLINE=true VITE_BRUNCH_CHAT_ENDPOINT=/agents/chat yarn exec turbo run build test:unit lint:tsc lint:eslint --filter=@hashintel/brunch-agent --filter=@hashintel/brunch-agent-plugin-sdcpn --filter=@hashintel/brunch-agent-binding-flue --filter=@hashintel/brunch-agent-transport-aisdk --filter=@apps/brunch-agent --filter=@apps/petrinaut-website --filter=@hashintel/petrinaut --continue=always --force --concurrency=1
sandbox-exec -f "$E/loopback-only.sb" env YARN_ENABLE_NETWORK=0 COREPACK_ENABLE_NETWORK=0 M7_ROOT_CREATION_OUTPUT=/tmp/<fresh-output> yarn workspace @apps/brunch-agent exec node --experimental-strip-types test/root-creation.integration.ts
sandbox-exec -f "$E/deny-network.sb" node "$P/capacity-reopen.mjs" /tmp/<fresh-output>/records.json
```

Current expected outcomes are the inventory portfolio failure and two explicit capacity-preservation failures; they are not green safety verdicts. Run the falsifier on another fresh output with `M7_FALSIFY_EMPTY_ASSERTION=1`. Retained final browser store: `/tmp/m7-root-creation-final/conversation.db`. Files in this packet are observations, not a restore/import route. JSON and logs are losslessly gzip-compressed; screenshots remain original PNG bytes. `artifact-manifest.json` pins their exact bytes. The authorized regular-copy JAR is 27,103,489 bytes / SHA-256 `9718ff7844e89462c75dcd9b20a35136f6db257bfe1b874db1e3002e99de4609`, source/destination inodes 108379967/115536702, no link/overwrite/download.

## Remaining obligations

The [class table](class-table.md) retains all required root classes and exclusions. New browser stale/retired/foreign/conflicting-node controls after the reopen assertion are present prospectively but **not reached**; do not skip the assertion to claim them. Complete actual derived-effect/field-why coverage, successful reopened node why, deletion/retirement, remaining arc operations, types/elements/scenario migration, scenarios, and parameters when the representation uses them remain construction work after the owning fix. Unsupported operations remain unavailable; none may be silently dropped from accepted-region meaning or the denominator. Core source/evidence/carry, true-process A5, provider proving and the eventual genuine inventory/behavior/utility gates retain their owners. Everything here is unpaid and network-denied; Lu remains the semantic and product gate.
