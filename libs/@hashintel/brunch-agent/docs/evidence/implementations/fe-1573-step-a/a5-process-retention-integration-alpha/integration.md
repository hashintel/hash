# A5 process retention — integration-owner replay

## Disposition

Accept the corrected synthetic A5 process/compaction proof, not a genuine-record, utility or Step A verdict. The authoritative worker evidence is [`a5-process-retention/reviewed/`](../a5-process-retention/reviewed/) and its [scoped review](../a5-process-retention/review.md); the earlier `final/` packet retains a superseded context oracle. Correction `c6c3d3fb63` explicitly excludes prior workpiece/why results and call identities from folded query context, including a source-redacted cached answer. It does **not** remove the authoritative current workpiece or claim that its overlapping prose vanishes from model context.

The parent reviewed the runner, actual mounted operations, observation audit and correction, checked all 180 retained artifact/source-byte manifest entries and 1,377 reviewed instrument files, and independently ran the complete three-process path both before and after integration. No product or runtime behavior changed in this lane.

## Integrated changes

| Worker commit | Alpha commit | Change |
| --- | --- | --- |
| `2b55b7fc1c` | `1d1fc04a89` | Actual Chrome creation and original-store A5 queries across process restart/threshold folding; runner and audit |
| `c6c3d3fb63` | `99be9e8285` | Correct the cached-answer context oracle and its counterexample |
| `eddffc9669` | `0a35eaa761` | Retain reviewed evidence |
| `29cc402040` | `b6639166e4` | Retain exact guarded command logs |

The integration owner additionally admitted the two new hermetic test importers in `apps/brunch-agent/test/architecture/boundaries.integration.ts` and added `test:reopened-why-retention` to the app package. The script executes the three explicitly guarded phases; it does not authorize egress, import state or allocate paid calls.

## Independent results

| Boundary | Parent worker-tree replay | Alpha replay |
| --- | --- | --- |
| Distinct actual Node processes | 60314 / 61212 / 61240 | 67382 / 67891 / 67902 |
| Same original SQLite store | `/tmp/m7-a5-process-review.d3H6g2/proof/original/conversation.db` | `/tmp/m7-a5-process-review.d3H6g2/alpha/original/conversation.db` |
| Actual threshold compactions | 2 | 2 |
| Independently pinned canonical completions | 31 | 31 |
| Falsifiers rejected | 10/10 | 10/10 |
| Source/build/runtime instrument | Unchanged through all phases | Unchanged through all phases |

Both runs create actual browser read/mutation/result records first, then query `brunch_workpiece` and `brunch_why` through the built ownership-guarded ChatAgent in separate OS processes against the original store. The original true-user source, validated overlapping carried relations, governing revision, distinct current revision, recorded effect and exact completions survive. Foreign authorization refuses. No completed browser effect is reissued. Reopened answers remain explicitly as-of the retained observation, not freshly live merely because the process reopened.

All ten counterexamples fail: source omission/change, carried relation loss, completed-response omission/duplication, settlement omission, same PID, false live attribution, original source entry still in context, and source-redacted prior query answer still in context. The last discriminator closes the actual review finding rather than merely repeating the prior passing assertion.

On alpha, the unchanged architecture test passes **27/27**, application TypeScript passes, ESLint has zero errors, and `git diff --check` passes. This is scoped verification of proof-only changes; the earlier **63/63 tasks / 1,627 tests** remains the last whole-portfolio baseline, not a newly claimed run. The parent also inspected the actual Chrome screenshot: labelled TEST preparation, current revision 3, canonical read/arc results and a partially-supported structured why result remain visible.

## Reproduction and artifacts

From the repository root, `yarn workspace @apps/brunch-agent test:reopened-why-retention /absolute/fresh/path` invokes the runner's verified deny/loopback profiles. The retained exact phase commands are in `proof/commands.log.gz` and `alpha/commands.log.gz`; the optional first positional argument chooses a fresh proof directory, never an existing store to relocate or reconstruct from these observations.

`artifact-manifest.json` hashes 88 retained replay artifacts. JSON observations and instrument maps are gzip-compressed without rewriting their source bytes; manifest entries retain both source and artifact SHA-256. Each cohort contains its audit, process/network logs, full public/context/event/source/record/completion observations and actual browser screenshot. Original live SQLite stores and Chrome profiles stay at their original local paths; they were not copied into a new test authority. These saved observations are diagnostic/equality evidence, never a state-import API.

All replays were synthetic and process-tree network-denied except their explicit local browser listener. Paid totals remain **5 calls / US$0.09113535**, with no reservation active during this integration. Nothing establishes genuine Vestera support, useful explanation coverage, native-provider fidelity, relocation/import, retrospective legacy repair, arbitrary concurrency, remote/power-loss durability or Step A/B acceptance. Final A4 and owner/utility gates still require the genuine path.
