# Pass-along prompt: Petrinaut/Brunch manual tour and review

You are the agent helping the owner tour and evaluate the reconciled Petrinaut/Brunch product. Work in `/Users/lunelson/.herdr/worktrees/hash/alpha` on `ln/pn-tooling-remediation`, or ask the owner for the relocated worktree. Read the repository `AGENTS.md`, package instructions and [restack review](petrinaut-tooling-restack-review.md) first. The six-unit automated walk and the subsequent async I-mode handoff repair are bounded evidence, not a live model-quality verdict. The first live I-mode **Surprise me** request in the tour failed before admission with `Mixed browser/server proposal refused before admission. Submit revision or server work separately from browser work.` Its exact proposed tool names were not captured. Retain this as a failed observation even if a later attempt works; a synthetic green mixed-proposal witness is not a live-provider retry. Your mission is to help the owner inspect the real UI, remodel a disposable net, exercise the assistant modes, and produce a reviewable evidence package. Do not edit production code, rewrite history, push, choose A/B, or mark downstream FE issues done during this tour.

## Authority and permission gate

Begin by reporting current branch/commit, worktree changes, configured local endpoints, which services and browser tabs already belong to the owner, and whether the product is reachable. Ask before starting or repurposing a shared process or pane. Use a new disposable document, never a shared user document, for changes. Do not print API keys, auth headers, personal conversation content or unredacted network bodies in the report.

**Stop before any paid provider request or live Voice session.** Obtain explicit owner approval for the provider/model, modes, scenario count, approximate request/cost ceiling and whether Voice is in scope. A configured credential is not approval. If approval is absent, run the network-disabled named witnesses and inspect UI/configuration without sending real model requests; label the native Stock `/api/chat` runtime check **NOT RUN**. After approval, keep a running cost/request tally and stop at the agreed limit or any unexpected provider behavior. Testing A and B is not selecting one; bring comparative findings to the owner for a separate choice.

The evaluation mode (`VITE_BRUNCH_EVALUATION_MODE=F|I|A|B`) is a **build-time** website setting, not a chat toggle. Rebuild or use an isolated artifact for each mode, verify the actual first request's `initialData.mode`, and never run an I-specific wrapper against an F/A/B build. Native S is the website's Stock assistant at `/api/chat`, not another Flue mode. The Brunch preview endpoint and default assistant are configured with `VITE_BRUNCH_CHAT_ENDPOINT` and `VITE_PETRINAUT_DEFAULT_ASSISTANT`; confirm the current proxy/origin from the website and Brunch README instead of assuming a port.

## Tour sequence and evidence to collect

1. **Controls and orientation.** In the real editor, locate the Labs assistant and Voice settings, show that Stock is the ordinary default without a preview override, Voice starts off, choices persist across reload, and switching Stock→Brunch rechecks Voice capability. If live Voice is authorized, test it separately and record service readiness; otherwise distinguish visible controls from a working voice session. Save a screenshot and the selected document/incarnation and conversation identity **without exposing credentials**.
2. **Native S control.** With permission for the Stock provider, use the real `/api/chat` endpoint and the canonical Petrinaut panel. Ask it to create a small Queue place and observe the browser tool result, actual document change, saved revision and the assistant continuation. Capture the request/model identity, prompt, canonical tool name/input/output and whether the document persisted; compare the canonical catalogue against F rather than inferring parity from one answer. This is the previously deferred runtime gate; a static catalogue or mocked component is not its substitute. If no permission, mark S runtime **NOT RUN**, even if the UI renders correctly.
3. **F transport control.** Use the same task and prompt as S when approved, but send Stock over Flue; use the same model only if both endpoints actually support it, otherwise record the mismatch and do not claim matched quality. Compare F's catalogue to all 47 currently canonical tools including `createExperiment`, with no Brunch draft/Ledger/provenance tools; observe one browser-result admission, continuation and saved change. Do not call F a Brunch construction result.
4. **Integrated I remodel.** After separate permission for any paid model request, retry **Surprise me** on a fresh disposable net and capture the actual proposed tool names, admission outcome and provider continuation; do not backfill the names missing from the first failure. Have the person describe a queue/inventory behavior and request a bounded canonical change. I may construct against the directly authorized live document before a Ledger settlement; a concurrent `mutate_workpiece` is not evidence that an unfinished browser write succeeded. Inspect the eventual Ledger revision and canonical `getLatestNetDefinition` result. Request a bounded root `addPlace`, then a type, transition and arc as appropriate; verify the handle's revision chain and repository settlement, diagnostics on code changes, canonical mutation metadata for the host-recorded root names, and Ledger evidence that really correlates a call with a settled change. I returns issued canonical browser results during the same Flue response, rather than by a later client-result signal; after Stop or a lost result, classify an attempted document effect as unknown, inspect the document before any further change and never auto-retry it. Try an ad-hoc correction to an existing definition rather than deleting and re-adding it. Distinguish a delivered `applied` result from durable settlement. Test Stop, reload and a stale/ambiguous read only on a disposable conversation; if an action cannot be safely performed, mark it **NOT RUN**.
5. **Unselected A and B.** In separate fresh conversations/builds with the same disposable task, let A declare an intent supported by an exact Ledger passage, then execute the canonical mutation separately; inspect the host-resolved revision/locator and mark expected-impact satisfaction for owner adjudication, not automatic acceptance. Let B apply one bounded construction with an intent and literal passage; inspect the private host authorization outcome, successful prefix, exact revision settlement and verified deep record. If the task warrants all three ordered operations, note what the built one-step witness did not establish. Preserve both options for the owner's later comparison; do not rename the user story to force one interface to win.
6. **Scenarios, metrics and experiments.** Add, update and remove a scenario and a metric through Petrinaut's six canonical tools on a disposable copy. Check each real document revision and saved state. Brunch currently reports these calls as `unrecorded`: that means no verified per-call cause, **not** that Petrinaut failed to save. For an explicit immediate run, use canonical `createExperiment`, check the source-bound `experimentRecord`, terminal result and non-mutating Ledger event. For a Ledger-derived proposal, obtain a fresh canonical read and ask for `draft_petrinaut_experiment`; inspect the `drafted`/`invalid` Flue result and shared execution card, then have the person separately choose Run or Dismiss. Run uses Petrinaut's experiment host; Run/Dismiss are editor-local and must not be reported as later Flue tool results. Record unsupported restrictions verbatim; `unsupported.blocksRun: false` is a model judgment, not proof of user consent to reporting-only semantics.
7. **Compiler and Voice boundaries.** Introduce a deliberate error in **net-function** code, check canonical `getNetCompilationErrors`, repair it and confirm a clean read and saved repair. Do not infer that this read compiled saved scenario or metric code: those are checked when creating an experiment. Exercise live Voice only within separate owner approval, and capture its capability and permission outcome rather than interpreting an available toggle as a successful call.
8. **Matched evaluation, only if separately authorized.** Follow `apps/brunch-agent/src/evaluations/matched-parity/README.md` for identical scenario, supported model, prompt, timeout/seed and capture policy across S/F/I/A/B; if an endpoint cannot run the selected model, stop and ask rather than label unequal runs matched. Start no paid arm or retry without the owner's explicit model/budget authorization. The pre-restack S/F artifacts in `/tmp/petrinaut-matched-parity-remediation-r2` and Stock prompt hash `a8b863c020b628a01bfe944ec0daaf51ae7e08adb2020c00048c6cd5012451fb` are historical comparators, not a matched post-restack verdict. Record failures as failures, preserve raw redacted traces and costs, and ask the owner before interpreting A versus B.

In an assigned worktree where their builds will not disrupt another process, the non-paid preflight commands are `yarn workspace @apps/brunch-agent test:manual:integrated-browser` and `test:manual:stock-over-flue-browser`. Each builds its own mode; run them one at a time. Neither proves the old legacy batch/layout hash and position-effect assertions. A green deterministic command is not a paid provider or manual UI result.

## Review output

Create a redacted evidence directory outside the repository unless the owner chooses a destination. For every S/F/I/A/B arm, report `PASS`, `PARTIAL`, `FAIL` or `NOT RUN` separately for catalogue/prompt, provider call, browser execution, saved revision, Flue admission, Ledger attribution, and human-observed model quality. Attach the exact command/build mode/commit, model and permission scope, screenshots or a screen recording, relevant tool-call IDs and terminal statuses, document revision chain, diagnostic text, and redacted history/result excerpts. Keep observations distinct from inferred causes; a source file or successful tool output alone cannot fill a missing row. Record paid requests/costs even when an arm fails.

End with (a) the native S runtime verdict or explicit pending status, (b) Stock-versus-F transport and I/A/B construction comparisons without an A/B selection, (c) draft Run/Dismiss and direct-experiment observations, (d) manual remodelling defects and reproducible steps, (e) risks the deterministic witnesses did not cover such as live Voice, remote persistence, current-mode layout/hierarchy and replay after reload, and (f) owner decisions needed for FE-1438/1394/1334 and FE-1503's optimisation-handoff consumer contract. Present A/B selection, reporting-only restriction consent, paid retries, and any code repair as questions to the owner rather than implied permissions. Stop and report at the first unsafe, ambiguous or unexpectedly paid boundary.
