# Handoff

> Generated by `dev-handoff` at 2026-08-28 13:31 UTC+2. Read this file to resume work.

## Goal

Prove Mission 3: one production Flue `ChatAgent` uses a structurally typed elicitation runbook to interview, recover a Markdown IR from `history()`, and construct Petri-net JSON that `parseSDCPNFile` accepts — without the typed capture kernel, `brunch_ask`, sweep, or Mission 2's capture store.

## Phase

- **Last completed skill**: `dev-build` (informal) — implemented the Cursor Mission 3 plan end-to-end. There was no `dev-scope` card; the plan's slices were the scope. Produced: runbook skill, headless driver, hermetic throughline, two real-model runs, one construction edit cycle, proof evidence. `MISSION.md` was **not** archived.
- **Current skill**: `dev-handoff`
- **Flow position**: `consult → plan → build → [handoff] → [review / acceptance / commit]`
- **Handoff trigger**: user requested `/dev-handoff` after the implementation session, before commit/PR/acceptance.

Completion of the usual `dev-*` chain for this work:

| Skill | State |
| --- | --- |
| consult / orient | done (brunch-agent `MISSION.md` + CONTEXT) |
| plan | done (Cursor plan; not a repo file) |
| scope | skipped — plan slices used as scope |
| spike | done as Spike A inside the build |
| build | done |
| review | not started (no `dev-review`; self-review only) |
| sync / commit | not started |

## In-flight work

> These artifacts exist only in the prior conversation, or only in Cursor, not in this package's git history. Reproduce them here with full fidelity.

### 1. Cursor plan (not in the git repo)

Path on the author's machine (do not edit unless the user asks):

`/Users/lunelson/.cursor/plans/Mission 3 runbook PN-becada85.plan.md`

Also saved as an upload in that session. The plan's todos were all marked completed in the build session. Sequence that was executed:

1. Spike A — `activate_skill` + `read_skill_resource` on the real `ChatAgent`; packaging choice; pin `parseSDCPNFile` on inbox fixtures
2. HITL 1 — author five Markdown files; review for scenario leak / PN vocab / kinds-slots / heading homes
3. Mount skill, shrink always-on, update faux `petrinaut-chat` + README
4. Headless `createFlueClient` driver + simulated Vestera expert
5. Real-model interview; recover IR; stop if not construction-ready
6. Same-conversation construct; `parseSDCPNFile`; no capture-store write
7. Gap return only if exposed; else record unexercised
8. One miss-classify / edit / rerun; write proof evidence; archive only on acceptance

The user instructed: **do not edit the plan file**. Linear writes still need explicit approval. Branch identity already existed: `ln/fe-1525-headless-runbook-pn`.

### 2. HITL gates — skipped by user instruction, self-performed

The plan required human-in-the-loop at several gates. The user then said implement the plan and **do not stop until all to-dos are complete**. The build session therefore did not pause:

| Gate | Plan intent | What actually happened |
| --- | --- | --- |
| HITL 0 | Confirm interviewer model before paid run | Script defaulted `BRUNCH_CHAT_MODEL` to `claude-sonnet-4-5`. Panel `ChatAgent` still defaults to `claude-haiku-4-5`. Expert also sonnet. No human-expert pass. |
| HITL 1 | Review authored runbook before tokens | Self-check against the plan checklist (no Vestera/truck/fab in resources; no kinds/slots; PN vocab only in construction/checks; provenance tags). No human sign-off. |
| HITL 2 | Stop if IR is a bag of notes | Run 1 IR was construction-ready (unknowns/conflicts filed). Did not stop. |
| HITL 3 | Full-artifact review | Written into `docs/evidence/proofs/implementations/fe-1525-headless-runbook-pn.md`. User has not accepted the mission. |

A new thread must **not** treat self-HITL as user acceptance.

### 3. Spike A packaging verdict (also now in the proof file)

Question: `SKILL.md` Vite import vs `defineSkill({ files })` so both the Vite app and `node --experimental-strip-types` stay honest.

Tried: Flue packaged-skill path is `/.flue/packaged-skills/${encodeURIComponent(skillId)}/${file}` with `skillId = skill:${name}:${contentHash}`. `read_skill_resource` input is `{ path, offset?, limit? }`.

Verdict: **`defineSkill` + `readFileSync` of sibling Markdown**. Vite `SKILL.md` import cannot load under the hermetic proof runner. Do not wrap Flue with a custom loader. Do not switch packaging without re-proving `read_skill_resource` on the same `ChatAgent`.

### 4. Dead end — case-pack path ENOENT (not in the proof file)

First `yarn runbook:headless` failed immediately:

```
ENOENT: .../apps/libs/@hashintel/brunch-agent/evaluations/cases/.../situation-pack.md
```

Cause: `import.meta.url` is `apps/brunch-agent/src/runbook-headless-run.ts`; `../../libs/...` resolved under `apps/`, not repo root. Fixed to `../../../libs/@hashintel/brunch-agent/...`. Do not "fix" this by moving the cases.

### 5. Dead end — `empty-interviewer` abort (fixed in driver, described in proof)

Run 1 `stopReason: "empty-interviewer"`. The driver took **only the last assistant message's text**. A tool-only Flue message (activate / `read_skill_resource`) made `latestAssistantText === ""`, aborted the interview after two expert replies, then still dispatched construct.

Fix (tool-runtime, not teaching): walk assistant messages from the end and return the first non-empty text. Run 2 then `stopReason: "hard-stop"` and completed five interview turns.

### 6. Local parse-strip experiment (conclusion in proof; command only in chat)

Run 2 `parse.ok === false`. Diagnosis script (from repo root):

```js
// as-is → false (weight ≤ 0, plus types/parameters schema)
// delete types + parameters → still false, only "Too small: expected number to be >0"
// nonpositive weights were exclusive mode-return arcs:
//   t_line1_finish_order → p_line1_idle_{white,tint,specialty} weight 0 (and Line 2/3 analogues)
// drop weight≤0 arcs AND omit types/parameters → parseSDCPNFile ok: true,
//   18 places, 24 transitions, hadMissingPositions: true
```

Petrinaut arc `weight` is `z.number().positive()`. Weight 0 cannot mean "don't take this branch". Invented `types`/`parameters` objects used `{ name, value }` / `{ name, type }` instead of Petrinaut's id/variableName/type/defaultValue (and the type-color schema).

**One teaching rerun already spent.** Plan forbade unbounded hill-climbing. Next construction-resource edit (if the user wants another run) should say: omit `types` and `parameters` unless they match Petrinaut; never emit arc `weight` ≤ 0; exclusive modes are separate transitions, not zero-weight arcs.

### 7. Plan rule bent: "edit one owning location"

Classified miss: construction / PN JSON field contract. Edited `pn-construction.md` (minimal valid object). Also added a one-liner to `checks.md`. Driver empty-text fix was a second class (tool-runtime). A purist read of the plan would count that as two homes. Recorded here so a reviewer does not treat it as undisclosed scope.

### 8. No `dev-review` — self-identified review debt

There was no `dev-review` pass. The table below is the full finding list from the build session, not a formal review.

## Review findings

> ALL findings from the session. None came from `dev-review`.

| # | Finding | Status | Implications |
|---|---------|--------|-------------|
| 1 | Real-model `parseSDCPNFile` still `ok: false` after the one allowed teaching rerun | `deferred` (budget spent) | Mission proof bullet 6 is hermetic-true, real-false. Acceptance is a human call. Next teaching edit is weight>0 + omit/correct `types`/`parameters`. |
| 2 | Opening overload: 4–10 numbered questions per turn | `deferred` | Universal smell; not a heading-catalogue failure. Edit home would be `elicitation.md` questioning section. |
| 3 | `Transform to PN` children still live in `elicitation.md` | `deferred` | Did not cause PN-shaped interviewing on these runs. Could move solely into `pn-construction.md` if a later run interviews in net vocabulary. |
| 4 | Construction-gap return unexercised | `deferred` | Agent named gaps and delivered `partial-with-named-gaps` instead of asking the smallest next question. Optional HITL contradiction ("washdown crew cannot cover two lines") was not injected. |
| 5 | Construct turns 162s then 271s | `deferred` | Interview turns 5–23s. Not Condition-5 sweep latency. Construct emission of a large net is the slow turn. Stop line about "ordinary teaching turns at minute-scale" did not fire; construct did. |
| 6 | IR fence sometimes not closed before `pn-json` | `deferred` | Scraper still recovered a block; `.ir.md` can contain trailing `</runbook-ir>` / "Now I'll construct…" prose. |
| 7 | `build-artifact.test.ts` not re-run against a fresh `dist` | `deferred` | Session ran `vitest --exclude test/build-artifact.test.ts`. `test:unit` via turbo depends on the app build; a stale bundle failed `route(\`/agents/\`)` earlier in the session. Rebuild before claiming CI green. |
| 8 | Two resource homes edited in the one cycle (`pn-construction.md` + `checks.md`) | `deferred` | See in-flight #7. |
| 9 | SQLite conversation DBs were first written into `docs/evidence/…` then deleted; driver now uses `os.tmpdir()` | `addressed` | Do not commit `*.db`. |
| 10 | HITL 1–3 never had a human pause | `deferred` | User may still want to read the five skill Markdown files and the two IR artifacts before accepting. |
| 11 | Nothing committed, pushed, or Linear-updated | `in-progress` | Branch has no upstream. Plan: Linear writes need explicit approval. |

### Diagnostic evidence

> Concrete proof points. Do not re-investigate these as if unknown.

- **Hermetic throughline (2026-08-28, vitest):** `test/runbook-headless.test.ts` passed. `hasIr: true`, `parseOk: true`, `hadMissingPositions: true`, tools `activate_skill` + `read_skill_resource`, no `sweep` / `brunch_ask`, all four resource filenames read.
- **Inbox oracle:** all `docs/inbox/sdcpn-examples-to-validate/*.json` parse `ok: true`.
- **Faux petrinaut-chat:** activate `{ name: "sdcpn-modelling" }`, then `read_skill_resource` path containing `elicitation.md`, ping + `readPetrinautDoc` still present.
- **Run 1 result line:** `stopReason: empty-interviewer`, `hasIr: true`, `parseOk: false`, `maxTurnMs: 162249`, tools only `activate_skill` + `read_skill_resource`. PN used `label` / `initial` / top-level `arcs`.
- **Run 2 result line:** `stopReason: hard-stop`, `hasIr: true`, `parseOk: false`, `maxTurnMs: 270857`. PN used `name` / `inputArcs` / `lambdaType` / `lambdaCode`. Parse error started `Too small: expected number to be >0`.
- **Local strip:** omit `types`+`parameters` and drop `weight <= 0` arcs → `{ ok: true, places: 18, transitions: 24, missingPos: true }`.
- **No capture store:** both run JSON records `wroteCaptureStore: false`.
- **Last unit run (session):** `yarn vitest run --exclude test/build-artifact.test.ts` → 13 files, 46 tests passed. `yarn lint:tsc` clean. `yarn lint:eslint` 0 errors, 11 warnings (`no-await-in-loop` on the sequential drive is sequential-on-purpose; same warning exists in `test/assets.test.ts`).
- **First headless attempt:** ENOENT under `apps/libs/@hashintel/...` (see in-flight #4).

## Decisions and assumptions

| Decision/Assumption | Status | Source |
|---|---|---|
| One ChatAgent, one skill `sdcpn-modelling`; replace `confirm-path` | `persisted` | `chat-agent.ts`, MISSION.md |
| Packaging: `defineSkill` + `readFileSync`, not Vite `SKILL.md` import | `persisted` | `sdcpn-modelling.ts`, proof file |
| IR recovered from last `runbook-ir` fence; PN from last `pn-json` fence | `persisted` | `runbook-artifacts.ts` |
| Headless interviewer default `claude-sonnet-4-5`; panel remains haiku unless `BRUNCH_CHAT_MODEL` | `persisted` | `runbook-headless-run.ts` vs `chat-agent.ts` |
| Expert is a second Anthropic call with only `situation-pack.md`; replies are user messages | `persisted` | driver + protocol.md |
| Do not implement `MISSION.next.md` (M4 host continuity, M5 typed map, M6 capture) | `persisted` | MISSION.md / MISSION.next.md |
| Capture pipe may remain on the app for M2 regression; Mission 3 proof must not apply it | `persisted` | MISSION.md; hermetic test asserts no sweep |
| `parseSDCPNFile` is the named Petrinaut-accepts boundary | `persisted` | MISSION.md; fog 6 answer in proof |
| One teaching rerun budget is spent; no second paid construct-quality rerun unless the user asks | `volatile` | conversation / plan slice 6 |
| Mission is not accepted until a human says so; do not archive `MISSION.md` | `volatile` | MISSION.md Status + conversation |
| Linear/PR/commit require explicit user ask | `volatile` | user rules + plan |
| Worktree is `bravo`; code lives in `@apps/brunch-agent`, mission docs in this lib | `persisted` | git layout |

## Repo state

- **Branch**: `ln/fe-1525-headless-runbook-pn` (no upstream; not pushed)
- **HEAD**: `e86933559d` Specify structurally typed elicitation runbooks
- **Recent commits**: `e86933559d` spec; `eb5bf2e1f8` brunch-agent image assets; `2457c12a39` housekeeping/inbox; `ff55ff7d91` upcoming-mission drafts; `0313ae2cc0` Archive Mission 2
- **Dirty files**: all Mission 3 implementation is **uncommitted** (modified chat-agent/README/package.json/petrinaut-chat tests; untracked skill tree, driver, artifact recovery, tests, protocol, proof, two run artifact triples `.md/.json/.ir.md`)
- **Test status**: last known green excluding `build-artifact.test.ts` (see finding 7). Do not assume `yarn workspace @apps/brunch-agent test:unit` via turbo is green without a rebuild.

## Artifact status

This package does not use `memory/SPEC.md` / `memory/ROADMAP.md`. Mission files are the authority.

| Artifact | Exists | Current vs conversation |
|---|---|---|
| `MISSION.md` | yes | current, still **Live** — do not archive |
| `MISSION.next.md` | yes | current — not execution authority |
| `docs/specs/structurally-typed-elicitation-runbooks.md` | yes | accepted design input; not re-opened |
| `docs/evidence/proofs/implementations/fe-1525-headless-runbook-pn.md` | yes | current for fog answers and checklist |
| run artifacts under `docs/evidence/evaluations/.../runbook-headless/` | yes | two runs; sqlite DBs deleted |
| `evaluations/protocols/.../runbook-headless/protocol.md` | yes | how to drive |
| `memory/SPEC.md` | no | N/A |
| `memory/ROADMAP.md` | no | N/A |
| Cursor plan | yes, outside git | do not edit unless asked |

## Next steps

1. **Human acceptance (or not) of Mission 3.** Read the proof file and the two `.ir.md` artifacts. Decide whether hermetic `parseOk` plus a locally-strippable real net is enough, or whether one more construction-resource edit + paid rerun is required. Do not archive `MISSION.md` without that call. Skill: none — this is a user decision, then `dev-consult` if reorienting.
2. **If they want the real net to parse:** edit only `pn-construction.md` (and maybe checks): omit `types`/`parameters` unless Petrinaut-shaped; forbid `weight` ≤ 0; exclusive line-states as separate transitions. Then `BRUNCH_RUNBOOK_HARD_STOP=5 yarn workspace @apps/brunch-agent runbook:headless`. That is a **second** teaching cycle the plan did not budget — get explicit okay. Skill: `dev-build` on that single miss.
3. **If they want it committed:** commit the dirty apps + lib evidence with a sentence-case imperative message (no `feat:` prefix). Do not add `*.db`. Do not push or open a PR or write Linear unless asked. Skill: git commit per user rules.
4. **Before claiming CI:** `turbo run build --filter @apps/brunch-agent` then full `test:unit` including `build-artifact.test.ts`.

## Open questions

- Does the user accept Mission 3 with real-model `parseOk: false` and a documented local strip that would make it parse?
- Interviewer model for a possible third run: keep sonnet, or try haiku / opus?
- Inject the optional HITL contradiction to exercise gap return, or leave unexercised?
- Commit now, or review the five skill Markdown files first (HITL 1 that never paused)?

## Resume prompt

Paste this into a new session:

> Read `libs/@hashintel/brunch-agent/HANDOFF.md`. It is the full state of Mission 3 (`ln/fe-1525-headless-runbook-pn`). Implementation is on disk and uncommitted. `MISSION.md` is still live — do not archive it. The immediate next step is a human acceptance call: hermetic PN parse succeeded; the second real-model construct still fails `parseSDCPNFile` on weight-0 exclusive arcs and malformed `types`/`parameters` (locally stripping those parses 18 places / 24 transitions). Do not start a second paid teaching rerun, commit, push, PR, or Linear write unless the user asks. Start by reading the handoff In-flight and Review findings tables, then the proof file at `docs/evidence/proofs/implementations/fe-1525-headless-runbook-pn.md`.
