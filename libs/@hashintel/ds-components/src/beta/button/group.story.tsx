import { Button, ButtonGroup } from "./button";

export const App = () => {
  return (
    <ButtonGroup variant="outline">
      <Button>Save</Button>
      <Button>Cancel</Button>
    </ButtonGroup>
  );
};
