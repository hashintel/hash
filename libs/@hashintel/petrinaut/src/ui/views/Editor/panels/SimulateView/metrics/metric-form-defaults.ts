import type { MetricFormState } from "./metric-form";

/**
 * Empty defaults for a new metric form.
 */
export const EMPTY_METRIC_FORM_STATE: MetricFormState = {
  name: "",
  description: "",
  code: "",
};
