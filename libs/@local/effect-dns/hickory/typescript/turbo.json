{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build", "@rust/effect-dns-hickory#manifest"],
      "inputs": [
        "$TURBO_DEFAULT$",
        "../rust/src/**",
        "../rust/build.rs",
        "../rust/Cargo.toml"
      ],
      "outputs": ["./dist/**"]
    },
    "test:unit": {
      "dependsOn": ["build"]
    },
    "lint:eslint": {
      "dependsOn": ["build"]
    },
    "lint:tsc": {
      "dependsOn": ["build"]
    }
  }
}
