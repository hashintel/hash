# hash-config

Layered configuration for HASH binaries: files, environment variables, and command-line overrides merge into one serde struct, and every value stays traceable to the source that supplied it.
