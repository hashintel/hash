cargo-features = ["edition2024"]

[package]
name = "hql-diagnostics"
version = "0.1.0"
authors.workspace = true
edition.workspace = true
license = "AGPL-3.0"
private = true

[dependencies]
anstyle = "1.0.8"
anstyle-yansi = "2.0.1"
ariadne = "0.4.1"
derive-where.workspace = true
hql-span = { version = "0.1.0", path = "../span" }
serde = { workspace = true, optional = true, features = ["derive"]}
serde_with = { version = "3.9.0", optional = true, default-features = false }
text-size = "1.1.1"

[dev-dependencies]
jsonptr = "0.5.1"
serde_json = "1.0.122"

[features]
serde = ["dep:serde", "dep:serde_with", "hql-span/serde"]

[[example]]
name = "sexpr"
required-features = ["serde"]

[lints]
workspace = true
