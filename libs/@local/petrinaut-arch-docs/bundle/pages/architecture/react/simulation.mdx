---
title: "Simulation provider"
description: "Owns the run configuration and mirrors the core simulation handle into React"
sidebar_order: 1031
---

> Owns the run configuration and mirrors the core simulation handle into React

**Package** `@hashintel/petrinaut` · **Layer id** `react.simulation` · **Files** 3 · **Lines** 934

Declared in [`libs/@hashintel/petrinaut/src/react/simulation/README.md`](https://github.com/hashintel/hash/blob/main/libs/@hashintel/petrinaut/src/react/simulation/README.md).

## Boundaries

Crossing one of these is never a plain function call.

| Kind | What may not cross | Declared in |
| --- | --- | --- |
| `thread` | Wraps the core's worker transport; every frame the UI reads has crossed a thread boundary | [README.md](https://github.com/hashintel/hash/blob/main/libs/@hashintel/petrinaut/src/react/simulation/README.md) |

## Invariants

- The initial marking is session state, configuration for the next run, and survives a reset — [README.md](https://github.com/hashintel/hash/blob/main/libs/@hashintel/petrinaut/src/react/simulation/README.md)

## Depends on

Aggregated from real TypeScript imports.

| Layer | Imports | Package boundary |
| --- | --- | --- |
| [Headless core](../core) | 3 | crossed |
| [Shared hooks](hooks) | 3 | — |
| [React bindings](.) | 2 | — |
| [Worker entry points](../core/workers) | 1 | crossed |
| [LSP provider](lsp) | 1 | — |
| [Editor state](state) | 1 | — |

## Depended on by

Who reaches into this layer.

| Layer | Imports | Package boundary |
| --- | --- | --- |
| [Editor shell](../ui/views/editor) | 8 | — |
| [Canvas](../ui/views/canvas) | 3 | — |
| [Playback provider](playback) | 2 | — |
| [React bindings](.) | 1 | — |
| [Execution frame source](execution-frame) | 1 | — |
| [Shared hooks](hooks) | 1 | — |
| [Editor state](state) | 1 | — |
| [Views](../ui/views) | 1 | — |

## Notes

# Simulation provider

The React owner of a Quick Simulation run. It holds the run configuration —
initial marking, parameter values, seed, `dt`, `maxTime` — calls the core's
`createSimulation()` with a browser worker factory, and mirrors the resulting
handle's `status` and `frames` stores into context.

The distinction that catches people: the initial marking is _configuration for
the next run_, not output of the current one. Resetting a run clears frames but
deliberately keeps the marking, because the common action after a reset is to run
again with the same starting state.

## Source

3 files resolve to this layer, rooted at [`libs/@hashintel/petrinaut/src/react/simulation`](https://github.com/hashintel/hash/blob/main/libs/@hashintel/petrinaut/src/react/simulation) — files under a sub-layer's folder belong to that sub-layer instead. The full list is in `architecture.json`.
