---
title: "Frames & metrics"
description: "The frame layout and the readers hosts use to inspect one frame"
sidebar_order: 1016
---

> The frame layout and the readers hosts use to inspect one frame

**Package** `@hashintel/petrinaut-core` · **Layer id** `core.simulation.frames` · **Files** 4 · **Lines** 678

Declared in [`libs/@hashintel/petrinaut-core/src/simulation/frames/README.md`](https://github.com/hashintel/hash/blob/main/libs/@hashintel/petrinaut-core/src/simulation/frames/README.md).

## Invariants

- Readers are views over an existing buffer, not copies, so reading a frame does not allocate per place or per transition — [README.md](https://github.com/hashintel/hash/blob/main/libs/@hashintel/petrinaut-core/src/simulation/frames/README.md)

## Depends on

Aggregated from real TypeScript imports.

| Layer | Imports | Package boundary |
| --- | --- | --- |
| [Simulation engine](engine) | 3 | — |
| [Document types](../types) | 3 | — |
| [Simulation](.) | 2 | — |
| [HIR compiler](../hir) | 1 | — |

## Depended on by

Who reaches into this layer.

| Layer | Imports | Package boundary |
| --- | --- | --- |
| [Simulation engine](engine) | 9 | — |
| [Monte Carlo runtime](monte-carlo) | 4 | — |
| [Simulation](.) | 3 | — |
| [Headless core](..) | 1 | — |
| [Simulation controller](runtime) | 1 | — |
| [Simulation worker](worker) | 1 | — |

## Notes

# Frames & metrics

The frame is the unit of simulation output, and this layer owns both its memory
layout and the read side.

- `internal-frame.ts` — the layout: how a frame's places, tokens and transition
  state are packed into buffers.
- `frame-reader.ts` — the host-facing view over a frame
  (`getPlaceTokens`, `getPlaceTokenCount`, `getTransitionState`).
- `transition-state.ts` — per-transition firing data exposed through the reader.
- `hir-metric.ts` — evaluating a compiled metric against a frame.

The reader is deliberately a view rather than a snapshot object. Rendering a
large net means reading every place every frame, so a reader that materialised
objects would make frame cost scale with net size in allocation as well as
compute.

## Source

4 files resolve to this layer, rooted at [`libs/@hashintel/petrinaut-core/src/simulation/frames`](https://github.com/hashintel/hash/blob/main/libs/@hashintel/petrinaut-core/src/simulation/frames) — files under a sub-layer's folder belong to that sub-layer instead. The full list is in `architecture.json`.
