---
title: "Monte Carlo runtime"
description: "Runs many independent simulations with bounded frame memory, reporting metric aggregates"
sidebar_order: 1017
---

> Runs many independent simulations with bounded frame memory, reporting metric aggregates

**Package** `@hashintel/petrinaut-core` · **Layer id** `core.simulation.monte-carlo` · **Files** 21 · **Lines** 3,048

Declared in [`libs/@hashintel/petrinaut-core/src/simulation/monte-carlo/README.md`](https://github.com/hashintel/hash/blob/main/libs/@hashintel/petrinaut-core/src/simulation/monte-carlo/README.md).

## Public seams

Import specifiers through which this layer is reachable from outside its package.

- `@hashintel/petrinaut-core/workers/monte-carlo`

## Boundaries

Crossing one of these is never a plain function call.

| Kind | What may not cross | Declared in |
| --- | --- | --- |
| `worker` | Frame buffers stay inside the worker; only metric aggregates are posted to the host | [README.md](https://github.com/hashintel/hash/blob/main/libs/@hashintel/petrinaut-core/src/simulation/monte-carlo/README.md) |

## Invariants

- Frame memory is bounded regardless of run length — no frame history is retained — [README.md](https://github.com/hashintel/hash/blob/main/libs/@hashintel/petrinaut-core/src/simulation/monte-carlo/README.md)
- Uses the same extension-aware SDCPN sanitization as the interactive simulator, so disabled surfaces are not compiled here either — [README.md](https://github.com/hashintel/hash/blob/main/libs/@hashintel/petrinaut-core/src/simulation/monte-carlo/README.md)

## Depends on

Aggregated from real TypeScript imports.

| Layer | Imports | Package boundary |
| --- | --- | --- |
| [Headless core](..) | 18 | — |
| [Simulation engine](engine) | 14 | — |
| [Simulation](.) | 6 | — |
| [Document types](../types) | 5 | — |
| [Frames & metrics](frames) | 4 | — |
| [Simulation controller](runtime) | 1 | — |
| [Readable store](../store) | 1 | — |

## Depended on by

Who reaches into this layer.

| Layer | Imports | Package boundary |
| --- | --- | --- |
| [Simulation](.) | 2 | — |
| [Worker entry points](../workers) | 2 | — |

## Notes

# Monte Carlo Simulator

## Goal

`MonteCarloSimulator` runs many independent SDCPN simulations with bounded
frame memory. It is separate from the interactive simulator: it does not retain
frame history, does not expose engine frame storage, and is designed for batch
statistics work.

Each run uses the same extension-aware SDCPN sanitization as the interactive
simulator, so disabled lambda, kernel, colour, dynamics, or parameter surfaces
are not compiled for Monte Carlo runs either.

## Concepts

| Name                    | Meaning                                                                                                     |
| ----------------------- | ----------------------------------------------------------------------------------------------------------- |
| `MonteCarloSimulator`   | Orchestrates a group of independent runs.                                                                   |
| `MonteCarloRun`         | One logical simulation run with its own seed, parameters, initial marking, status, time, and frame buffers. |
| `MonteCarloFrameBuffer` | Internal reusable binary storage for one frame. Each run owns a current buffer and next buffer.             |
| `advanceAll()`          | Deterministic round-robin scheduler. Advances every active run by one frame.                                |
| `runUntilComplete()`    | Repeats `advanceAll()` until all runs are complete/errored or a guard limit is reached.                     |

## Memory Model

Each run owns two reusable `ArrayBuffer`s:

- `currentFrame`: the current frame.
- `nextFrame`: the write target for the next frame.

Each step writes into `nextFrame`, then swaps the two pointers. The simulator
does not append frames to a history array.

The frame buffer stores:

- per-place token counts
- per-place token value offsets
- per-transition elapsed time
- per-transition firing counts
- per-transition fired flags
- contiguous `Float64` token values

The token value section has a capacity. During transition execution the
simulator calculates the next token value count from removals and additions.
If the target buffer cannot hold it, that run reallocates a larger buffer and
continues. Reallocation is per run and tracked in run summaries.

The current growth policy doubles token value capacity on reallocation:

```text
nextCapacity = max(requiredTokenValueCount, currentCapacity * 2, 8)
```

Future versions should make this smarter. Static analysis could estimate the
maximum expansion ratio from arc weights and output token dimensions. Dynamic
analysis could adjust the growth ratio per run based on observed growth rate.

## Scheduling

The current strategy is single-threaded deterministic round-robin. This keeps
all runs moving together and avoids one long run starving the others. Future
parallelization can shard `MonteCarloRun`s across Web Workers without changing
the run state model.

## Current Limits

This first implementation focuses on bounded frame retention and a testable API.
It still reuses the existing user-code object API for dynamics, lambda, and
transition kernels. Future IR compilation can make those functions operate
directly on numeric buffers.

## Source

21 files resolve to this layer, rooted at [`libs/@hashintel/petrinaut-core/src/simulation/monte-carlo`](https://github.com/hashintel/hash/blob/main/libs/@hashintel/petrinaut-core/src/simulation/monte-carlo) — files under a sub-layer's folder belong to that sub-layer instead. The full list is in `architecture.json`.
