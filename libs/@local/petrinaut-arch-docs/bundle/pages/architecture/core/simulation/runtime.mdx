---
title: "Simulation controller"
description: "Host-side controller for a run — owns the transport, the frame store and the status streams"
sidebar_order: 1018
---

> Host-side controller for a run — owns the transport, the frame store and the status streams

**Package** `@hashintel/petrinaut-core` · **Layer id** `core.simulation.runtime` · **Files** 3 · **Lines** 388

Declared in [`libs/@hashintel/petrinaut-core/src/simulation/runtime/README.md`](https://github.com/hashintel/hash/blob/main/libs/@hashintel/petrinaut-core/src/simulation/runtime/README.md).

## Public seams

Import specifiers through which this layer is reachable from outside its package.

- `@hashintel/petrinaut-core`

## Boundaries

Crossing one of these is never a plain function call.

| Kind | What may not cross | Declared in |
| --- | --- | --- |
| `thread` | Talks to the worker only through the transport; it holds no reference to engine state | [README.md](https://github.com/hashintel/hash/blob/main/libs/@hashintel/petrinaut-core/src/simulation/runtime/README.md) |

## Depends on

Aggregated from real TypeScript imports.

| Layer | Imports | Package boundary |
| --- | --- | --- |
| [Headless core](..) | 4 | — |
| [Simulation](.) | 3 | — |
| [Simulation worker](worker) | 3 | — |
| [Simulation engine](engine) | 1 | — |
| [Frames & metrics](frames) | 1 | — |
| [Readable store](../store) | 1 | — |
| [Document types](../types) | 1 | — |

## Depended on by

Who reaches into this layer.

| Layer | Imports | Package boundary |
| --- | --- | --- |
| [Simulation](.) | 2 | — |
| [Monte Carlo runtime](monte-carlo) | 1 | — |

## Notes

# Simulation controller

The main-thread half of a run. `createSimulation` here is what a host actually
calls; the engine and worker below it never talk to the host directly.

- `transport.ts` — the message channel to the worker, and the ack/backpressure
  protocol that decides when the worker may compute more.
- `frame-store.ts` — retains computed frames so the host can scrub back through
  a run.
- `simulation.ts` — assembles the two into a handle exposing `status`, `frames`,
  `getFrame`, `ack` and `setBackpressure`.

This split is why the same engine serves both execution paths: an interactive run
keeps every frame in the store here, while an experiment reuses two buffers in
the worker and never populates a store at all.

## Source

3 files resolve to this layer, rooted at [`libs/@hashintel/petrinaut-core/src/simulation/runtime`](https://github.com/hashintel/hash/blob/main/libs/@hashintel/petrinaut-core/src/simulation/runtime) — files under a sub-layer's folder belong to that sub-layer instead. The full list is in `architecture.json`.
