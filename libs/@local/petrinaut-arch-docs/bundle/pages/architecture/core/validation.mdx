---
title: "Validation"
description: "Structural integrity validators for SDCPN entities, enforcing naming conventions"
sidebar_order: 1022
---

> Structural integrity validators for SDCPN entities, enforcing naming conventions

**Package** `@hashintel/petrinaut-core` · **Layer id** `core.validation` · **Files** 3 · **Lines** 130

Declared in [`libs/@hashintel/petrinaut-core/src/validation/README.md`](https://github.com/hashintel/hash/blob/main/libs/@hashintel/petrinaut-core/src/validation/README.md).

## Invariants

- Pure-function wrappers over Zod schemas, so validators are callable from any layer without side effects — [README.md](https://github.com/hashintel/hash/blob/main/libs/@hashintel/petrinaut-core/src/validation/README.md)

## Depended on by

Who reaches into this layer.

| Layer | Imports | Package boundary |
| --- | --- | --- |
| [Schemas](schemas) | 5 | — |
| [Headless core](.) | 3 | — |

## Notes

# validation/

Structural integrity validators for SDCPN entities. These enforce naming
conventions (PascalCase for places/transitions, lower_snake_case for parameter
variable names) using Zod schemas with pure-function wrappers.

Currently used by the property-panel UI components to validate on blur.

**@todo FE-521:** These validators should also be enforced in `MutationProvider`
(as a safety net) and surfaced in the Diagnostics tab (so that files
loaded with pre-existing invalid names show warnings).

## Source

3 files resolve to this layer, rooted at [`libs/@hashintel/petrinaut-core/src/validation`](https://github.com/hashintel/hash/blob/main/libs/@hashintel/petrinaut-core/src/validation) — files under a sub-layer's folder belong to that sub-layer instead. The full list is in `architecture.json`.
