---
title: "Schemas"
description: "Zod schemas for document entities, metrics and scenarios, and the descriptions the AI tools read"
sidebar_order: 1012
---

> Zod schemas for document entities, metrics and scenarios, and the descriptions the AI tools read

**Package** `@hashintel/petrinaut-core` · **Layer id** `core.schemas` · **Files** 3 · **Lines** 553

Declared in [`libs/@hashintel/petrinaut-core/src/schemas/README.md`](https://github.com/hashintel/hash/blob/main/libs/@hashintel/petrinaut-core/src/schemas/README.md).

## Invariants

- Every entity schema ends in `satisfies z.ZodType<T>` against the canonical type, so a schema cannot drift from the type it validates — [README.md](https://github.com/hashintel/hash/blob/main/libs/@hashintel/petrinaut-core/src/schemas/README.md)

## Depends on

Aggregated from real TypeScript imports.

| Layer | Imports | Package boundary |
| --- | --- | --- |
| [Validation](validation) | 5 | — |
| [Document types](types) | 3 | — |
| [Headless core](.) | 1 | — |
| [Simulation engine](simulation/engine) | 1 | — |

## Depended on by

Who reaches into this layer.

| Layer | Imports | Package boundary |
| --- | --- | --- |
| [Headless core](.) | 3 | — |
| [File format](file-format) | 3 | — |
| [Clipboard](clipboard) | 1 | — |

## Notes

# Schemas

Zod definitions for the document's vocabulary: entities (places, transitions,
arcs, colours, parameters), metrics, and scenarios.

Two things make this layer more than boilerplate.

**Schemas are bound to the canonical types.** The types themselves live in
`../types/sdcpn.ts`; each schema here closes with
`satisfies z.ZodType<Place>` (or the equivalent). The direction matters: the type
is authoritative and the schema is checked against it, so adding a field to the
type without adding it to the schema is a compile error rather than a validator
that silently accepts incomplete documents.

**`.meta({ description })` is API surface, not a comment.** Those descriptions
are what the AI tools present when reasoning about a net, so they document
user-visible consequences — for example that renaming a place breaks every
lambda, kernel, metric, visualizer and scenario reference to it. Editing a
description changes what the assistant tells users, so treat it with the same
care as the code it describes.

## Source

3 files resolve to this layer, rooted at [`libs/@hashintel/petrinaut-core/src/schemas`](https://github.com/hashintel/hash/blob/main/libs/@hashintel/petrinaut-core/src/schemas) — files under a sub-layer's folder belong to that sub-layer instead. The full list is in `architecture.json`.
