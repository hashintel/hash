---
title: "File format"
description: "Reads and writes the on-disk SDCPN document format, plus export converters"
sidebar_order: 1005
---

> Reads and writes the on-disk SDCPN document format, plus export converters

**Package** `@hashintel/petrinaut-core` · **Layer id** `core.file-format` · **Files** 5 · **Lines** 576

Declared in [`libs/@hashintel/petrinaut-core/src/file-format/README.md`](https://github.com/hashintel/hash/blob/main/libs/@hashintel/petrinaut-core/src/file-format/README.md).

## Invariants

- Parsing is the only entry point for untrusted document input, so it validates rather than trusting shape — [README.md](https://github.com/hashintel/hash/blob/main/libs/@hashintel/petrinaut-core/src/file-format/README.md)

## Depends on

Aggregated from real TypeScript imports.

| Layer | Imports | Package boundary |
| --- | --- | --- |
| [Document types](types) | 4 | — |
| [Schemas](schemas) | 3 | — |
| [Headless core](.) | 2 | — |

## Depended on by

Who reaches into this layer.

| Layer | Imports | Package boundary |
| --- | --- | --- |
| [Headless core](.) | 5 | — |
| [Actual mode](actual-mode) | 1 | — |

## Notes

# File format

Serialisation for saved nets: `parse-sdcpn-file.ts` and `serialize-sdcpn.ts` are
the round-trip pair, `remove-visual-info.ts` strips canvas positions for diffing
or headless use, and `sdcpn-to-tikz.ts` exports a net as TikZ for papers.

Anything that reads a file a user supplied goes through the parser here.
Schema migration for older documents lives alongside the document model, not in
this layer — this layer's job ends at producing a well-formed current-version
document.

## Source

5 files resolve to this layer, rooted at [`libs/@hashintel/petrinaut-core/src/file-format`](https://github.com/hashintel/hash/blob/main/libs/@hashintel/petrinaut-core/src/file-format) — files under a sub-layer's folder belong to that sub-layer instead. The full list is in `architecture.json`.
