{
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["./dist/**"]
    },
    "codegen": {
      // Outputs of GraphQL generation
      "outputs": ["./src/**/*.gen.*"],
      "dependsOn": ["^build"]
    }
  }
}
