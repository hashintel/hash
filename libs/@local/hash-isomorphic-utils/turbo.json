{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["codegen", "^build"],
      "outputs": ["./dist/**"]
    },
    "codegen": {
      // Outputs of GraphQL generation
      "outputs": ["./src/**/*.gen.*"]
    },
    "test:unit": {
      "dependsOn": ["codegen", "^build"]
    }
  }
}
