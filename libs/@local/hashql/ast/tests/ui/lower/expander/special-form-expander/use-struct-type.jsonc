//@ run: fail
//@ description: struct that are annotated with a type should fail
[
  "::kernel::special_form::use",
  "::core::math",
  {
    "#struct": { "x": "x" },
    "#type": "X"
    //~^ ERROR type annotations are not allowed on the import list
  },
  { "#literal": 1 }
]
