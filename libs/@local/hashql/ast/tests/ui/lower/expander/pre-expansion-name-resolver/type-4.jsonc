//@ run: pass
//@ description: Test that `type/4` is resolved to `::kernel::special_form::type`, but that no aliases are created.
["type", "a", "Integer", ["+", { "#literal": 2 }, { "#literal": 3 }]]
