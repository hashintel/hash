//@ run: fail
//@ description: Generic constraints with bounds should be sanitized and error out
[
  "`+`<T: Number>",
  //~^ ERROR `add` does not accept generic arguments
  { "#literal": 1 },
  { "#literal": 2 }
]
