//@ run: pass
//@ description: Chained constant folding where a folded result is used in another comparison
// (1 == 2) folds to false, then (false == false) folds to true.
[
  "if",
  { "#literal": true },
  [
    "let",
    "cmp",
    ["==", { "#literal": 1 }, { "#literal": 2 }],
    ["==", "cmp", { "#literal": false }]
  ],
  { "#literal": false }
]
