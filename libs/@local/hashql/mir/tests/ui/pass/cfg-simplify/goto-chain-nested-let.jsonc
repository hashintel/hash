//@ run: pass
//@ description: Goto chaining through multiple nested let bindings
// Multiple sequential lets create a chain of blocks that should all merge.
// Before: bb0 -> bb1 (let a) -> bb2 (let b) -> bb3 (let c) -> bb4 (return)
// After: single block with all assignments
[
  "let",
  "a",
  { "#literal": 1 },
  [
    "let",
    "b",
    { "#literal": 2 },
    [
      "let",
      "c",
      { "#literal": 3 },
      "c"
    ]
  ]
]
