//@ run: pass
//@ description: Switch folding with constant comparison result
// The comparison `2 <= 3` evaluates to a constant true at compile time.
// This tests that constant propagation feeds into switch simplification.
[
  "if",
  ["<=", { "#literal": 2 }, { "#literal": 3 }],
  { "#literal": "less-or-equal" },
  { "#literal": "greater" }
]
