//@ run: pass
//@ description: Switch with identical targets degenerates to goto
// When both branches of an if return the same value, the switch is redundant.
// Before: switch_int(cond) -> [0: bb_same, 1: bb_same]
// After: goto bb_same (with the switch removed)
[
  "let",
  "x",
  { "#literal": 42 },
  [
    "if",
    ["<=", "x", { "#literal": 50 }],
    "x",
    "x"
  ]
]
