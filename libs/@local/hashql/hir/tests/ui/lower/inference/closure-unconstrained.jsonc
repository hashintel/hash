//@ run: pass
//@ description: Closure inference
// This **should** fail during type checking, because an arbitrary `T` cannot be used during `math.add` operations.
[
  "let",
  "add",
  [
    "fn",
    { "#tuple": ["T"] },
    { "#struct": { "a": "T", "b": "T" } },
    "T",
    ["+", "a", "b"]
  ],
  ["add", { "#literal": 1 }, { "#literal": 2 }]
]
