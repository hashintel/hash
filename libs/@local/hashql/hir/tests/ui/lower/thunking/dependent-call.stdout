════ Initial HIR ═══════════════════════════════════════════════════════════════

let a:0 = 2,
    b:0 = 3
in
::core::cmp::gte(a:0, b:0)

════ HIR after thunking ════════════════════════════════════════════════════════

let a:0 = thunk -> 2,
    b:0 = thunk -> 3,
    %2 = thunk ->
        let %3 = a:0(),
            %4 = b:0(),
            %5 = %3 >= %4
        in
        %5
in
%2
