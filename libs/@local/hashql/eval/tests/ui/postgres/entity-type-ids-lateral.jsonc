//@ run: pass
//@ description: Entity type IDs access triggers LEFT JOIN LATERAL subquery with unnest + jsonb_agg
// biome-ignore format: readability
["::graph::tail::collect",
  ["::graph::body::filter",
    ["::graph::head::entities", ["input", "time_axis", "_"]],
    ["fn", { "#tuple": [] }, { "#struct": { "vertex": "_" } }, "_",
      ["==",
        "vertex.metadata.entity_type_ids",
        ["input", "expected_types", "List<::graph::types::ontology::VersionedUrl>"]
      ]
    ]
  ]
]
