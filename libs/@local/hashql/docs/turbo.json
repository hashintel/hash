{
  "extends": ["//"],
  "tasks": {
    "manifest": {
      "dependsOn": ["^manifest", "railroad-plugin#manifest"]
    },
    "build": {
      "dependsOn": ["railroad-plugin#build:wasm", "^manifest"],
      "outputs": ["dist/main.html"]
    },
    "dev": {
      "dependsOn": ["railroad-plugin#build:wasm"]
    }
  }
}
