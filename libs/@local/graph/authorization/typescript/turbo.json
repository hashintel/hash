{
  "extends": ["//"],
  "tasks": {
    "codegen": {
      "dependsOn": ["^build", "@rust/hash-graph-authorization#build:types"],
      "inputs": [
        "$TURBO_DEFAULT$",
        {
          "mode": "dependencyOutputs",
          "globs": ["types/index.snap.d.ts"],
          "from": ["@rust/hash-graph-authorization#build:types"]
        }
      ],
      "outputs": ["src/generated/**"]
    },
    "build": {
      "outputs": ["dist/**"]
    }
  }
}
