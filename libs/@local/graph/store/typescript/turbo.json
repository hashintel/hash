{
  "extends": ["//"],
  "tasks": {
    "codegen": {
      "dependsOn": ["@rust/hash-graph-store#build:types"],
      "inputs": [
        "$TURBO_DEFAULT$",
        {
          "mode": "dependencyOutputs",
          "globs": ["types/index.snap.d.ts"],
          "from": ["@rust/hash-graph-store#build:types"]
        }
      ],
      "outputs": ["src/generated/**"]
    },
    "build": {
      "dependsOn": [
        "codegen",
        "@blockprotocol/type-system#build",
        "@local/hash-graph-authorization#build",
        "^manifest"
      ],
      "outputs": ["dist/**"]
    }
  }
}
