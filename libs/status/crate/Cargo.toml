[package]
name = "hash-status"
version = "0.0.0"
edition = "2021"
description = "The HASH Status and Error Model."

[dependencies]
error-stack = { version = "0.2.4" }
serde = { version = "1.0.152", features = ["derive"] }
serde_json = "1.0.91"
