{
  "extends": ["//"],
  "tasks": {
    "codegen": {
      // Outputs of GraphQL generation
      "outputs": ["./src/**/*.gen.*"],
      "dependsOn": ["@local/hash-isomorphic-utils#build"]
    },
    "test:integration": {
      "dependsOn": ["codegen", "^build", "^start:test:healthcheck"]
    }
  }
}
