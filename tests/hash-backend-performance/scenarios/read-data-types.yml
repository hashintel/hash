scenarios:
  - name: Read all data types
    flow:
      - post:
          url: /
          json:
            query: |
              query {
                queryDataTypes(
                  constrainsValuesOn: { outgoing: 0 }
                  includeArchived: true
                ) {
                  vertices
                }
              }
          expect:
            - notHasProperty: errors
